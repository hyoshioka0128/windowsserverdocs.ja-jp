---
ms.assetid: 8738c03d-6ae8-49a7-8b0c-bef7eab81057
title: "集約型アクセス ポリシー (デモンストレーション手順) の展開します。"
description: 
author: billmath
ms.author: billmath
manager: femila
ms.date: 05/31/2017
ms.topic: article
ms.prod: windows-server-threshold
ms.technology: identity-adds
ms.openlocfilehash: 16973b32407985ffc3f66bf5ac579384a756b5d0
ms.sourcegitcommit: db290fa07e9d50686667bfba3969e20377548504
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 12/12/2017
---
# <a name="deploy-a-central-access-policy-demonstration-steps"></a>集約型アクセス ポリシー (デモンストレーション手順) の展開します。

>適用対象: Windows Server 2016、Windows Server 2012 R2、Windows Server 2012

このシナリオでは、金融部門のセキュリティの運用はファイル サーバーに保管されたアーカイブ金融情報を保護できるように、集約型アクセス ポリシーのニーズを指定する集約型情報セキュリティと連携しています。 各国のアーカイブ金融情報は、同じ国の金融従業員が読み取り専用としてアクセスできます。 集約型金融管理グループは、すべての国の金融情報にアクセスできます。  
  
集約型アクセス ポリシーを展開するには、次のフェーズが含まれます。  
  
|フェーズ|説明  
|---------|---------------  
|[計画: ポリシーのニーズおよび展開に必要な構成を識別します。](Deploy-a-Central-Access-Policy--Demonstration-Steps-.md#BKMK_1.2)|ポリシーと展開に必要な構成の必要性を識別します。 
|[実装: コンポーネントおよびポリシーを構成します。](Deploy-a-Central-Access-Policy--Demonstration-Steps-.md#BKMK_1.3)|コンポーネントおよびポリシーを構成します。  
|[集約型アクセス ポリシーを展開します。](Deploy-a-Central-Access-Policy--Demonstration-Steps-.md#BKMK_1.4)|ポリシーを展開します。  
|[変更を維持する: およびポリシーのステージング](Deploy-a-Central-Access-Policy--Demonstration-Steps-.md#BKMK_1.5)|ポリシーの変更およびステージングします。 
  
## <a name="BKMK_1.1"></a>テスト環境をセットアップします。  
開始する前に、このシナリオをテストするラボを設定する必要があります。 ラボを設定するための手順で詳しく説明は[付録 b: 設定をテスト環境](Appendix-B--Setting-Up-the-Test-Environment.md)します。  
  
## <a name="BKMK_1.2"></a>計画: ポリシーのニーズおよび展開に必要な構成を識別します。  
ここでは、高度な一連のフェーズでは、計画、展開の支援を要請する手順を説明します。  
  
||手順|使用例|  
|-|--------|-----------|  
|1.1|ビジネスでは、集約型アクセス ポリシーが必要であることを決定します。|ファイル サーバーに格納されている金融情報を保護するのには、金融部門のセキュリティの運用は集約型アクセス ポリシーのニーズを指定する集約型情報セキュリティと連携しています。|  
|1.2|アクセス ポリシーを表現します。|金融ドキュメントは、金融部門のメンバーのみが読み取る必要があります。 金融部門のメンバーが自分の国でドキュメントのみにアクセスする必要があります。 金融管理者のみ書き込みアクセス権が必要です。 FinanceException グループのメンバーに対して例外が許可されます。 このグループは、読み取りアクセス権があります。|  
|1.3|Windows Server 2012 コンストラクトでアクセス ポリシーを表現します。|対象とします。<br /><br />-Resource.Department には Finance が含まれています<br /><br />アクセス規則:<br /><br />-User.Country=Resource.Country と User.department の読み取りを許可する = Resource.Department<br />-User.MemberOf(FinanceAdmin) のフル コントロールを許可します。<br /><br />例外:<br /><br />読み取り memberOf(FinanceException) を許可します。|  
|1.4|ポリシーに必要なファイルのプロパティを決定します。|タグ ファイル:<br /><br />部門<br />-国|  
|1.5|要求の種類と、ポリシーに必要なグループを決定します。|要求の種類:<br /><br />-国<br />部門<br /><br />ユーザー グループ:<br /><br />-FinanceAdmin<br />-FinanceException|  
|1.6|このポリシーを適用する上のサーバーを決定します。|すべての金融ファイル サーバー上でポリシーを適用します。|  
  
## <a name="BKMK_1.3"></a>実装: コンポーネントおよびポリシーを構成します。  
このセクションでは、金融ドキュメントの集約型アクセス ポリシーを展開する例を示します。  
  
|違います|手順|使用例|  
|------|--------|-----------|  
|2.1|要求の種類を作成します。|次の要求の種類を作成します。<br /><br />部門<br />-国|  
|2.2|リソースのプロパティを作成します。|作成し、次のリソース プロパティを有効にします。<br /><br />部門<br />-国|  
|2.3|集約型アクセス規則を構成します。|前のセクションで決定したポリシーが含まれた金融ドキュメント規則を作成します。|  
|2.4|集約型アクセス ポリシー (CAP) を構成します。|金融ポリシーという CAP を作成し、その CAP に金融ドキュメント規則を追加します。|  
|2.5|ファイル サーバーに移行先の集約型アクセス ポリシー|金融ポリシー CAP をファイル サーバーに公開します。|  
|2.6|信頼性情報、複合認証、および Kerberos 防御 KDC のサポートを有効にします。|信頼性情報、複合認証、および contoso.com に対して Kerberos 防御 KDC のサポートを有効にします。|  
  
次の手順で 2 つの要求の種類を作成する: 国と部門。  
  
#### <a name="to-create-claim-types"></a>要求の種類を作成するには  
  
1.  パスワードを使用して contoso \administrator としてログ、Hyper-V マネージャーでサーバー DC1 を開き**pass@word1**します。  
  
2.  Open Active Directory Administrative Center.  
  
3.  をクリックして、**ツリー ビュー アイコン**、展開**ダイナミック アクセス制御**、し、[**要求の種類**します。  
  
    右クリック**要求の種類**、] をクリックして**新規**、] をクリックし、**要求の種類**します。  
  
    > [!TIP]  
    > 開くことも、**要求の種類の作成:**ウィンドウから、**タスク**ウィンドウ。 **タスク**] ウィンドウで、をクリックして**新規**、] をクリックし、**要求の種類**します。  
  
4.  **ソース属性**を一覧表示、属性の一覧をスクロール] をクリックして**部門**します。 これを設定する必要があります、**表示名**] フィールドに**部門**します。 をクリックして**OK**します。  
  
5.  **タスク**] ウィンドウで、をクリックして**新規**、] をクリックし、**要求の種類**します。  
  
6.  **ソース属性**を一覧表示、属性の一覧をスクロール ダウン クリックして、**c**属性 (国名)。 **表示名**フィールドを入力**国**します。  
  
7.  **提案された値**セクションで、**次の値を提案:**、] をクリックし、**追加**します。  
  
8.  **値**と**表示名**フィールドで、「**米国**、] をクリックし、**OK**します。  
  
9. 上記の手順を繰り返します。 **提案された値を追加**] ダイアログ ボックスで、「**JP**で、**値**と**表示名**フィールド、およびクリック**OK**します。  
  
![ソリューション ガイド](media/Deploy-a-Central-Access-Policy--Demonstration-Steps-/PowerShellLogoSmall.gif)Windows PowerShell と同等のコマンド * * *  
  
次の Windows PowerShell コマンドレットまたはコマンドレットは、前の手順と同じ機能を実行します。 表示される場合でも可能性があります複数行に改行書式上の制約のためには、各コマンドレットを 1 つの行を入力します。  
  
 
    New-ADClaimType country -SourceAttribute c -SuggestedValues:@((New-Object Microsoft.ActiveDirectory.Management.ADSuggestedValueEntry("US","US","")), (New-Object Microsoft.ActiveDirectory.Management.ADSuggestedValueEntry("JP","JP","")))  
    New-ADClaimType department -SourceAttribute department  
      
 
  
> [!TIP]  
> Active Directory 管理センターで Windows PowerShell 履歴ビューアーを使用すると、Active Directory 管理センターで実行する各手順の Windows PowerShell コマンドレットを検索します。 詳細については、次を参照してください[Windows PowerShell 履歴ビューアー。](https://technet.microsoft.com/library/hh831702)  
  
次の手順では、リソースのプロパティを作成します。 次の手順では、ファイル サーバーを使用できるように、ドメイン コントローラーでグローバル リソース プロパティ リストに自動的に追加されているリソース プロパティを作成します。  
  
#### <a name="to-create-and-enable-pre-created-resource-properties"></a>作成して、事前に作成したリソースのプロパティを有効にするには  
  
1.  Active Directory 管理センターの左側のウィンドウでをクリックして**ツリー ビュー**します。 展開**ダイナミック アクセス制御**、し、[**リソース プロパティ**します。  
  
2.  右クリック**リソース プロパティ**、] をクリックして**新規**、] をクリックし、**参照用リソース プロパティ**します。  
  
    > [!TIP]  
    > リソース プロパティからも選択できます、**タスク**ウィンドウ。 をクリックして**新規**] をクリックし、**参照用リソース プロパティ**します。  
  
3.  **選択を共有する要求の種類の推奨値リスト**、] をクリックして**国**します。  
  
4.  **表示名**フィールドを入力**国**、] をクリックし、**OK**します。  
  
5.  をダブルクリック、**リソース プロパティ**一覧で、下にスクロールして、**部門**リソース プロパティです。 右クリックし、をクリックして**を有効にする**します。 これにより、組み込み**部門**リソース プロパティです。  
  
6.  **リソース プロパティ**一覧 Active Directory 管理センターのナビゲーション ウィンドウでことが 2 つの有効なリソースのプロパティ。  
  
    -   国  
  
    -   部門  
  
![ソリューション ガイド](media/Deploy-a-Central-Access-Policy--Demonstration-Steps-/PowerShellLogoSmall.gif)Windows PowerShell と同等のコマンド * * *  
  
次の Windows PowerShell コマンドレットまたはコマンドレットは、前の手順と同じ機能を実行します。 表示される場合でも可能性があります複数行に改行書式上の制約のためには、各コマンドレットを 1 つの行を入力します。  
  
```  
New-ADResourceProperty Country -IsSecured $true -ResourcePropertyValueType MS-DS-MultivaluedChoice -SharesValuesWith country  
Set-ADResourceProperty Department_MS -Enabled $true  
Add-ADResourcePropertyListMember "Global Resource Property List" -Members Country  
Add-ADResourcePropertyListMember "Global Resource Property List" -Members Department_MS  
  
```  
  
次の手順では、リソースにアクセスできるユーザーを定義する集約型アクセス規則を作成します。 このシナリオでは、ビジネス規則です。  
  
-   金融ドキュメントは、金融部門のメンバーのみが読み取ることができます。  
  
-   金融部門のメンバーが自分の国のドキュメントのみにアクセスできます。  
  
-   金融管理者のみが書き込みアクセス権を持つことができます。  
  
-   FinanceException グループのメンバーに対しては例外を許可しました。 このグループは、読み取りアクセス権があります。  
  
-   管理者およびドキュメント所有者はのフル アクセスが続行します。  
  
または、Windows Server 2012 コンストラクトで規則を表現します。  
  
対象とする: Resource.Department が含まれています Finance には  
  
アクセス規則:  
  
-   読み取り User.Country=Resource.Country および User.department を許可する = Resource.Department  
  
-   フル コントロール User.MemberOf(FinanceAdmin) を許可します。  
  
-   読み取り User.MemberOf(FinanceException) を許可します。  
  
#### <a name="to-create-a-central-access-rule"></a>集約型アクセス規則を作成するには  
  
1.  Active Directory 管理センターの左側のウィンドウでをクリックして**ツリー ビュー**[**ダイナミック アクセス制御**、] をクリックし、**集約型アクセス規則**します。  
  
2.  右クリック**集約型アクセス規則**、] をクリックして**新規**、] をクリックし、**集約型アクセス規則**します。  
  
3.  **名**フィールドを入力**金融ドキュメント規則**します。  
  
4.  **ターゲット リソース**セクションで、をクリックして**編集**、し、[、**集約型アクセス規則**ダイアログ ボックスで、をクリックして**条件を追加**します。 次の条件を追加します。   
    [**リソース**][**部門**][**等しい**][**Value**][**Finance**]、をクリックし、**OK**します。  
  
5.  **アクセス許可**セクションで、**次のアクセス権を使用して現在のアクセス許可として**、] をクリックして**編集**、し、[、**セキュリティのアクセス許可の詳細設定**] ダイアログ ボックスをクリックして**追加**します。  
  
    > [!NOTE]  
    > **提案されたアクセス許可として次のアクセス許可を使用して**オプションでは、ステージング ポリシーを作成することができます。 これを行う方法について、管理を参照してください: 変更とステージ、ポリシー」セクションを参照します。  
  
6.  **アクセス許可のアクセス許可エントリ**ダイアログ ボックスで、] をクリックして**プリンシパルの選択**、種類**Authenticated Users**、] をクリックし、**[OK]**します。  
  
7.  **アクセス許可のアクセス許可エントリ**ダイアログ ボックスで、] をクリックして**条件を追加**、し、次の条件を追加します。   
    [**User**][**国**][**Any of**][**リソース**][**国**]   
     をクリックして**条件を追加**します。   
     [**And**]   
    をクリックして [**ユーザー**] [**部門**] [**のいずれかの**] [**リソース**] [**部門**] です。 設定、**アクセス許可**に**読み取り**します。  
  
8.  をクリックして**OK**、] をクリックし、**追加**します。 をクリックして**プリンシパルの選択**、種類**FinanceAdmin**、] をクリックし、**OK**します。  
  
9. 選択、**変更、読み取りと実行、読み取り、書き込み**アクセス許可、およびクリック**OK**します。  
  
10. をクリックして**追加**、] をクリックして**プリンシパルの選択**、種類**FinanceException**、] をクリックし、**OK**します。 アクセス許可を選択**読み取り**と**読み取りと実行**します。  
  
11. をクリックして**OK** 3 回を完了し、Active Directory 管理センターに戻ります。  
  
    ![ソリューション ガイド](media/Deploy-a-Central-Access-Policy--Demonstration-Steps-/PowerShellLogoSmall.gif)Windows PowerShell と同等のコマンド * * *  
  
    次の Windows PowerShell コマンドレットまたはコマンドレットは、前の手順と同じ機能を実行します。 表示される場合でも可能性があります複数行に改行書式上の制約のためには、各コマンドレットを 1 つの行を入力します。  
  
 
    $countryClaimType Get-ADClaimType 国を =  
    $departmentClaimType Get-ADClaimType 部門を =  
    $countryResourceProperty Get-ADResourceProperty 国を =  
    $departmentResourceProperty Get-ADResourceProperty 部門を =  
    $currentAcl ="O:SYG:SYD:AR(A;;FA;; OW) (A;FA;;BA) (A; 0x1200a9;;S-1-5-21-1787166779-1215870801-2157059049-1113) (A; 0x1301bf;;S-1-5-21-1787166779-1215870801-2157059049-1112) (A;FA;;SY) (XA; 0x1200a9;;AU;((@USER." + $countryClaimType.Name +"Any_of @RESOURCE." + $countryResourceProperty.Name +") & & (@USER." + $departmentClaimType.Name +"Any_of @RESOURCE." + $departmentResourceProperty.Name +")))]  
    $resourceCondition ="(@RESOURCE." + $departmentResourceProperty.Name +"Contains {`"Finance`"})"  
    New-ADCentralAccessRule「金融ドキュメント規則」- CurrentAcl $currentAcl ResourceCondition $resourceCondition  

  
> [!IMPORTANT]  
> 上記のコマンドレット例では、FinanceAdmin グループのセキュリティ識別子 (Sid) ユーザーは作成時に決定され、表示されるが、この例では異なります。 たとえば、提供されているの SID の値 (S-1-5-21-1787166779-1215870801-2157059049-1113)、使用する必要がありますはで、展開を作成する必要がある FinanceAdmin グループの SID を実際に置き換えられます。 Windows PowerShell を使用して、このグループの SID の値を検索し、その値を変数に割り当てる、ここで、変数を使用することができます。 詳細については、次を参照してください。[Windows PowerShell ヒント: Sid の操作](https://go.microsoft.com/fwlink/?LinkId=253545)します。  
  
同じ国および同じ部門のドキュメントにアクセスするユーザーを許可する集約型アクセス規則が作成されました。 この規則により、FinanceAdmin グループはドキュメントを編集して、FinanceException グループはドキュメントを読み取ることができます。 このルールでは、金融として分類されているドキュメントのみが対象です。  
  
#### <a name="to-add-a-central-access-rule-to-a-central-access-policy"></a>集約型アクセス ポリシーに集約型アクセス規則を追加するには  
  
1.  Active Directory 管理センターの左側のウィンドウでをクリックして**ダイナミック アクセス制御**、] をクリックし、**集約型アクセス ポリシー**します。  
  
2.  **タスク**] ウィンドウで、をクリックして**新規**、] をクリックし、**集約型アクセス ポリシー**します。  
  
3.  **集約型アクセス ポリシーの作成:**、種類**金融ポリシー**で、**名**ボックス。  
  
4.  **メンバー集約型アクセス規則**、] をクリックして**追加**します。  
  
5.  をダブルクリック、**金融ドキュメント規則**、[追加] に、**次の集約型アクセス規則の追加**一覧と、クリックして**[OK]**します。  
  
6.  をクリックして**OK**を完了します。 金融ポリシーという集約型アクセス ポリシーが作成されました。  
  
    ![ソリューション ガイド](media/Deploy-a-Central-Access-Policy--Demonstration-Steps-/PowerShellLogoSmall.gif)Windows PowerShell と同等のコマンド * * *  
  
    次の Windows PowerShell コマンドレットまたはコマンドレットは、前の手順と同じ機能を実行します。 表示される場合でも可能性があります複数行に改行書式上の制約のためには、各コマンドレットを 1 つの行を入力します。  
  
    ```  
    New-ADCentralAccessPolicy "Finance Policy" Add-ADCentralAccessPolicyMember   
    -Identity "Finance Policy"   
    -Member "Finance Documents Rule"  
  
    ```  
  
#### <a name="to-apply-the-central-access-policy-across-file-servers-by-using-group-policy"></a>グループ ポリシーを使用してファイル サーバー全体で集約型アクセス ポリシーを適用するには  
  
1.  **開始**画面で、**検索**ボックスに、入力**グループ ポリシーの管理**します。 ダブルクリック**グループ ポリシー管理**します。  
  
    > [!TIP]  
    > 場合、**管理ツールを表示**設定が無効になっている、**管理ツール**にフォルダーとその内容は表示されません、**設定**結果。  
  
    > [!TIP]  
    > 実稼働環境では、ファイル サーバー組織単位 (OU) を作成し、このポリシーを適用するすべてのファイル サーバーをこの OU に追加する必要があります。 グループ ポリシーを作成し、そのポリシーをこの OU に追加できます。  
  
2.  作成したグループ ポリシー オブジェクトを編集するこの手順で[ビルド ドメイン コントローラー](Appendix-B--Setting-Up-the-Test-Environment.md#BKMK_Build)」セクションをテスト環境を作成した集約型アクセス ポリシーが含まれます。 グループ ポリシー管理エディターでに移動して、ドメイン (この例では contoso.com) の組織単位を選択します。**グループ ポリシーの管理**、**フォレスト: contoso.com**、**ドメイン**、**contoso.com**、**Contoso**、**FileServerOU**します。  
  
3.  右クリック**FlexibleAccessGPO**、] をクリックし、**編集**します。  
  
4.  グループ ポリシー管理エディター] ウィンドウに移動**コンピューターの構成**、展開**ポリシー**、展開**Windows 設定**、] をクリック**セキュリティ設定**します。  
  
5.  展開**ファイル システム**、右クリックして**集約型アクセス ポリシー**、] をクリックし、**管理集約型アクセス ポリシー**します。  
  
6.  **集約型アクセス ポリシー構成**] ダイアログ ボックスで、追加**金融ポリシー**、] をクリックし、**OK**します。  
  
7.  下にスクロールして**高度な監査ポリシー構成**、これを展開します。  
  
8.  展開**監査ポリシー**、] を選択して**オブジェクトへのアクセス**します。  
  
9. ダブルクリック**集約型アクセス ポリシー ステージングの監査**します。 次の 3 つすべてのチェック ボックスを選択し、クリックして**OK**します。 この手順により、集約型アクセス ステージング ポリシーに関連した監査イベントを受信するシステムです。  
  
10. ダブルクリック**ファイル システムのプロパティの監査**します。 次の 3 つすべてのチェック ボックスを選択し、[ **OK**します。  
  
11. グループ ポリシー管理エディターを閉じます。 グループ ポリシーに集約型アクセス ポリシーが含まれているようになりました。  
  
信頼性情報またはデバイスの認証データを提供するドメインのドメイン コントローラーのドメイン コントローラーは、ダイナミック アクセス制御をサポートするように構成する必要があります。  
  
#### <a name="to-enable-support-for-claims-and-compound-authentication-for-contosocom"></a>信頼性情報とサポート contoso.com の複合認証を有効にするには  
  
1.  グループ ポリシーの管理を開き、[ **contoso.com**、] をクリックし、**ドメイン コントローラー**します。  
  
2.  右クリック**既定のドメイン コントローラー ポリシー**、] をクリックし、**編集**します。  
  
3.  グループ ポリシー管理エディター] ウィンドウでダブルクリック**コンピューターの構成**、] をダブルクリック**ポリシー**、] をダブルクリック**管理用テンプレート**、] をダブルクリック**システム**、順にダブルクリック**KDC**します。  
  
4.  ダブルクリック**KDC で信頼性情報、サポートは複合認証、および Kerberos 防御**します。 **KDC で信頼性情報、サポートは複合認証、および Kerberos 防御**ダイアログ ボックスで、] をクリックして**有効**選択**サポートされている**から、**オプション**ドロップダウン リスト。 (必要集約型アクセス ポリシーでユーザーの信頼性情報を使用するには、この設定を有効にします。)  
  
5.  閉じる**グループ ポリシー管理**します。  
  
6.  コマンド プロンプトを開き`gpupdate /force`します。  
  
## <a name="BKMK_1.4"></a>集約型アクセス ポリシーを展開します。  
  
||手順|使用例|  
|-|--------|-----------|  
|3.1|ファイル サーバーの適切な共有フォルダーに CAP を割り当てます。|ファイル サーバー上の適切な共有フォルダーに集約型アクセス ポリシーを割り当てます。|  
|3.2|アクセスが適切に構成されていることを確認します。|さまざまな国および部門からのユーザーのアクセスを確認します。|  
  
この手順では、ファイル サーバーに集約型アクセス ポリシーを割り当てます。 前の手順を作成した集約型アクセス ポリシーを受信しているファイル サーバーにログオンされ、共有フォルダに、ポリシーを割り当てます。  
  
#### <a name="to-assign-a-central-access-policy-to-a-file-server"></a>ファイル サーバーに集約型アクセス ポリシーを設定するには  
  
1.  HYPER-V マネージャーでサーバー FILE1 に接続します。 パスワードを使用して contoso \administrator を使用してサーバーにログオンします。**pass@word1**します。  
  
2.  管理者特権でコマンド プロンプトを開き: **gpupdate/force**します。 これにより、グループ ポリシーの変更が、サーバーへの影響を実行します。  
  
3.  また、Active Directory からグローバル リソース プロパティーを更新する必要があります。 ウィンドウを開き、管理者特権で Windows PowerShell、種類`Update-FSRMClassificationpropertyDefinition`します。 入力、] をクリックし、Windows PowerShell を閉じます。  
  
    > [!TIP]  
    > ファイル サーバーにログオンしてグローバル リソース プロパティーを更新することもできます。 ファイル サーバーからグローバル リソース プロパティーを更新するには、次  
    >   
    > 1.  Contoso \administrator としてファイル サーバー FILE1 パスワードを使用してへのログオン**pass@word1**します。  
    > 2.  ファイル サーバー リソース マネージャーを開きます。 ファイル サーバー リソース マネージャーを開くにはクリックして**開始**、種類**ファイル サーバー リソース マネージャー**、] をクリックし、**ファイル サーバー リソース マネージャー**します。  
    > 3.  ファイル サーバー リソース マネージャーで、をクリックして**ファイル分類管理**、右クリックして**分類プロパティ**] をクリックし、**更新**します。  
  
4.  Windows エクスプ ローラーを開き、左側のウィンドウでドライブをクリック D. を右クリックし、**Finance Documents**フォルダー、およびクリック**プロパティ**します。  
  
5.  をクリックして、**分類**] タブ、[**国**、し、[**米国**で、**値**フィールド。  
  
6.  をクリックして**部門**を選択し、**Finance**で、**値**] フィールドにをクリックし、**適用**します。  
  
    > [!NOTE]  
    > 金融部門のターゲット ファイルに集約型アクセス ポリシーが構成されたことに注意してください。 前の手順では、国と部門属性を持つフォルダー内のすべてのドキュメントをマークします。  
  
7.  をクリックして、**セキュリティ**タブをクリックし、をクリックして**詳細**します。 をクリックして、**集約型ポリシー** ] タブ。  
  
8.  をクリックして**変更**[**金融ポリシー**クリックして、ドロップダウン メニューから**適用**します。 表示することができます、**金融ドキュメント規則**ポリシーに一覧表示します。 すべての Active Directory 内の規則を作成するときに設定したアクセス許可を表示する項目を展開します。  
  
9. をクリックして**OK** Windows エクスプ ローラーに戻ります。  
  
次の手順では、アクセスが適切に構成されていることを確認します。  ユーザー アカウントが、適切な部門属性が設定されてする必要があります (これを使用して設定 Active Directory 管理センター)。 新しいポリシーの結果を表示する最も簡単な方法は、使用する、**有効なアクセス**] タブの [Windows エクスプ ローラーでします。 **有効なアクセス**] タブは、特定のユーザー アカウントのアクセス権を示しています。  
  
#### <a name="to-examine-the-access-for-various-users"></a>さまざまなユーザーのアクセスを調べる  
  
1.  HYPER-V マネージャーでサーバー FILE1 に接続します。 Contoso \administrator を使用してサーバーにログオンします。 Windows エクスプ ローラーで D:\ に移動します。 右クリックし、**Finance Documents**フォルダー、およびクリック**プロパティ**します。  
  
2.  をクリックして、**セキュリティ**] タブ、[ **[詳細設定]**、をクリックし、**有効なアクセス**] タブ。  
  
3.  ユーザーのアクセス許可を確認するには、次のようにクリックします。**ユーザーを選択**、ユーザーの名前を入力し、[] をクリック**有効なアクセス許可を表示**に有効なアクセス権を参照してください。 例えば：  
  
    -   Myriam Delesalle (MDelesalle) は金融部門に、フォルダーに読み取りアクセス権を持つ必要があります。  
  
    -   Miles Reid (MReid) は FinanceAdmin グループのメンバーであるし、フォルダーにアクセスの変更が必要です。  
  
    -   Esther Valle (EValle) は金融部門; にはただし、FinanceException グループのメンバーであるし、読み取りアクセス権を持つ必要があります。  
  
    -   Maira Wenzel (MWenzel) は金融部門に、いずれかのメンバーではない、FinanceAdmin グループと FinanceException グループ。 彼女は必要はありません、アクセス フォルダーにします。  
  
    という名前の最後の列通知**アクセス制限**有効なアクセス許可] ウィンドウでします。 この列は、どのゲート ユーザーのアクセス許可を指示します。 この場合は、共有と NTFS アクセス許可のすべてのユーザー フル コントロールが許可されます。 ただし、集約型アクセス ポリシーは、以前に構成した規則に基づいてアクセスを制限します。  
  
## <a name="BKMK_1.5"></a>変更を維持する: およびポリシーのステージング  
  
||||  
|-|-|-|  
|数|手順|使用例|  
|4.1|クライアントのデバイスの信頼性情報を構成します。|グループ ポリシーをデバイスの信頼性情報を有効にする設定します。|  
|4.2|デバイスの信頼性情報を有効にします。|デバイスの国の要求の種類を有効にします。|  
|4.3|ステージング ポリシーを変更したい既存の集約型アクセス規則に追加します。|ステージング ポリシーを追加する金融ドキュメント規則を変更します。|  
|4.4|ステージング ポリシーの結果を表示します。|Ester Velle のアクセス許可を確認します。|  
  
#### <a name="to-set-up-group-policy-setting-to-enable-claims-for-devices"></a>グループ ポリシーを設定するには、設定を有効にするデバイスの信頼性情報します。  
  
1.  DC1 に、開いているグループ ポリシーの管理] をクリックしてログオン**contoso.com**、] をクリックして**Default Domain Policy**を右クリックし、選択**編集**します。  
  
2.  グループ ポリシー管理エディター] ウィンドウに移動**コンピューターの構成**、**ポリシー**、**管理用テンプレート**、**システム**、**Kerberos**します。  
  
3.  選択**信頼性情報、複合認証、および Kerberos 防御の Kerberos クライアント サポート**] をクリック**を有効にする**します。  
  
#### <a name="to-enable-a-claim-for-devices"></a>デバイスの信頼性情報を有効にするには  
  
1.  パスワードを使用して contoso \administrator としてログ、Hyper-V マネージャーでサーバー DC1 を開き**pass@word1**します。  
  
2.  **ツール**] メニューの [Active Directory 管理センターを開きます。  
  
3.  をクリックして**ツリー ビュー**、展開**ダイナミック アクセス制御**、] をダブルクリック**要求の種類**、] をダブルクリックし、**国**要求します。  
  
4.  **この種類の要求発行できるは、次のクラスの**を選択、**コンピューター**チェック ボックスをオンします。 をクリックして**OK**します。   
    両方**ユーザー**と**コンピューター**チェック ボックスが選択されています。 デバイスでユーザーに加えて country 要求を使用できるようになりましたことができます。  
  
次の手順では、ステージング ポリシー規則を作成します。 ステージング ポリシーを使用して、有効にする前に、新規ポリシー エントリの効果を監視することができます。 次の手順では、ステージング ポリシー エントリを作成し、共有フォルダーでその効果をモニターします。  
  
#### <a name="to-create-a-staging-policy-rule-and-add-it-to-the-central-access-policy"></a>ステージング ポリシー規則を作成し、集約型アクセス ポリシーに追加するには  
  
1.  パスワードを使用して contoso \administrator としてログ、Hyper-V マネージャーでサーバー DC1 を開き**pass@word1**します。  
  
2.  Open Active Directory Administrative Center.  
  
3.  をクリックして**ツリー ビュー**、展開**ダイナミック アクセス制御**、] を選択して**集約型アクセス規則**します。  
  
4.  右クリック**金融ドキュメント規則**、] をクリックし、**プロパティ**します。  
  
5.  **提案されたアクセス許可**] セクションで、選択、**を有効にするためのアクセス許可の構成をステージング**チェック ボックスをオン] をクリックして**編集**、] をクリックし、**追加**します。 **提案されたアクセス許可のアクセス許可エントリ**] ウィンドウで、] をクリックして、**プリンシパルの選択**リンクを入力**Authenticated Users**、] をクリックし、**OK**します。  
  
6.  をクリックして、**条件を追加**リンクし、次の条件を追加します。   
     [**User**][**国**][**Any of**][**リソース**][**国**] です。  
  
7.  をクリックして**条件を追加**もう一度、し、次の条件を追加します。  
    [**And**]   
     [**デバイス**][**国**][**Any of**][**リソース**][**国**]  
  
8.  をクリックして**条件を追加**もう一度、し、次の条件を追加します。  
    [と]   
     [**User**][**Group**][**いずれかのメンバー**][**値**] \ (**FinanceException**)  
  
9. FinanceException を設定するには、グループで [**項目を追加**し、[、**ユーザーの選択、コンピューター、サービス アカウントまたはグループ**ウィンドウで、種類**FinanceException**します。  
  
10. をクリックして**アクセス許可**[**フル コントロール**、] をクリック**OK**します。  
  
11. [事前セキュリティ設定の提案されたアクセス許可] ウィンドウの [ **FinanceException** ] をクリック**削除**します。  
  
12. をクリックして**OK** 2 回を完了します。  
  
![ソリューション ガイド](media/Deploy-a-Central-Access-Policy--Demonstration-Steps-/PowerShellLogoSmall.gif)Windows PowerShell と同等のコマンド * * *  
  
次の Windows PowerShell コマンドレットまたはコマンドレットは、前の手順と同じ機能を実行します。 表示される場合でも可能性があります複数行に改行書式上の制約のためには、各コマンドレットを 1 つの行を入力します。  
  
```  
Set-ADCentralAccessRule  
-Identity: "CN=FinanceDocumentsRule,CN=CentralAccessRules,CN=ClaimsConfiguration,CN=Configuration,DC=Contoso.com"  
-ProposedAcl: "O:SYG:SYD:AR(A;;FA;;;BA)(A;;FA;;;SY)(A;;0x1301bf;;;S-1-21=1426421603-1057776020-1604)"  
-Server: "WIN-2R92NN8VKFP.Contoso.com"  
  
```  
  
> [!NOTE]  
> 上記のコマンドレット例では、サーバーの値は、テスト ラボ環境でサーバーを反映します。 Windows PowerShell 履歴ビューアーを使用すると、Active Directory 管理センターで実行する各手順の Windows PowerShell コマンドレットを検索します。 詳細については、次を参照してください[Windows PowerShell 履歴ビューアー。](https://technet.microsoft.com/library/hh831702)  
  
この提案されたアクセス許可セットで、FinanceException グループのメンバーする必要がのフル アクセス ファイル、属している国からドキュメントとして同じ国からデバイスを使用してアクセスしたときにします。 監査エントリでは、セキュリティ ログになった場合、金融部門のだれかが、ファイルにアクセスしようとしています。ファイル サーバーで利用できます。 ただし、ポリシーがステージング環境から昇格するまで、セキュリティの設定は適用されません。  
  
次の手順では、ステージング ポリシーの結果を確認します。 現在のルールに基づくアクセス許可を持っているユーザー名と共有フォルダーにアクセスします。 Esther Valle (EValle) は、FinanceException のメンバーと彼女は現在読み取り権限を備えています。 このステージング ポリシーに従って EValle に権限をまったく必要はありません。  
  
#### <a name="to-verify-the-results-of-the-staging-policy"></a>ステージング ポリシーの結果を確認するには  
  
1.  パスワードを使用して contoso \administrator として上で Hyper-V マネージャーとログ ファイル サーバー FILE1 に接続**pass@word1**します。  
  
2.  コマンド プロンプト ウィンドウを開き**gpupdate/force**します。 これにより、グループ ポリシーの変更が反映されるサーバーにします。  
  
3.  Hyper-V マネージャーでサーバー CLIENT1 に接続します。 現在ログオンしているユーザーをログオフします。 仮想マシン CLIENT1 を再起動します。 [Contoso \evalle を使用してコンピューターにログオンpass@word1します。  
  
4.  デスクトップ \\\FILE1\Finance ドキュメントへのショートカットをダブルクリックします。 Evalle は引き続きアクセスするファイルです。 FILE1 に切り替えます。  
  
5.  開いている**イベント ビューアー**デスクトップのショートカットからです。 展開**Windows ログ**、し、[**セキュリティ**します。 エントリを開きます**Event ID 4818**下にある、**集約型アクセス ポリシー ステージング**タスク カテゴリ。 EValle にアクセスが許可されているが表示されます。ただし、ステージングのポリシーに従って、ユーザーはアクセスが拒否されました。  
  
## <a name="next-steps"></a>次の手順  
System Center Operations Manager などの集約型サーバー管理システムがある場合は、イベントの監視も構成することができます。 これにより、管理者はそれらを適用する前に集約型アクセス ポリシーの効果を監視できます。  
  

