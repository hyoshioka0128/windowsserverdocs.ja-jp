---
title: dir
description: 'Windows コマンド」のトピック * * *- '
ms.custom: na
ms.prod: windows-server-threshold
ms.reviewer: na
ms.suite: na
ms.technology: manage-windows-commands
ms.tgt_pltfrm: na
ms.topic: article
ms.assetid: edcbf69b-eaa4-466e-b210-3dd8892f4d93
author: coreyp-at-msft
ms.author: coreyp
manager: dongill
ms.date: 10/16/2017
ms.openlocfilehash: 37fdcd1f60281eedc4faa9a14e18410b1215b685
ms.sourcegitcommit: 6ef4986391607bb28593852d06cc6645e548a4b3
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 06/07/2019
ms.locfileid: "66811211"
---
# <a name="dir"></a>dir



ディレクトリのファイルとサブディレクトリの一覧を表示します。 パラメーターを指定せずに使用されている場合**dir**ディスクのボリューム ラベルとシリアル番号、続いて (その名前と日付と時刻の各が最後に変更を含む)、ディスク上のディレクトリおよびファイルの一覧が表示されます。 ファイルの場合**dir**名の拡張子とサイズをバイト単位で表示します。 **Dir**もファイルと表示されているディレクトリの累積的なサイズ、およびディスクに残っているバイト単位で空き領域の合計数が表示されます。

このコマンドを使用する方法の例については、[例](#examples)を参照してください。

## <a name="syntax"></a>構文

```
dir [<Drive>:][<Path>][<FileName>] [...] [/p] [/q] [/w] [/d] [/a[[:]<Attributes>]][/o[[:]<SortOrder>]] [/t[[:]<TimeField>]] [/s] [/b] [/l] [/n] [/x] [/c] [/4]
```

## <a name="parameters"></a>パラメーター

|パラメーター|説明|
|---------|-----------|
|[\<ドライブ >:] [<Path>]|一覧を表示するドライブとディレクトリを指定します。|
|[\<ファイル名 >]|特定のファイルまたはファイルの一覧を表示するグループを指定します。|
|/p|一度に、一覧の 1 つの画面が表示されます。 次の画面を表示するには、キーボードの任意のキーを押してください。|
|/q|ファイル所有権情報を表示します。|
|/w|最大 5 個のファイル名または行ごとにディレクトリ名を持つ、ワイド形式で一覧を表示します。|
|/d|一覧と同じ形式で表示されて **/w**列で並べ替えられているファイル。|
|/a[[:]\<Attributes>]|これらのディレクトリと、指定した属性付きのファイルの名前のみが表示されます。 省略した場合 **/a**、 **dir**を除くすべてのファイルの名前を表示します。 非表示とシステム ファイル。 使用する場合 **/a**を指定せず*属性*、 **dir**非表示など、すべてのファイルの名前とシステム ファイルが表示されます。</br>次の一覧では、それぞれのために使用できる値について説明します*属性*します。 コロン (:) を使用します。省略可能です。 これらの値の任意の組み合わせを使用し、スペースを含む値を分離しません。</br>**d**ディレクトリ</br>**h**ファイルを非表示</br>**s**システム ファイル</br>**l**再解析ポイント</br>**r**読み取り専用ファイル</br>**a**アーカイブ ファイル</br>**i**コンテンツ インデックス付きのファイルがありません</br>**-** つまり、"not"プレフィックスします。|
|/o[[:]\<SortOrder>]|に従って出力を並べ替える*SortOrder*、次の値の任意の組み合わせがあることができます。</br>**n**名 (アルファベット順)</br>**e**で拡張機能 (アルファベット順)</br>**g**ディレクトリを最初にグループ化</br>**s**サイズ (小さい順)</br>**d**で日付/時刻 (古い順)</br>**-** 順序を逆にプレフィックス</br>注:コロンを使用しては省略可能です。 複数の値は、一覧表示する順序で処理されます。 スペースを含む複数の値を分離されません。</br>場合*SortOrder*が指定されていない**dir/o**がアルファベット順で並べ替えられても、ファイルを後に、アルファベット順でディレクトリを一覧表示します。|
|/t[[:]\<TimeField>]|表示または並べ替えに使用するには、どの時刻フィールドを指定します。 以下に示しますを使用できる値の各*タイム フィールドに使える*:</br>**c**の作成</br>**a** 最後のアクセス</br>**w**最終書き込み|
|/s|出現するすべての指定したディレクトリ内の指定したファイル名とすべてのサブディレクトリの一覧を表示します。|
|/b|ディレクトリと追加情報のファイル、ベアに一覧表示しません。 **/b**オーバーライド **/w**します。|
|/l|表示では、ディレクトリ名とファイル名を小文字に並べ替えられていません。|
|/n|画面の右端に長いリスト形式で表示します。|
|/x|8dot3 以外のファイル名を生成する短い名前が表示されます。 表示、表示と同じ **/n**、短い名前は、長い名前の前に挿入されますが、します。|
|/c|ファイルのサイズでは、桁区切り記号を表示します。 これは既定の動作です。 使用 **/c**区切り記号を非表示にします。|
|/4|年を 4 桁の形式で表示します。|
|/?|コマンド プロンプトにヘルプを表示します。|

## <a name="remarks"></a>注釈

- 複数を使用する*FileName*パラメーターは、スペース、コンマまたはセミコロンで各ファイル名を区切ります。
- ワイルドカード文字を使用することができます ( **&#42;** または **?** ) ファイル名の 1 つまたは複数の文字を表す、ファイルまたはサブディレクトリのサブセットを表示します。

  **アスタリスク (\*)。** たとえば、文字の任意の文字列の代わりにアスタリスクを使用します。  
  - **dir \*.txt**で始まる、.txt など、.txt、.txt1 .txt_old 拡張子を持つ現在のディレクトリ内のすべてのファイルを一覧表示されます。
  - **読み取る dir\*.txt**で始まる、「読み取り」とは拡張子が .txt、.txt、.txt1、.txt_old などで始まる、現在のディレクトリ内のすべてのファイルを一覧表示されます。
  - **読み取る dir\*.\\** * 現在のディレクトリに「読み取り」で始まる拡張子が付いたファイルすべてを一覧表示されます。

  アスタリスクのワイルドカードは常に使用短いファイル名のマッピング、予期しない結果を取得する可能性があります。 たとえば、次のディレクトリには、2 つのファイル (t.txt2 と t97.txt) が含まれています。 
 
  ```
  C:\test>dir /x
  Volume in drive C has no label.
  Volume Serial Number is B86A-EF32
    
  Directory of C:\test
    
  11/30/2004  01:40 PM <DIR>  .
  11/30/2004  01:40 PM <DIR> ..
  11/30/2004  11:05 AM 0 T97B4~1.TXT t.txt2
  11/30/2004  01:16 PM 0 t97.txt
  ```  

  その入力が想像**dir t97\\** * ファイル t97.txt を返します。 ただし、入力**dir t97\\** * アスタリスクのワイルドカードには、その短い名前マップ T97B4~1.TXT を使用して t97.txt にファイル t.txt2 が一致するために、両方のファイルを返します。 同様に、入力**del t97\\** * 両方のファイルを削除します。

  **疑問符 (?):** 名前の単一の文字の代わりに疑問符 () を使用します。 たとえば、入力**dir を読み取る。txt**を「読み取り」で始まり、最大 3 つの文字が続く、.txt 拡張子を持つ現在のディレクトリ内のファイルを一覧表示されます。 これには、Read.txt、Read1.txt、Read12.txt、Read123.txt、および Readme1.txt がない Readme12.txt が含まれます。
- ファイルの表示属性を指定します。

  使用する場合 **/a**で 1 つ以上の値を持つ*属性*、 **dir**指定のすべての属性を持つファイルのみの名前を表示します。 例では、使用する場合の **/a**で**r**と **-h**属性として (いずれかを使用して **/、: r h**または **/ar-h**)、 **dir**のみを表示を非表示には、読み取り専用ファイルの名前になります。
- ファイル名の並べ替えを指定します。

  1 つ以上を指定する場合*SortOrder*値、 **dir**と 2 番目の条件によって、次の最初の条件では、ファイル名を並べ替えます。 使用する場合など **/o**で、 **e**と **-s**値*SortOrder* (いずれかを使用して**最も-s**または **/oe-s**)、 **dir**最大規模の最初の拡張機能によってディレクトリとファイルの名前を並べ替えます、最終的な結果が表示されます。 拡張機能でアルファベット順に並べ替えるを最初に、表示拡張機能なしでファイル名、ディレクトリ名、およびファイル名拡張子を持つ。
- リダイレクト文字とパイプを使用します。

  リダイレクト文字を使用する場合 ( **>** ) を送信する**dir**出力をファイルまたはパイプ ( **|** ) を送信する**dir**を別のコマンドを使用して出力 **/a:-d**と **/b**ファイル名のみを一覧表示します。 使用することができます*FileName*で **/b**と **/s**ことを指定する**dir**が現在のディレクトリとそのサブディレクトリのすべてのファイルを検索名前と一致する*FileName*します。 **Dir**ドライブ文字、ディレクトリ名、ファイル名、および各ファイルのファイル名拡張子 (1 行につき 1 つのパス) を名前を付けます検出のみを表示します。 パイプを使用して送信する前に**dir**を別のコマンドの出力は、TEMP 環境変数に設定項目のファイル。
- **Dir**コマンドで他のパラメーターは、回復コンソールから利用できます。

## <a name="examples"></a>例

後、別のでは、ワイド形式と画面ごとに一時停止、アルファベット順でディレクトリをすべて 1 つを表示するには、ルート ディレクトリが現在のディレクトリであることを確認し、入力します。

```
dir /s/w/o/p
```

**Dir**ルート ディレクトリ、サブディレクトリ、および拡張機能を含め、ルート ディレクトリ内のファイルを一覧表示されます。 次に、 **dir**サブディレクトリの名前と、ツリーの各サブディレクトリ内のファイル名を一覧表示されます。

前の例を変更するように**dir**ファイル名と、拡張機能が表示されますが、ディレクトリ名、型が省略されます。

```
dir /s/w/o/p/a:-d
```

ディレクトリの一覧を印刷するには、次のように入力します。

```
dir > prn
```

指定すると**prn**ディレクトリの一覧は、lpt1 に接続されているプリンターに送信されます。 置き換える必要がある場合は、プリンターは、別のポートに接続されているが、 **prn**適切なポートの名前に置き換えます。

出力をリダイレクトすることもできます、 **dir**を置き換えることで、ファイルにコマンド**prn**ファイル名を使用します。 パスを入力することもできます。 Direct にはたとえば、 **dir**型であるレコード ディレクトリにファイル dir.doc に出力します。

```
dir > \records\dir.doc
```

Dir.doc が存在しない場合**dir**レコード ディレクトリが存在しない場合を除き、それを作成します。 その場合は、次のメッセージが表示されます。

`File creation error`

ドライブ C 上のすべてのディレクトリ内には、拡張子 .txt のすべてのファイル名の一覧を表示するに次のように入力します。

```
dir c:\*.txt /w/o/s/p
```

**Dir**が表示されたら、ワイド形式で、ディレクトリごとにアルファベット順の一覧に対応するファイルの名前を付けます、毎回続行する任意のキーを押すまで、画面が入力を一時停止します。

#### <a name="additional-references"></a>その他の参照情報

[コマンド ライン構文の記号](command-line-syntax-key.md)