---
title: コア ネットワーク ガイド
description: このガイドでは、計画、および Windows Server 2016 で、新しいフォレスト内の新しい Active Directory ドメインとネットワークが完全に機能するために必要なコア コンポーネントを展開する方法の説明
manager: brianlic
ms.prod: windows-server-threshold
ms.technology: networking
ms.topic: article
ms.assetid: b3cd60f7-d380-4712-9a78-0a8f551e1121
ms.author: pashort
author: shortpatti
ms.openlocfilehash: 90390a7b975bc358fb96715d23fe542bc261220e
ms.sourcegitcommit: 19d9da87d87c9eefbca7a3443d2b1df486b0b010
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 03/28/2018
---
# <a name="core-network-guide"></a>コア ネットワーク ガイド

>適用対象: Windows Server (半期チャネル)、Windows Server 2016

このガイドは、計画し、新しいフォレスト内の新しい Active Directory ドメインとネットワークが完全に機能するために必要なコア コンポーネントを展開する方法について説明します。

> [!NOTE]
> このガイドでは、TechNet ギャラリーから Microsoft Word 形式でダウンロードできます。 詳細については、次を参照してください。[コア ネットワーク ガイドの Windows Server 2016](https://gallery.technet.microsoft.com/Core-Network-Guide-for-9da2e683)します。

このガイドには、次のセクションが含まれています。

- [このガイドについて](#BKMK_about)

- [コア ネットワークの概要](#BKMK_overview)

- [コア ネットワークの計画](#BKMK_planning)

- [コア ネットワークの展開](#BKMK_deployment)

- [その他のテクニカル リソース](#BKMK_resources)

- [付録 A E から](#BKMK_appendix)

## <a name="BKMK_about"></a>このガイドについて
このガイドは、ネットワーク管理者およびシステム管理者のワークグループから構成されるネットワークを置き換えるドメイン ベースのネットワークを作成するとき、または新しいネットワークをインストールしているユーザーに適しています。 このガイドで提供される展開シナリオでは、サービスや機能を今後、ネットワークに追加する必要が予想される場合に特に便利です。

設計を確認し、展開のガイドについては、各サービスと必要な構成に、このガイドを提供するかどうかを決定するに役立つ、この展開シナリオで使用されるテクノロジのことをお勧めします。

コア ネットワークはネットワーク ハードウェア、デバイスのコレクションとソフトウェアが組織の情報技術 (IT) の基本的なサービスを提供する必要があります。

Windows Server コア ネットワークは、次を含む多くのメリットを提供します。

-   コンピューターとその他の伝送制御プロトコル/インターネット プロトコル (TCP/IP) 互換デバイス間のネットワーク接続のコア プロトコル。 TCP/IP は、コンピューターに接続するネットワークを構築するための標準プロトコル スイートです。 TCP/IP はネットワーク プロトコル ソフトウェア Microsoft Windows オペレーティング システムで提供される実装および、TCP/IP プロトコル スイートをサポートすることです。

-   動的ホスト構成プロトコル (DHCP) 自動 IP アドレス割り当てコンピューターと DHCP クライアントとして構成されているその他のデバイスにします。 ネットワーク上のすべてのコンピューター上の IP アドレスを手動で構成は、時間がかかり、DHCP サーバーを使用して、IP アドレス構成をコンピューターとその他のデバイスを動的に提供するよりも柔軟性が低くなります。

-   ドメイン ネーム システム (DNS) の名前解決サービス。 DNS は、コンピューターまたはデバイスの完全修飾ドメイン名を使用して、ネットワーク上のコンピューターとデバイスの IP アドレスを検索するユーザー、コンピューター、アプリケーション、およびサービスです。

-   これは、同じクラスと属性定義 (スキーマ)、サイトとレプリケーションの情報 (構成)、およびフォレスト全体の検索機能 (グローバル カタログ) を共有する 1 つまたは複数の Active Directory ドメイン フォレスト。

-   最初のドメインがあるフォレスト ルート ドメインは、新しいフォレストに作成されます。 フォレスト全体の管理グループには、Enterprise Admins および Schema Admins グループは、フォレスト ルート ドメイン内にあります。 さらに、フォレスト ルート ドメインでは、他のドメインとは、コンピューター、ユーザー、および Active Directory ドメイン サービス (AD DS) で、管理者によって定義されているグループ オブジェクトのコレクション。 これらのオブジェクトは、共通のディレクトリ データベースおよびセキュリティ ポリシーを共有します。 ドメイン、組織の拡大を追加した場合は、他のドメインとセキュリティ関係を共有することもできます。 ディレクトリ サービスもディレクトリ データを格納でき、承認されたコンピューター、アプリケーション、およびユーザー データにアクセスできます。

-   ユーザーとコンピューターのアカウント データベース。 ディレクトリ サービスは、一元管理のユーザー アカウントのデータベース ユーザーとコンピューター、アプリケーション、データベース、共有ファイルとフォルダー、およびプリンターなどのリソースをネットワークおよびネットワークへのアクセスに接続する権限を持つユーザーとコンピューター アカウントを作成することができますを提供します。

コア ネットワークでは、組織の成長し、IT 要件の変化に応じて、ネットワークを拡張することもできます。 たとえば、コア ネットワークとドメイン、IP サブネット、リモート アクセス サービス、ワイヤレス サービス、およびその他の機能および Windows Server 2016 によって提供されるサーバーの役割を追加することができます。

### <a name="network-hardware-requirements"></a>ネットワーク ハードウェア要件
コア ネットワークを正常に展開するには、ネットワーク ハードウェアを展開する必要があります。

-   イーサネット、ファスト イーサネット、またはギガバイト イーサネット ケーブル

-   ハブ、レイヤー 2 または 3 スイッチ、ルーター、またはその他のコンピューターとデバイスの間のネットワーク トラフィックの中継機能を実行するデバイスです。

-   それぞれしたクライアントとサーバーのオペレーティング システムの最小ハードウェア要件を満たすコンピューター。

## <a name="what-this-guide-does-not-provide"></a>このガイドで説明としていない新機能
このガイドは、以下の展開の手順を説明していません。

-   ケーブル、ルーター、スイッチ、およびハブなどのネットワーク ハードウェア

-   プリンターやファイル サーバーなどの他のネットワーク リソース

-   インターネット接続

-   リモート アクセス

-   無線アクセス

-   クライアント コンピューターの展開

> [!NOTE]
> DHCP サーバーから IP アドレスのリースを受信する既定では、Windows クライアント オペレーティング システムを実行しているコンピューターを構成します。 そのため、ない追加 DHCP またはインターネット プロトコル バージョン 4 (IPv4) のクライアント コンピューターの構成が必要です。

## <a name="technology-overviews"></a>テクノロジの概要
次のセクションでは、コア ネットワークを作成する展開される必須テクノロジの概要を提供します。

### <a name="active-directory-domain-services"></a>Active Directory ドメイン サービス
ディレクトリは、ユーザーとコンピューターなど、ネットワーク上のオブジェクトに関する情報を格納する階層構造です。 AD DS などのディレクトリ サービスでは、ディレクトリ データを格納すると、ネットワーク ユーザーと管理者がこのデータを使用できるメソッドを提供します。 たとえば、AD DS 名、電子メール アドレス、パスワード、および電話番号を含む、ユーザー アカウントに関する情報を格納でき、この情報にアクセスする権限のある他のユーザーを同じネットワーク上。

### <a name="dns"></a>DNS
DNS は、インターネットや他の組織ネットワークなど、TCP/IP ネットワークの名前解決プロトコルです。 により、クライアント コンピューターの情報をホストしている DNS サーバーと認識サービスを簡単に解決するのには、英数字の DNS 名をコンピューターが相互に通信するために使用する IP アドレスです。

### <a name="dhcp"></a>DHCP
DHCP は、ホストの IP 構成の管理を合理化するための IP 標準です。 DHCP 標準は、ネットワーク上の DHCP 対応クライアントの IP アドレスの動的な割り当てとその他の関連する構成詳細を管理する手段として DHCP サーバーの使用を示します。

DHCP を使用すると、DHCP サーバーを使用して、コンピューターや、ローカル ネットワーク上のプリンターなど、他のデバイスに IP アドレスを動的に割り当てることができます。 TCP/IP ネットワーク上のすべてのコンピューターは、IP アドレスおよび関連付けられたサブネット マスクは、ホスト コンピューターと、コンピューターが接続されているサブネットの両方を識別するため、一意の IP アドレスにすることが必要です。 DHCP を使用すると、DHCP クライアントとして構成されているすべてのコンピューターが、ネットワークの場所およびサブネットに適切な IP アドレスを受信することを確認できますデフォルト ゲートウェイや DNS サーバーなどの DHCP オプションを使用して提供して、できる自動的に DHCP クライアント、ネットワーク上で正しく機能する必要があるという情報でします。

IP ベース ネットワークでは、DHCP によって複雑さとコンピューターの再構成にかかわる管理作業の量が削減されます。

### <a name="tcpip"></a>TCP/IP
Windows Server 2016 での TCP/IP は次のよう

-   ネットワークの業界標準のネットワーク プロトコルに基づくソフトウェア。

-   ローカル エリア ネットワーク (LAN) とワイド エリア ネットワーク (WAN) 環境の両方に Windows ベースのコンピューターの接続をサポートしているルーティング可能なエンタープライズ ネットワーク プロトコルです。

-   コア テクノロジおよび情報を共有するために異種システムで Windows ベースのコンピューターを接続するためのユーティリティです。

-   World Wide Web およびファイル転送プロトコル (FTP) サーバーなどのグローバル インターネット サービスへのアクセスを取得するための基盤です。

-   堅牢かつスケーラブルなクロスプラット フォーム、クライアント/サーバー フレームワーク。

TCP/IP には、Windows ベースのコンピュータに接続し、他の Microsoft を含む、Microsoft 以外のシステムと情報を共有できるようにする基本的な TCP/IP ユーティリティが用意されています。

-    Windows Server 2016

-   Windows 10

-    Windows Server 2012 R2

-   Windows 8.1

-    Windows Server 2012

-   Windows 8

-    Windows Server 2008 R2

-    Windows 7

-    Windows Server 2008

-   Windows Vista

-   インターネット ホスト

-   Apple Macintosh システム

-   IBM メインフレーム

-   UNIX および Linux システム

-   オープン VMS システム

-   ネットワーク対応プリンター

-   タブレット、携帯電話をワイヤード (有線) イーサネットと無線の 802.11 テクノロジを有効になっています。

## <a name="BKMK_overview"></a>コア ネットワークの概要
次の図では、Windows Server コア ネットワーク トポロジを示します。

![Windows Server コア ネットワーク トポロジ](../media/Core-Network-Guide/cng16_overview.jpg)

> [!NOTE]
> このガイドでは、802.1 X ワイヤード (有線) およびワイヤレス展開コア ネットワーク必携ガイドを使用するために実装できるなどのセキュリティで保護されたネットワーク アクセス ソリューションの基盤となるネットワーク トポロジを省略可能なネットワーク ポリシー サーバー (NPS) サーバーと Web サーバー (IIS) サーバーを追加する手順についても含まれます。 詳細については、次を参照してください。[のネットワーク アクセスの認証および Web サービスのオプションの機能を展開する](#BKMK_optionalfeatures)します。

### <a name="core-network-components"></a>コア ネットワークのコンポーネント
コア ネットワークのコンポーネントを次に示します。

##### <a name="router"></a>ルーター
この展開ガイドでは、DHCP 転送を有効にしたルーターで区切られた 2 つのサブネットを含む、コア ネットワークを展開するための指示を提供します。 ただし、レイヤー 2 スイッチ、レイヤー 3 スイッチ、または要件やリソースに応じて、ハブを展開することができます。 スイッチを展開する場合は、スイッチが DHCP 転送に対応する必要がありますか、各サブネット上の DHCP サーバーを配置する必要があります。 ハブを展開する場合、単一のサブネットを展開して、DHCP サーバーで DHCP 転送または 2 つ目のスコープは必要はありません。

##### <a name="static-tcpip-configurations"></a>静的 TCP/IP 構成
この展開内のサーバーは、静的な IPv4 アドレスで構成されます。 DHCP サーバーから IP アドレスのリースを受信する既定では、クライアント コンピューターを構成します。

##### <a name="active-directory-domain-services-global-catalog-and-dns-server-dc1"></a>Active Directory ドメイン サービス グローバル カタログおよび DNS サーバー DC1
両方がこのサーバーは、ディレクトリを提供する、DC1 という名前にインストールされ、名前解決サービスすべてのコンピューターと、ネットワーク上のデバイスを Active Directory ドメイン サービス (AD DS) およびドメイン ネーム システム (DNS) です。

##### <a name="dhcp-server-dhcp1"></a>DHCP サーバー DHCP1
DHCP サーバーで、DHCP1 という名前は、ローカル サブネット上のコンピューターにインターネット プロトコル (IP) アドレスのリースを提供するスコープで構成されます。 DHCP サーバーは、ルーターで DHCP 転送が構成されている場合は、他のサブネット上のコンピューターに IP アドレスのリースを提供するその他のスコープを持つも構成できます。

##### <a name="client-computers"></a>クライアント コンピューター
Windows クライアント オペレーティング システムを実行しているコンピューターは、既定では、DHCP サーバーから IP アドレスと DHCP オプションを自動的に取得するは、DHCP クライアントとして構成されます。

## <a name="BKMK_planning"></a>コア ネットワークの計画
コア ネットワークを展開する前に、次の項目を計画する必要があります。

-   [サブネットを計画します。](#bkmk_NetFndtn_Pln_Subnt)

-   [すべてのサーバーの基本構成の計画](#bkmk_NetFndtn_Pln_AllSrvrs)

-   [DC1 の展開の計画](#bkmk_NetFndtn_Pln_AD-DNS-01)

-   [ドメイン アクセスを計画します。](#bkmk_NetFndtn_Pln_DomAccess)

-   [DHCP1 の展開の計画](#bkmk_NetFndtn_Pln_DHCP-01)

以下のセクションでは、これらの各項目の詳細を提供します。

> [!NOTE]
> 展開の計画については、次を参照しても[付録 E - コア ネットワークの計画準備シート](#BKMK_E)します。

### <a name="bkmk_NetFndtn_Pln_Subnt"></a>サブネットを計画します。
伝送制御プロトコル/インターネット プロトコル (TCP/IP) ネットワークでは、ルーターは、ハードウェアとソフトウェアのサブネットと呼ばれる別の物理ネットワーク セグメントで使用される相互接続に使用されます。 ルーターは、各サブネット間で IP パケットを転送するようにも使用されます。 ルーターとサブネットのこのガイドの手順に進む前に、必要な数など、ネットワークの物理的なレイアウトを決定します。

さらに、静的 IP アドレスを使用してネットワーク上のサーバーを構成するには、コア ネットワーク サーバーが配置されているサブネットに使用する IP アドレスの範囲を決定する必要があります。 このガイドでは、プライベート IP アドレスの範囲を 10.0.0.1 ~ 10.0.0.254 と 10.0.1.1 - 10.0.1.254 は例については、として使用されますが、プライベート IP アドレスの範囲を必要に応じてを使用することができます。

> [!IMPORTANT]
> 各サブネットに使用する IP アドレスの範囲を選択した後は、ルーターがインストールされているサブネットに使用されるものと同じ IP アドレス範囲から IP アドレスでルーターを構成することを確認します。 たとえば、ルーターが既定で 192.168.1.1 の IP アドレスで構成されている IP アドレス範囲 10.0.0.0/24 のサブネットにルーターをインストールする場合は、10.0.0.0/24 IP アドレスの範囲から IP アドレスを使用するようにルーターを再構成する必要があります。

プライベートの IP アドレスの範囲で設定された Request for Comments (RFC) 1918 認識、次のとおりです。

-   10.0.0.0 - 10.255.255.255

-   172.16.0.0 - 172.31.255.255

-   192.168.0.0 - 192.168.255.255

RFC 1918 で指定されているプライベート IP アドレスの範囲を使用する場合は、これらのアドレスを通過する要求は、インターネット サービス プロバイダー (ISP) ルーターによって自動的に破棄されますので、プライベート IP アドレスを使用してインターネットに直接接続できません。 コア ネットワークにインターネット接続を追加するには、その後、結ぶ必要がありますパブリック IP アドレスを取得する契約を ISP とします。

> [!IMPORTANT]
> プライベート IP アドレスを使用する場合は、インターネット上でルーティングできるパブリック IP アドレスをローカル ネットワーク上のプライベート IP アドレスの範囲を変換する何らかの種類のプロキシ サーバーまたはネットワーク アドレス変換 (NAT) サーバーを使用する必要があります。 ほとんどのルーターは、非常に簡単にする必要がありますが、NAT 対応ルーターを選択すると、NAT サービスを提供します。

詳細については、次を参照してください。 [DHCP1 の展開を計画](#bkmk_NetFndtn_Pln_DHCP-01)します。

### <a name="bkmk_NetFndtn_Pln_AllSrvrs"></a>すべてのサーバーの基本構成の計画
コア ネットワーク内の各サーバーに対してする必要がありますコンピューターの名前を割り当てるし、静的な IPv4 アドレスとコンピューターの場合は、その他の TCP/IP プロパティを構成します。

#### <a name="planning-naming-conventions-for-computers-and-devices"></a>コンピューターとデバイスの名前付け規則を計画します。
ネットワークにわたって一貫性を保つのためにはサーバー、プリンター、およびその他のデバイスに一貫した名前を使用することをお勧めします。 コンピューター名を使用してユーザーを支援することし、管理者の用途と、サーバー、プリンター、またはその他のデバイスの場所を識別しやすくします。 たとえば、3 台の DNS サーバー サンフランシスコ、ロサンゼルス、され、シカゴに 1 台の場合は、命名規則を使用可能性があります*サーバー関数*-*場所*-*数*:

-   DNS-DEN-01 します。 この名前はコロラド州デンバーに DNS サーバーを表します。 かどうかは追加の DNS サーバーがデンバーに追加された、DNS-DEN-02 と DNS-DEN-03 に示すように、名前の数値をインクリメントことができます。

-   DNS-SPAS-01 します。 この名前はカリフォルニア州南パサデナに DNS サーバーを表します。

-   DNS-ORL-01 します。 この名前はフロリダ州オーランドに DNS サーバーを表します。

このガイドでは、サーバーの名前付け規則非常に単純では、プライマリ サーバーの機能と番号で構成されます。 たとえば、DC1 という名前のドメイン コント ローラーは、し、DHCP1 という名前の DHCP サーバーがします。

このガイドを使用してコア ネットワークをインストールする前に、命名規則を選択することをお勧めします。

#### <a name="planning-static-ip-addresses"></a>静的 IP アドレスを計画します。
静的 IP アドレスを持つ各コンピューターを構成する前に、サブネットと IP アドレスの範囲を計画する必要があります。 さらに、DNS サーバーの IP アドレスを決定する必要があります。 追加のサブネットなどの他のネットワークまたはインターネットへのアクセスを提供するルーターをインストールする場合は、静的 IP アドレス構成の既定のゲートウェイとも呼ばれます、ルーターの IP アドレスがわかっている必要があります。

次の表は、静的 IP アドレス構成の値の例を示します。

|構成項目|値の例|
|-----------------------|------------------|
|IP アドレス|10.0.0.2|
|サブネット マスク|255.255.255.0|
|デフォルト ゲートウェイ (ルーターの IP アドレス)|10.0.0.1|
|優先 DNS サーバー|10.0.0.2|

> [!NOTE]
> 1 つ以上の DNS サーバーを展開する場合は、代替 DNS サーバーの IP アドレスも計画できます。

### <a name="bkmk_NetFndtn_Pln_AD-DNS-01"></a>DC1 の展開の計画
Active Directory ドメイン サービス (AD DS) をインストールする前に重要な計画手順を次に示しますと DNS を DC1 にします。

#### <a name="planning-the-name-of-the-forest-root-domain"></a>フォレスト ルート ドメインの名前を計画します。
AD DS 設計プロセスの最初の手順では、組織で必要なフォレスト数を決定します。 フォレストでは、最上位の AD DS コンテナーし、共通のスキーマとグローバル カタログを共有する 1 つまたは複数のドメインで構成されます。 組織では、複数のフォレストを使用できますが、単一のフォレスト設計が推奨されるモデルとを管理する最も簡単なほとんどの組織で。

組織内の最初のドメイン コント ローラーを作成するときに、最初のドメイン (フォレスト ルート ドメインとも呼ばれます) と最初のフォレストを作成しています。 ただし、このガイドを使用してこの操作を実行する前に、組織に最適なドメイン名判断する必要があります。 ほとんどの場合、組織名は、ドメイン名として使用して、多くの場合、このドメイン名が登録されています。 外部の展開を計画している場合は、インターネット ベースの情報と、顧客またはパートナー サービスを提供し、されていない場合、使用するドメイン名を選択し、組織が所有できるようにドメイン名を登録し、Web サーバー。

#### <a name="planning-the-forest-functional-level"></a>フォレストの機能レベルの計画
AD DS をインストールするには、中を使用するフォレストの機能レベルを選択する必要があります。 Windows Server 2003 Active Directory で導入された、ドメインおよびフォレストの機能は、ドメイン全体またはフォレスト全体、ネットワーク環境内で Active Directory の機能を有効にする方法を提供します。 さまざまなレベルのドメインの機能およびフォレストの機能は、環境によって、使用できます。

フォレストの機能は、フォレスト内のすべてのドメイン間での機能を使用できます。 次のフォレストの機能レベルを使用できます。

-    Windows Server 2008 します。 このフォレストの機能レベルには、Windows Server 2008 および Windows Server オペレーティング システムの以降のバージョンを実行しているドメイン コント ローラーのみがサポートしています。

-    Windows Server 2008 R2。 このフォレストの機能レベルは、Windows Server 2008 R2 のドメイン コント ローラーおよび以降のバージョンの Windows Server オペレーティング システムを実行しているドメイン コント ローラーをサポートします。

-    Windows Server 2012 です。 このフォレストの機能レベルには、Windows Server 2012 ドメイン コント ローラーおよび以降のバージョンの Windows Server オペレーティング システムを実行しているドメイン コント ローラーがサポートしています。

-    Windows Server 2012 R2 します。 このフォレストの機能レベルは、Windows Server 2012 R2 のドメイン コント ローラーおよび以降のバージョンの Windows Server オペレーティング システムを実行しているドメイン コント ローラーをサポートします。

-    Windows Server 2016 します。 このフォレストの機能レベルには、Windows Server 2016 ドメイン コント ローラーと以降のバージョンの Windows Server オペレーティング システムを実行しているドメイン コント ローラーのみがサポートしています。

新しいフォレストに新しいドメインを展開して、すべてのドメイン コント ローラーが実行されている Windows Server 2016 場合、は、AD DS のインストール中に、Windows Server 2016 のフォレスト機能レベルと AD DS を構成することをお勧めします。

> [!IMPORTANT]
> フォレストの機能レベルが発生した後、以前のオペレーティング システムを実行しているドメイン コント ローラーがフォレストに導入できなきます。 たとえば、Windows Server 2016 にフォレストの機能レベルを上げる場合 Windows Server 2012 R2 または Windows Server 2008 を実行するドメイン コント ローラーをフォレストに追加できません。

AD DS の構成項目の例を次の表に示します。

|構成項目の場合:|値の例:|
|------------------------|-------------------|
|完全 DNS 名|例:<br /><br />-corp.contoso.com<br />-example.com|
|フォレストの機能レベル|Windows Server 2008 <br />Windows Server 2008 R2 <br />Windows Server 2012 <br />Windows Server 2012 R2 <br />Windows Server 2016|
|Active Directory ドメイン サービス データベース フォルダーの場所|E:\Configuration\\<br /><br />または、既定の場所をそのまま使用します。|
|Active Directory ドメイン サービスのログ ファイル フォルダーの場所|E:\Configuration\\<br /><br />または、既定の場所をそのまま使用します。|
|Active Directory Domain Services SYSVOL フォルダーの場所|E:\Configuration\\<br /><br />既定の場所を受け入れるか|
|ディレクトリ復元モード Administrator パスワード|**J\ * p2leO4$ F**|
|応答ファイルの名前 (省略可能)|**AD DS_AnswerFile**|

#### <a name="planning-dns-zones"></a>DNS ゾーンの計画
プライマリ、Active Directory 統合 DNS サーバー、前方参照ゾーンが既定では、DNS サーバーの役割のインストール中に作成されます。 前方参照ゾーンは、コンピューターおよびデバイスを別のコンピューターまたはデバイスの IP アドレスが DNS 名に基づいてのクエリを実行できます。 前方参照ゾーン] だけでなく、DNS 逆引き参照ゾーンを作成することをお勧めします。 Dns 逆引き参照のクエリ、コンピューターまたはデバイスが別のコンピューターやその IP アドレスを使用してデバイスの名前を検出できます。 逆引き参照ゾーンを展開すると、通常 DNS のパフォーマンスが向上し、DNS クエリの成功が大幅に増加します。

逆引き参照ゾーンを作成するときに、DNS に標準で定義されているし、実用的で信頼性の高い逆引きクエリを実行する方法を提供するインターネット DNS 名前空間で予約されて、in-addr.arpa ドメインが DNS で構成されます。 逆引き名前空間を作成するに in-addr.arpa ドメイン内部にサブドメインが形成される、IP アドレスのドット区切り 10 進表記の数字の順序を逆を使用します。

In-addr.arpa ドメインの適用対象はインターネット プロトコル バージョン 4 (IPv4) に基づくすべての TCP/IP ネットワーク アドレス指定します。 新しいゾーン ウィザードでは、自動的に新しい逆引き参照ゾーンを作成するときに、このドメインが使用するいると仮定します。

新しいゾーン ウィザードを実行しているときに、次の選択が推奨されます。

|構成項目|値の例|
|-----------------------|------------------|
|ゾーンの種類|**プライマリ ゾーン**、および**ゾーンを格納する Active Directory で**が選択されています。|
|Active Directory ゾーン レプリケーション スコープ|**このドメイン内のすべての DNS サーバー**|
|最初の逆引き参照ゾーン名ウィザードのページ|**IPv4 逆引き参照ゾーン**|
|2 番目の逆引き参照ゾーン名ウィザードのページ|ネットワーク ID = 10.0.0。|
|動的更新|**保護された動的更新のみを許可します。**|

### <a name="bkmk_NetFndtn_Pln_DomAccess"></a>ドメイン アクセスを計画します。
ドメインにログオンするには、コンピューターがドメイン メンバー コンピューターをする必要があり、ログオンを試みる前に AD DS でユーザー アカウントを作成する必要があります。

> [!NOTE]
> Windows を実行している個々 のコンピューターがあるローカル ユーザーとグループのユーザー アカウントのセキュリティ アカウント マネージャー (SAM) のユーザー アカウント データベースというデータベース。 SAM データベースにローカル コンピューターでユーザー アカウントを作成する場合は、ローカル コンピューターにログオンすることができますが、ドメインにログオンすることはできません。 ドメイン ユーザー アカウントは、ローカル ユーザーとグループ、ローカル コンピューター上ではなく、ドメイン コント ローラーで、Active Directory ユーザーとコンピューター Microsoft 管理コンソール (MMC) で作成されます。

ドメインのログオン資格情報で最初の成功したログオンした後、コンピューターがドメインから削除またはログオン設定が手動で変更された場合を除き、ログオン設定が保持されます。

ログオンする前に、ドメインに。

-   Active Directory ユーザーとコンピュータにユーザー アカウントを作成します。 各ユーザーには、Active Directory ユーザーとコンピュータに Active Directory Domain Services ユーザー アカウントを持って必要があります。 詳細については、次を参照してください。 [Active Directory ユーザーとコンピュータにユーザー アカウントを作成](#BKMK_createUA)します。

-   正しい IP アドレス構成を確認します。 コンピューターをドメインに参加させるには、IP アドレスがコンピューターに必要です。 このガイドでサーバーに静的 IP アドレスを構成し、クライアント コンピューターは、DHCP サーバーから IP アドレスのリースを受信します。 このため、クライアントをドメインに参加する前に、DHCP サーバーを展開する必要があります。 詳細については、次を参照してください。 [DHCP1 を展開する](#BKMK_deployDHCP01)します。

-   コンピューターをドメインに参加します。 ネットワーク リソースにアクセスを提供または任意のコンピューターをドメインに参加する必要があります。 詳細については、次を参照してください。[への参加サーバー コンピューターをドメインとログオン](#BKMK_joinlogserver)と[ドメインしてログオンするクライアント コンピューターを参加させる](#BKMK_joinlogclients)します。

### <a name="bkmk_NetFndtn_Pln_DHCP-01"></a>DHCP1 の展開の計画
DHCP1 で DHCP サーバーの役割をインストールする前に重要な計画手順を次に示します。

#### <a name="planning-dhcp-servers-and-dhcp-forwarding"></a>DHCP サーバーおよび DHCP 転送を計画します。
DHCP メッセージはブロードキャスト メッセージであるために、ルーターでサブネット間では転送されません。 複数のサブネットがある各サブネットの DHCP サービスを提供する場合、次のいずれかを行う必要があります。

-   各サブネット上の DHCP サーバーをインストールします。

-   DHCP ブロードキャスト メッセージをサブネット間で転送し、サブネットごとに 1 つのスコープ、DHCP サーバーで複数のスコープを構成するようにルーターを構成します。

ほとんどの場合、DHCP ブロードキャスト メッセージを転送するようにルーターを構成するは、ネットワークの各物理セグメント上の DHCP サーバーを展開するよりコスト効率です。

#### <a name="planning-ip-address-ranges"></a>IP アドレス範囲を計画します。
各サブネットには、独自の一意の IP アドレス範囲が必要です。 これらの範囲は、スコープと DHCP サーバーで表されます。

スコープは、DHCP サービスを使用するサブネット上のコンピューターの IP アドレスの管理グループです。 管理者は、まず各物理サブネットのスコープを作成し、スコープを使用して、クライアントで使用されるパラメータを定義します。

スコープには、次のプロパティ。

-   DHCP サービス リースの提供に使用されるアドレス、または除外する範囲の IP アドレス。

-   サブネット マスク: 特定の IP アドレスのサブネット プレフィックスを決定します。

-   作成時に割り当てられているスコープ名。

-   リース期間の値を動的に割り当てられた IP アドレスを受信する DHCP クライアントに割り当てられています。

-   DNS サーバーの IP アドレス、ルーターまたはデフォルト ゲートウェイの IP アドレスなどの DHCP クライアントに割り当て用に構成された DHCP スコープ オプション。

-   予約は必要に応じて、DHCP クライアントが常に同じ IP アドレスを受信することを確認するために使用します。

サーバーを展開する前に、サブネット、および各サブネットに使用する IP アドレスの範囲の一覧を表示します。

#### <a name="planning-subnet-masks"></a>サブネット マスクを計画します。
ネットワーク Id および IP アドレス内のホスト Id は、サブネット マスクを使用して識別されます。 各サブネット マスクは 32 ビットの数値のすべての連続するビット グループを使用するネットワークを識別するには、(1) ID とすべてゼロ (0)、IP アドレスのホスト ID 部分を識別します。

たとえば、IP アドレス 131.107.16.200 で通常使用するサブネット マスクは、次の 32 ビット 2 進数を示します。

```
11111111 11111111 00000000 00000000
```

このサブネット マスクの数値は、ネットワーク ID とホスト ID セクションでは、この IP アドレスのどちらも 16 ビット長であることを示す 16 0 ビット、続けて 16 の 1 のビットです。 通常、このサブネット マスクは 255.255.0.0 としてはドット区切り 10 進表記で表示されます。

次の表は、インターネット アドレス クラスのサブネット マスクを表示します。

|アドレス クラス|サブネット マスクのビット|サブネット マスク|
|-----------------|------------------------|---------------|
|クラス A|11111111 00000000 00000000 00000000|255.0.0.0|
|クラス B|11111111 11111111 00000000 00000000|255.255.0.0|
|クラス C|11111111 11111111 11111111 00000000|255.255.255.0|

Dhcp スコープを作成する、スコープの IP アドレスの範囲を入力して、DHCP では、これらの既定のサブネット マスクの値が提供されます。 通常、既定のサブネット マスクの値は、特別な要件なくほとんどのネットワークと各 IP ネットワーク セグメントが 1 つの物理ネットワークに対応する場所で受け入れられます。

場合によっては、カスタマイズしたサブネット マスクを使用して IP サブネットを実装することができます。 IP サブネット、サブネットで、元のクラスベースのネットワーク ID の区分を指定する IP アドレスの既定のホスト ID 部分に分割することができます。

サブネット マスクの長さをカスタマイズして、実際のホスト ID に使用されるビット数を減らすことができます。

アドレス指定とルーティングの問題を防ぐためには、ネットワーク セグメント上のすべての TCP/IP コンピューターが同じサブネット マスクを使用して、コンピューターまたはデバイスごとに一意の IP アドレスがあることを確認してください。

#### <a name="planning-exclusion-ranges"></a>除外範囲を計画します。
DHCP サーバーのスコープを作成するときに、すべての DHCP サーバーのコンピューターやその他のデバイスなどの DHCP クライアントにリースが許可されている IP アドレスが含まれる IP アドレスの範囲を指定します。 移動し、一部のサーバーを手動で構成し、DHCP サーバーを使用している同じ IP アドレスの範囲から IP アドレスを静的なその他のデバイス、誤って IP アドレスの競合を作成することができます、して、DHCP サーバーがある両方同じ IP アドレスを割り当てるさまざまなデバイスにします。

この問題を解決するには、DHCP スコープの除外範囲を作成できます。 除外範囲は、DHCP サーバーは使用できません、スコープの IP アドレスの範囲内の連続した範囲の IP アドレスです。 除外範囲を作成する場合、DHCP サーバーにすると、競合する IP アドレスを作成することがなく、これらのアドレスを手動で割り当てることが、その範囲内のアドレスは割り当てられません。

各スコープに対して除外範囲を作成することで、DHCP サーバーによって配布から IP アドレスを除外できます。 静的 IP アドレスが構成されているすべてのデバイスの除外を使用する必要があります。 除外されたアドレスでは、その他のサーバー、非 DHCP クライアント、ディスクレス ワークステーション、またはルーティングとリモート アクセスおよび PPP クライアントを手動で割り当てられているすべての IP アドレスを含める必要があります。

ネットワークの将来の成長に対応する余分なアドレスを持つ、除外範囲を構成することをお勧めします。 次の表は、10.0.0.1 ~ の IP アドレスの範囲を使用するスコープに対する除外範囲の例を示します 10.0.0.254 と 255.255.255.0 のサブネット マスク。

|構成項目|値の例|
|-----------------------|------------------|
|除外範囲の IP アドレスの開始|10.0.0.1|
|除外範囲終了 IP アドレス|10.0.0.25|

#### <a name="planning-tcpip-static-configuration"></a>TCP/IP の静的構成を計画します。
静的 IP アドレスを持つ、ルーター、DHCP サーバー、および DNS サーバーなどの特定のデバイスを構成する必要があります。 さらに、追加のデバイス、常に確認するには、プリンターなど、同じ IP アドレスを持っているがあります。 各サブネットの静的を構成するデバイスの一覧し、DHCP サーバーが静的に構成されたデバイスの IP アドレスをリースしていないことを確認する DHCP サーバーで使用する場合、除外範囲を計画します。 除外範囲は、DHCP サービスの提供から除外されて、スコープ内の IP アドレスの限られたシーケンスです。 除外範囲は、ネットワーク上の DHCP クライアントにこれらの範囲内のアドレスがサーバーによって提供しないことを保証します。

たとえば、サブネットの IP アドレスの範囲が 192.168.0.1 ~ 192.168.0.254 し、静的 IP アドレスを構成する 10 台のデバイスを使用している場合、192. 168.0 除外範囲を作成できます。*x* 10 個以上の IP アドレスを含むスコープ: 192.168.0.1 ~ 192.168.0.15 までです。

この例では 10 個の除外された IP アドレスを使用して静的 IP アドレスを持つサーバーおよびその他のデバイスを構成して追加の 5 つの IP アドレスは、将来追加する新しいデバイスの静的構成の使用可能な残されます。 この除外範囲で DHCP サーバーには 192.168.0.16 192.168.0.254 経由のアドレス プールが残ります。

AD DS および DNS の構成項目の追加の例を次の表に示します。

|構成項目|値の例|
|-----------------------|------------------|
|ネットワーク接続バインディング|イーサネット|
|DNS サーバーの設定|DC1.corp.contoso.com|
|優先 DNS サーバーの IP アドレス|10.0.0.2|
|スコープ] ダイアログ ボックスの値を追加します。<br /><br />1。 スコープ名<br />2. 開始 IP アドレス<br />3. 終了 IP アドレス<br />4. サブネット マスク<br />5. デフォルト ゲートウェイ (省略可能)<br />6. リース期間|1。 プライマリ サブネット<br />2.  10.0.0.1<br />3.  10.0.0.254<br />4.  255.255.255.0<br />5.  10.0.0.1<br />6. 8 日|
|IPv6 DHCP サーバーの操作モード|有効になっていません|

## <a name="BKMK_deployment"></a>コア ネットワークの展開
コア ネットワークを展開するには、基本的な手順は次のとおりです。

1.  [すべてのサーバーを構成します。](#BKMK_configuringAll)

2.  [DC1 を展開します。](#BKMK_deployADDNS01)

3.  [サーバーのコンピューターをドメインに追加してログオン](#BKMK_joinlogserver)

4.  [DHCP1 を展開します。](#BKMK_deployDHCP01)

5.  [クライアント コンピューターをドメインに追加してログオン](#BKMK_joinlogclients)

6.  [ネットワーク アクセスの認証および Web サービスのオプションの機能を展開します。](#BKMK_optionalfeatures)

> [!NOTE]
> -   このガイドのほとんどの手順に対応する Windows PowerShell コマンドが提供されます。 Windows PowerShell でこれらのコマンドレットを実行する前に、値の例をネットワーク展開に適切な値に置き換えます。 さらに、Windows PowerShell の 1 つの行で、各コマンドレットを入力する必要があります。 このガイドで、複数の行で、ブラウザーまたは他のアプリケーションによって制約とドキュメントの表示を書式設定のために個々 のコマンドレットがあります。
> -   このガイドの手順を場合についての説明を含まない、**ユーザー アカウント制御**続行の許可を要求するダイアログ ボックスが開きます。 ながら、お客様の操作に応答] ダイアログ ボックスが開いた場合] をクリックし、このガイドの手順を実行する、このダイアログ ボックスが開いた場合**続行**します。

### <a name="BKMK_configuringAll"></a>すべてのサーバーを構成します。
Active Directory Domain Services や DHCP など、他のテクノロジをインストールする前に、次の項目を構成する必要です。

-   [コンピューターを名前します。](#BKMK_rename)

-   [静的 IP アドレスを構成します。](#BKMK_ip)

次のセクションを使用すると、サーバーごとにこれらの操作を実行します。

メンバーシップ**管理者**、またはそれと同等がこれらの手順を実行するために必要な最小値。

#### <a name="BKMK_rename"></a>コンピューターを名前します。
このセクションで、手順を使用すると、コンピューターの名前を変更します。 コンピューターの名前変更をオペレーティング システムが自動的に作成コンピューター名を使用したくない場合に便利です。

> [!NOTE]
> Windows PowerShell を使用してこの手順を実行するには、PowerShell とを別々 の行では、次のコマンドレットを入力し、ENTER キーを押します。 置換する必要がありますも*ComputerName*とを使用する名前。
>
> `Rename-Computer`*コンピューター名*
>
> `Restart-Computer`

###### <a name="to-rename-computers-running-windows-server-2016-windows-server-2012-r2-and-windows-server-2012"></a>Windows Server 2016、Windows Server 2012 R2、および Windows Server 2012 を実行しているコンピューターの名前を変更するには

1.  サーバー マネージャーで、クリックして**ローカル サーバー**します。 コンピューター**プロパティ**詳細ウィンドウに表示されます。

2.  **プロパティ**で、**コンピューター名**、既存のコンピューター名をクリックします。 **システム プロパティ**] ダイアログ ボックスが開きます。 をクリックして**変更**します。 **コンピューター名/ドメイン名の変更**] ダイアログ ボックスが開きます。

3.  **コンピューター名/ドメイン名の変更**] ダイアログ ボックスで、**コンピューター名**、お使いのコンピューターの新しい名前を入力します。 たとえば、DC1 というコンピューター名にする場合は、入力**DC1**します。

4.  をクリックして**[ok]** 2 回クリックして**閉じる**します。 名前の変更を完了] をクリックすると、すぐにコンピューターを再起動する場合**今すぐ再起動**します。 それ以外の場合、] をクリックして**後で再起動**します。

> [!NOTE]
> 他の Microsoft オペレーティング システムを実行しているコンピューターの名前を変更する方法については、次を参照してください。[付録 A - コンピューターの名前を変更する](#BKMK_A)します。

#### <a name="BKMK_ip"></a>静的 IP アドレスを構成します。
このトピックの「手順を使用するにはインターネット プロトコル バージョン 4 (IPv4) を構成する Windows Server 2016 を実行しているコンピューターの静的 ip アドレスのネットワーク接続のプロパティに対応します。

> [!NOTE]
> Windows PowerShell を使用してこの手順を実行するには、PowerShell とを別々 の行では、次のコマンドレットを入力し、ENTER キーを押します。 インターフェイス名と、この例では、IP アドレスは、またコンピューターを構成するために必要な値に置き換える必要があります。
>
> `New-NetIPAddress -IPAddress 10.0.0.2 -InterfaceAlias "Ethernet" -DefaultGateway 10.0.0.1 -AddressFamily IPv4 -PrefixLength 24`
>
> `Set-DnsClientServerAddress -InterfaceAlias "Ethernet" -ServerAddresses 127.0.0.1`

###### <a name="to-configure-a-static-ip-address-on-computers-running-windows-server-2016-windows-server-2012-r2-and-windows-server-2012"></a>Windows Server 2016、Windows Server 2012 R2、および Windows Server 2012 を実行しているコンピューターで静的 IP アドレスを構成するには

1.  タスク バーで、[ネットワーク] アイコンを右クリックし、をクリックして**オープン ネットワークと共有センター**します。

2.  **ネットワークと共有センター**、] をクリックして**アダプター設定を変更する**します。 **ネットワーク接続**フォルダーを開き、利用可能なネットワーク接続が表示されます。

3.  **ネットワーク接続**、構成、およびをクリックする接続を右クリックして**プロパティ**します。 ネットワーク接続**プロパティ**] ダイアログ ボックスが開きます。

4.  ネットワーク接続で**プロパティ**] ダイアログ ボックスで、**この接続は、次の項目を使用して**を選択**インターネット プロトコル バージョン 4 (Tcp/ipv4)**、] をクリックし、**プロパティ**します。 **インターネット プロトコル バージョン 4 (Tcp/ipv4) プロパティ**] ダイアログ ボックスが開きます。

5.  **インターネット プロトコル バージョン 4 (Tcp/ipv4) プロパティ**] の [、**全般的な**] タブ、[**次の IP アドレスを使用して**します。 **IP アドレス**、使用する IP アドレスを入力します。

6.  Tab キーで、カーソルを押して**サブネット マスク**します。 サブネット マスクの既定値が自動的に入力します。 既定のサブネット マスクを受け入れるか、使用するサブネット マスクを入力します。

7.  **デフォルト ゲートウェイ**、デフォルト ゲートウェイの IP アドレスを入力します。

    > [!NOTE]
    > 構成する必要があります**デフォルト ゲートウェイ**、ルーターのローカル エリア ネットワーク (LAN) インターフェイスで使用する同じ IP アドレスを使用します。 たとえば、ルーターが lan だけでなくインターネットなどのワイド エリア ネットワーク (WAN) に接続されている場合は、構成する LAN インターフェイスとして指定することと同じ IP アドレスを持つ、**デフォルト ゲートウェイ**します。 別の例では、LAN A はアドレス範囲 10.0.0.0/24 を使用して、LAN B はアドレス範囲 192.168.0.0/24 を使用して、2 つの Lan に接続されているルーターがある場合はアドレス 10.0.0.1 など、そのアドレス範囲で LAN A ルーターの IP アドレスを構成します。 さらに、このアドレスの範囲の DHCP スコープ構成**デフォルト ゲートウェイ**を IP アドレス 10.0.0.1 します。 LAN B、192.168.0.1 など、そのアドレス範囲から取得したアドレスで LAN B ルーターのインターフェイスを構成し、LAN B スコープ 192.168.0.0/24 を構成、**デフォルト ゲートウェイ**値 192.168.0.1 で構成します。

8.  **優先 DNS サーバー**、DNS サーバーの IP アドレスを入力します。 優先 DNS サーバーにローカル コンピューターを使用する場合は、ローカル コンピューターの IP アドレスを入力します。

9. **代替 DNS サーバー**、存在する場合、代替の DNS サーバーの IP アドレスを入力します。 代替 DNS サーバーにローカル コンピューターを使用する場合は、ローカル コンピューターの IP アドレスを入力します。

10. をクリックして**OK**、] をクリックし、**閉じる**します。

> [!NOTE]
> 他の Microsoft オペレーティング システムを実行しているコンピューターで静的 IP アドレスを構成する方法については、次を参照してください。[付録 B - 静的 IP アドレスを構成する](#BKMK_B)します。

#### <a name="BKMK_deployADDNS01"></a>DC1 を展開します。
DC1 を展開するコンピューターを実行している Active Directory ドメイン サービス (AD DS) および DNS では、次の順序で手順を完了する必要があります。

-   セクションの手順を実行[すべてのサーバーを構成する](#BKMK_configuringAll)します。

-   [AD DS および DNS を新しいフォレストをインストールします。](#BKMK_installAD-DNS)

-   [Active Directory ユーザーとコンピュータにユーザー アカウントを作成します。](#BKMK_createUA)

-   [グループ メンバーシップを割り当てる](#BKMK_assigngroup)

-   [DNS 逆引き参照ゾーンを構成します。](#BKMK_reverse)

**管理者特権**

小規模なネットワークをインストールして、ネットワークの唯一の管理者は場合、は、自分のユーザー アカウントを作成して、自分のユーザー アカウントを Enterprise Admins および Domain Admins の両方のメンバーとして追加するをお勧めします。 これを行うしやすくなると、すべてのネットワーク リソースの管理者として機能します。 ログインすることこのアカウントを持つのみと管理タスクを実行する必要があります。 関連するタスクを非 IT を実行するための別のユーザー アカウントを作成することをお勧めします。

大規模な組織で複数の管理者があれば、組織の従業員の最適なグループ メンバーシップの決定を AD DS のドキュメントを参照してください。

**ドメイン ユーザー アカウントとローカル コンピューター上のユーザー アカウントの違い**

ドメイン ベースのインフラストラクチャの利点の 1 つは、ドメイン内の各コンピューターにユーザー アカウントを作成する必要がないことです。 これは、コンピューターがクライアント コンピューターまたはサーバーであるかどうかは true。

このため、ドメイン内の各コンピューターでないユーザー アカウントを作成する必要があります。 Active Directory ユーザーとコンピュータにすべてのユーザー アカウントを作成し、グループ メンバーシップを割り当てる、前述の手順を使用します。 既定では、すべてのユーザー アカウントは、Domain Users グループのメンバーです。

Domain Users グループのすべてのメンバー ログオンできます任意のクライアント コンピューターをドメインに参加していること後。

コンピューターにログオンするユーザーが許可される日付と時刻を指定するユーザー アカウントを構成することができます。 使用する各ユーザーが許可されているコンピューターを指定することもできます。 これらの設定を構成するのに Active Directory ユーザーとコンピュータを開くを構成するユーザー アカウントを見つけて、アカウントをダブルクリックします。 ユーザー アカウントで**プロパティ**、] をクリックして、**アカウント**タブをクリックし、をクリックするか**ログオン時間**または**ログに**します。

#### <a name="BKMK_installAD-DNS"></a>AD DS および DNS を新しいフォレストをインストールします。

次の手順のいずれかを使用して、Active Directory ドメイン サービス (AD DS) をインストールすることができますおよび DNS と新しいフォレストに新しいドメインを作成します。 

最初の手順は、Windows PowerShell を使って、2 番目の手順は、サーバー マネージャーを使用して AD DS および DNS をインストールする方法を表示中にこれらの操作を実行する方法について説明します。

>[!IMPORTANT]
>この手順の手順を実行が完了したら、コンピューターが自動的に再起動されます。

**AD DS と Windows PowerShell を使用して DNS をインストールします。**

AD DS および DNS インストールし、構成には、次のコマンドを使用できます。 この例では、ドメイン名は、ドメインを使用する値に置き換える必要があります。

>[!NOTE]
>これらの Windows PowerShell コマンドの詳細については、次のリファレンス トピックを参照してください。
>- [Install-windowsfeature](https://technet.microsoft.com/itpro/powershell/windows/server-manager/install-windowsfeature)
>- [Install-addsforest](https://technet.microsoft.com/itpro/powershell/windows/adds/deployment/install-addsforest)

メンバーシップ**管理者**はこの手順を実行するために必要な最小値。

- 管理者として Windows PowerShell を実行、次のコマンドを入力し、ENTER キーを押します。  

`Install-WindowsFeature AD-Domain-Services -IncludeManagementTools`

インストールが正常に完了したら、Windows PowerShell で次のメッセージが表示されます。

    
    Success Restart Needed  Exit Code   Feature Result
    ------- --------------  ---------   --------------
    True    No              Success     {Active Directory Domain Services, Group P...
    

- Windows PowerShell でテキストを置き換えて次のコマンドを入力**corp.contoso.com** 、ドメイン名とし、ENTER キーを押します。

````
Install-ADDSForest -DomainName "corp.contoso.com"
````

- Windows PowerShell ウィンドウの上部に表示されているが、インストールおよび構成プロセス中に、次のプロンプトが表示されます。 それが表示されたら、パスワードを入力し、し、ENTER キーを押します。

    **SafeModeAdministratorPassword:**

- パスワードを入力し、ENTER キーを押して、次の確認プロンプトが表示されます。 同じパスワードを入力し、ENTER キーを押します。

    **SafeModeAdministratorPassword を確認します。**

- 次のプロンプトが表示されたら、文字を入力**Y**し、ENTER キーを押します。

    
    ターゲット サーバーはドメイン コント ローラーとして構成されているし、この操作が完了すると、再起動します。
    この操作を続行しますか。
    [Y]、[はい] [A] [はい] に [N] すべて [S] にもない [L] を一時停止 [?](既定値は"Y") ヘルプ:
    
- AD DS および DNS の成功、通常のインストール中に表示される警告メッセージを読み取ることができます。 これらのメッセージは、通常はされ、インストール エラーを示す値ではありません。

- インストールが成功すると、コンピューターが再起動できるように、コンピューターからログオフ記録されるしようとしていることを示すメッセージが表示されます。 クリックすると**閉じる**、コンピューターの電源をすぐにログオンして、コンピューターが再起動します。 クリックしていない場合**閉じる**、既定の期間の後にコンピューターを再起動します。

- サーバーを再起動した後は、Active Directory Domain Services および DNS が正常にインストールを確認できます。 Windows PowerShell を開き、次のコマンドを入力 ENTER キーを押します。

````
Get-WindowsFeature
````

このコマンドの結果は、Windows PowerShell で表示され、下の画像の結果と同様にする必要があります。 テクノロジ名の左側に角かっこ、インストールされているテクノロジの文字が含まれている**X**の値と**インストール状態**は**インストール**します。

![Get-windowsfeature コマンドの結果](../media/Core-Network-Guide/server-roles-installed.jpg)

**AD DS と DNS サーバー マネージャーを使用してインストールします。**

1.  Dc1 で**サーバー マネージャー**、] をクリックして**管理**、] をクリックし、**追加の役割と機能**します。 追加の役割と機能のウィザードが開きます。

2.  **開始する前に**、] をクリックして**次**します。

    > [!NOTE]
    > **開始する前に**以前選択した場合、追加の役割と機能のウィザードのページが表示されない**既定でこのページをスキップ**、追加の役割と機能のウィザードが実行されました。

3.  **[インストールの種類**、いることを確認**役割ベースまたは機能ベースのインストール**をクリックして選択して**次**します。

4.  **対象サーバーの選択**、いることを確認**サーバー プールからサーバーを選択**が選択されています。 **サーバー プール**、ローカル コンピューターが選択されていることを確認します。 をクリックして**次**します。

5.  **サーバーの役割の選択**で、**役割**、] をクリックして**Active Directory Domain Services**します。 **Active Directory ドメイン サービスに必要な機能を追加する**、] をクリックして**機能の追加**します。 をクリックして**次**します。

6.  **機能の選択**、] をクリックして**次**、し、[ **Active Directory Domain Services**が提供され、] をクリックし、情報を確認**[次へ]**します。

7.  **インストール オプションの確認**、] をクリックして**インストール**します。 インストールの進行状況] ページでは、インストール プロセス中にステータスが表示されます。 プロセスが完了すると、メッセージの詳細でクリックして**このサーバーのドメイン コント ローラーを昇格**します。 Active Directory ドメイン サービス構成ウィザードが開きます。

8.  **展開構成**[**新しいフォレストの追加**します。 **ルート ドメイン名**、ドメインの完全修飾ドメイン名 (FQDN) を入力します。 たとえば、FQDN が corp.contoso.com の場合は、入力**corp.contoso.com**します。をクリックして**次**します。

9. **ドメイン コント ローラー オプション**で、**新しいフォレストおよびルート ドメインの機能レベルを選択して**、フォレストの機能レベルとを使用するドメインの機能レベルを選択します。 **ドメイン コント ローラーの機能を指定**、いることを確認**ドメイン ネーム システム (DNS) サーバー**と**グローバル カタログ (GC)**が選択されています。 **パスワード**と**パスワードの確認入力**、使用するディレクトリ サービス復元モード (DSRM) パスワードを入力します。 をクリックして**次**します。

10. **DNS オプション**、] をクリックして**次**します。

11. **追加オプション**、NetBIOS 名、ドメインに割り当てられていることを確認し、必要な場合にのみ変更します。 をクリックして**次**します。

12. **パス**で、 **AD DS データベース、ログ ファイル、および SYSVOL の場所を指定**、次のいずれかを実行します。

    -   既定値を受け入れます。

    -   フォルダーの場所を使用するように入力**データベース フォルダー**、**ログ ファイル フォルダー**、および**SYSVOL フォルダー**します。

13. をクリックして**次**します。

14. **オプションの確認**、選択内容を確認します。

15. 設定を Windows PowerShell スクリプトにエクスポートする場合は、クリックして**スクリプトの表示**します。 スクリプトがメモ帳で開きするフォルダーの場所に保存することができます。 をクリックして**次**します。 **前提条件のチェック**、選択内容を検証します。 チェックが完了したら、クリックして**インストール**します。 Windows のメッセージが表示されたら、] をクリックして**閉じる**します。 サーバーは、AD DS および DNS のインストールの完了に再起動します。

16. インストールの成功を確認するには、サーバーの再起動後、サーバー マネージャー コンソールを表示します。 AD DS および DNS の両方は、以下の画像で強調表示されている項目と同様に、左側のウィンドウに表示されます。

![AD DS および DNS サーバー マネージャーで](../media/Core-Network-Guide/server-roles-installed-sm.jpg)

##### <a name="BKMK_createUA"></a>Active Directory ユーザーとコンピュータにユーザー アカウントを作成します。
この手順を使用すると、Active Directory ユーザーとコンピューター Microsoft 管理コンソール (MMC) で新しいドメイン ユーザー アカウントを作成します。

メンバーシップ**Domain Admins**、相当するものでは、この手順を実行するために必要な最低限またはします。

> [!NOTE]
> Windows PowerShell を使用してこの手順を実行するには、PowerShell と 1 つの行に、次のコマンドレットを入力し、ENTER キーを押します。 使用する値にこの例では、ユーザー アカウント名を置き換える必要があります。
>
> `New-ADUser -SamAccountName User1 -AccountPassword (read-host "Set user password" -assecurestring) -name "User1" -enabled $true -PasswordNeverExpires $true -ChangePasswordAtLogon $false`
>
> ENTER キーを押すと、ユーザー アカウントのパスワードを入力します。 アカウントが作成され、既定では、メンバーシップが付与 Domain Users グループ。
>
> 次のコマンドレットでは、新しいユーザー アカウントの追加のグループ メンバーシップを割り当てることができます。 次の例は、User1 を Domain Admins と Enterprise Admins グループに追加します。 このコマンドが、ユーザー アカウント名、ドメイン名、および、要件に一致するようにグループを変更することを実行する前に確認します。
>
> `Add-ADPrincipalGroupMembership -Identity "CN=User1,CN=Users,DC=corp,DC=contoso,DC=com" -MemberOf "CN=Enterprise Admins,CN=Users,DC=corp,DC=contoso,DC=com","CN=Domain Admins,CN=Users,DC=corp,DC=contoso,DC=com"`

###### <a name="to-create-a-user-account"></a>ユーザー アカウントを作成するには

1.  DC1 で、サーバー マネージャーで、をクリックして**ツール**、] をクリックし、 **Active Directory ユーザーとコンピューター**します。 Active Directory ユーザーとコンピューター] MMC が開きます。 選択されていない場合は、ドメインのノードをクリックします。 たとえば、ドメインが corp.contoso.com の場合は、クリックして**corp.contoso.com**します。

2.  詳細ウィンドウで、ユーザー アカウントを追加するフォルダーを右クリックします。

    **どこ。**

    -   Active Directory ユーザーとコンピューター/*ドメイン ノード*/*フォルダー*

3.  をポイント**新規**、] をクリックし、**ユーザー**します。 **新しいオブジェクト - ユーザー** ] ダイアログ ボックスが開きます。

4.  **名**、ユーザーの名前を入力します。

5.  **頭文字**、ユーザーの頭文字を入力します。

6.  **姓、名**、ユーザーの姓を入力します。

7.  変更**フル_ネーム**頭文字を追加したり、名と姓の順序を逆にしたりします。

8.  **ユーザー ログオン名**、ユーザー ログオン名を入力します。 をクリックして**次**します。

9. **新しいオブジェクト - ユーザー**で、**パスワード**と**パスワードの確認入力**、ユーザーのパスワードを入力し、適切なパスワード オプションを選択します。

10. をクリックして**次**で新しいユーザー アカウントの設定を確認し、をクリックして**完了**します。

##### <a name="BKMK_assigngroup"></a>グループ メンバーシップを割り当てる
この手順を使用すると、ユーザー、コンピューター、またはグループを Active Directory ユーザーとコンピューター Microsoft 管理コンソール (MMC) のグループに追加します。

メンバーシップ**Domain Admins**、またはそれと同等がこの手順を実行するために必要な最小値。

###### <a name="to-assign-group-membership"></a>グループのメンバーシップを割り当てる

1.  DC1 で、サーバー マネージャーで、をクリックして**ツール**、] をクリックし、 **Active Directory ユーザーとコンピューター**します。 Active Directory ユーザーとコンピューター] MMC が開きます。 選択されていない場合は、ドメインのノードをクリックします。 たとえば、ドメインが corp.contoso.com の場合は、クリックして**corp.contoso.com**します。

2.  詳細ウィンドウで、メンバーを追加するグループが含まれているフォルダーをダブルクリックします。

    どこ。

    -   **Active Directory ユーザーとコンピューター**/*ドメイン ノード*/*グループが含まれているフォルダー*

3.  詳細ウィンドウで、オブジェクトを右クリックなどのユーザーまたはコンピューターのグループに追加し、をクリックする**プロパティ**します。 オブジェクトの**プロパティ**] ダイアログ ボックスが開きます。 をクリックして、**のメンバー** ] タブ。

4.  **のメンバー** ] タブ、[**追加**します。

5.  **選択するオブジェクト名を入力**、クリックして、オブジェクトを追加するグループの名前を入力**OK**します。

6.  その他のユーザー、グループ、またはコンピューターにグループ メンバーシップを割り当てる、手順 4 と 5 を繰り返します。

##### <a name="BKMK_reverse"></a>DNS 逆引き参照ゾーンを構成します。
この手順を使用すると、ドメイン ネーム システム (DNS) の逆引き参照ゾーンを構成します。

メンバーシップ**Domain Admins**はこの手順を実行するために必要な最小値。

> [!NOTE]
> -   中規模および大規模組織では、構成して、Active Directory ユーザーとコンピューターで DNSAdmins グループを使用することをお勧めします。 詳細については、次を参照してください[さまざまな技術資料。](#BKMK_resources)
> -   Windows PowerShell を使用してこの手順を実行するには、PowerShell と 1 つの行に、次のコマンドレットを入力し、ENTER キーを押します。 使用する値にこの例では DNS 逆引き参照ゾーンとゾーン ファイル名を置き換える必要があります。 逆引き参照ゾーン名のネットワーク ID を逆にすることを確認します。 たとえば、ネットワーク ID が 192.168.0 の場合は、作成、逆引き参照ゾーン名**0.168.192.in-addr.arpa**します。
>
> `Add-DnsServerPrimaryZone 0.0.10.in-addr.arpa -ZoneFile 0.0.10.in-addr.arpa.dns`

###### <a name="to-configure-a-dns-reverse-lookup-zone"></a>DNS 逆引き参照ゾーンを構成するには

1.  DC1 で、サーバー マネージャーで、をクリックして**ツール**、] をクリックし、 **DNS**します。 DNS MMC が開きます。

2.  展開されていない場合は、dns でツリーを展開するサーバー名をダブルクリックします。 たとえば、DNS サーバー名が DC1 の場合は、ダブルクリック**DC1**します。

3.  選択**逆引き参照ゾーン**、右クリック**逆引き参照ゾーン**、] をクリックし、**新しいゾーン**します。 新しいゾーン ウィザードが開きます。

4.  **新しいゾーン ウィザードへようこそ**、] をクリックして**次**します。

5.  **ゾーンの種類**[**プライマリ ゾーン**します。

6.  DNS サーバーが書き込み可能なドメイン コント ローラーの場合は、いることを確認**ゾーンを格納する Active Directory で**が選択されています。 をクリックして**次**します。

7.  **Active Directory ゾーン レプリケーション スコープ**[**このドメインのドメイン コント ローラーで実行されているすべての DNS サーバーに**さまざまなオプションを選択する特別な理由がない限り、します。 をクリックして**次**します。

8.  最初の**逆引き参照ゾーン名**] ページで、[ **IPv4 逆引き参照ゾーン**します。 をクリックして**次**します。

9. 2 番目の**逆引き参照ゾーン名**] ページで、次のいずれかを実行します。

    -   **ネットワーク ID**、IP アドレスの範囲のネットワーク ID を入力します。 たとえば、IP アドレスの範囲が 10.0.0.1 ~ 10.0.0.254 の場合は、入力**10.0.0**します。

    -   **逆引き参照ゾーン名**、IPv4 逆引き参照ゾーン名が自動的に追加します。 をクリックして**次**します。

10. **動的更新**、許可する動的更新の種類を選択します。 をクリックして**次**します。

11. **新しいゾーン ウィザードの完了**選択内容を確認し、[クリックして**完了**します。

#### <a name="BKMK_joinlogserver"></a>サーバーのコンピューターをドメインに追加してログオン
Active Directory ドメイン サービス (AD DS) をインストールし、コンピューターをドメインに参加させるアクセス許可を持つ 1 つまたは複数のユーザー アカウントを作成した後は、動的ホスト構成プロトコル (DHCP) などの他のテクノロジをインストールするためにドメインと、サーバーにログオンするコア ネットワーク サーバーを参加できます。

展開する場合、AD DS を実行しているサーバーを除くすべてのサーバー上で、次の手順を実行します。

1.  提供されている手順を完了[すべてのサーバーを構成する](#BKMK_configuringAll)します。

2.  およびに参加する、サーバーをドメインに追加の展開のタスクを実行するのにサーバーにログオンするのにには、次の 2 つの手順の手順を使用します。

> [!NOTE]
> Windows PowerShell を使用してこの手順を実行するには、PowerShell を開くし、次のコマンドレットを入力し、ENTER キーを押します。 使用する名前にドメイン名を置き換える必要があります。
>
> `Add-Computer -DomainName corp.contoso.com`
>
> そのためにはメッセージが表示されたら、ユーザー名とコンピューターをドメインに参加させるアクセス許可を持つアカウントのパスワードを入力します。 コンピューターを再起動するには、次のコマンドを入力し、ENTER キーを押します。
>
> `Restart-Computer`

###### <a name="to-join-computers-running--windows-server-2016--windows-server-2012-r2--and--windows-server-2012--to-the-domain"></a>ドメインに Windows Server 2016、Windows Server 2012 R2、および Windows Server 2012 を実行しているコンピューターを参加させる

1.  サーバー マネージャーで、クリックして**ローカル サーバー**します。 詳細ウィンドウで、をクリックして**ワークグループ**します。 **システム プロパティ**] ダイアログ ボックスが開きます。

2.  **システム プロパティ**ダイアログ ボックスで、をクリックして**変更**します。 **コンピューター名/ドメイン名の変更**] ダイアログ ボックスが開きます。

3.  **コンピューター名**で、**のメンバー**、] をクリックして**ドメイン**、しに参加するドメインの名前を入力します。 たとえば、ドメイン名が corp.contoso.com の場合は、入力**corp.contoso.com**します。

4.  をクリックして**OK**します。 **Windows セキュリティ**] ダイアログ ボックスが開きます。

5.  **コンピューター名/ドメイン名の変更**で、**ユーザー名**、ユーザー名を入力し、[**パスワード**パスワードを入力し、[クリックして**OK**します。 **コンピューター名/ドメイン名の変更**をドメインにそれを通知するダイアログ ボックスが開きます。 をクリックして**OK**します。

6.  **コンピューター名/ドメイン名の変更**] ダイアログ ボックスで、変更を適用するコンピューターを再起動する必要があることを示すメッセージが表示されます。 をクリックして**OK**します。

7.  **システムのプロパティ**] ダイアログ ボックスで、**コンピューター名**] タブ、[**閉じる**します。 **Microsoft Windows** ] ダイアログ ボックスが開き、もう一度変更を適用するコンピューターを再起動する必要があることを示すメッセージが表示されます。 をクリックして**今すぐ再起動**します。

> [!NOTE]
> ドメインに他の Microsoft オペレーティング システムを実行しているコンピューターを参加させる方法については、次を参照してください。[付録 C - コンピューターをドメインに参加させる](#BKMK_C)します。

###### <a name="to-log-on-to-the-domain-using-computers-running-windows-server-2016"></a>Windows Server 2016 を実行しているコンピューターを使用してドメインにログオンするには

1.  ログオフ、またはコンピューターを再起動します。

2.  Ctrl キーを押しながら ALT + 削除します。 ログオン画面が表示されます。

3.  をクリックして、左下隅で**その他のユーザー**します。

4.  **ユーザー名**、ユーザー名を入力します。

5.  **パスワード**、ドメイン パスワードを入力し、矢印をクリックしてまたは ENTER キーを押します。

> [!NOTE]
> 他の Microsoft オペレーティング システムを実行しているコンピューターを使用してドメインにログオンする方法については、次を参照してください。[付録 D - ドメインにログオン](#BKMK_D)します。

#### <a name="BKMK_deployDHCP01"></a>DHCP1 を展開します。
コア ネットワークのこのコンポーネントを展開するには、前に、以下を実行する必要があります。

-   セクションの手順を実行[すべてのサーバーを構成する](#BKMK_configuringAll)します。

-   セクションの手順を実行[への参加サーバー コンピューターをドメインとログオン](#BKMK_joinlogserver)します。

コンピューターである DHCP1 を展開する動的ホスト構成プロトコル (DHCP) サーバーの役割を実行するには、次の順序で次の手順を完了する必要があります。

-   [動的ホスト構成プロトコル (DHCP) のインストールします。](#BKMK_installDHCP)

-   [作成し、新しい DHCP スコープをアクティブ化](#BKMK_newscopeDHCP)

> [!NOTE]
> Windows PowerShell を使用してこれらの手順を実行するには、PowerShell とを別々 の行では、次のコマンドレットを入力し、ENTER キーを押します。 スコープ名、IP アドレスの開始および終了範囲、サブネット マスク、およびその他の値をこの例では、またを使用する値に置き換える必要があります。
>
> `Install-WindowsFeature DHCP -IncludeManagementTools`
>
> `Add-DhcpServerv4Scope -name "Corpnet" -StartRange 10.0.0.1 -EndRange 10.0.0.254 -SubnetMask 255.255.255.0 -State Active`
>
> `Add-DhcpServerv4ExclusionRange -ScopeID 10.0.0.0 -StartRange 10.0.0.1 -EndRange 10.0.0.15`
>
> `Set-DhcpServerv4OptionValue -OptionID 3 -Value 10.0.0.1 -ScopeID 10.0.0.0 -ComputerName DHCP1.corp.contoso.com`
>
> `Add-DhcpServerv4Scope -name "Corpnet2" -StartRange 10.0.1.1 -EndRange 10.0.1.254 -SubnetMask 255.255.255.0 -State Active`
>
> `Add-DhcpServerv4ExclusionRange -ScopeID 10.0.1.0 -StartRange 10.0.1.1 -EndRange 10.0.1.15`
>
> `Set-DhcpServerv4OptionValue -OptionID 3 -Value 10.0.1.1 -ScopeID 10.0.1.0 -ComputerName DHCP1.corp.contoso.com`
>
> `Set-DhcpServerv4OptionValue -DnsDomain corp.contoso.com -DnsServer 10.0.0.2`
>
> `Add-DhcpServerInDC -DnsName DHCP1.corp.contoso.com`

##### <a name="BKMK_installDHCP"></a>動的ホスト構成プロトコル (DHCP) のインストールします。
インストールして、追加の役割と機能のウィザードを使用して DHCP サーバーの役割を構成するのには、この手順を使用することができます。

メンバーシップ**Domain Admins**、相当するものでは、この手順を実行するために必要な最低限またはします。

###### <a name="to-install-dhcp"></a>DHCP をインストールするには

1.  Dhcp1 のサーバー マネージャーで、をクリックして**管理**、] をクリックし、**追加の役割と機能**します。 追加の役割と機能のウィザードが開きます。

2.  **開始する前に**、] をクリックして**次**します。

    > [!NOTE]
    > **開始する前に**以前選択した場合、追加の役割と機能のウィザードのページが表示されない**既定でこのページをスキップ**、追加の役割と機能のウィザードが実行されました。

3.  **[インストールの種類**、いることを確認**役割ベースまたは機能ベースのインストール**をクリックして選択して**次**します。

4.  **対象サーバーの選択**、いることを確認**サーバー プールからサーバーを選択**が選択されています。 **サーバー プール**、ローカル コンピューターが選択されていることを確認します。 をクリックして**次**します。

5.  **サーバーの役割の選択**で、**役割**[ **DHCP サーバー**します。 **DHCP サーバーに必要な機能を追加**、] をクリックして**機能の追加**します。 をクリックして**次**します。

6.  **機能の選択**、] をクリックして**[次へ]**、し、[ **DHCP サーバー**が提供され、] をクリックし、情報を確認**次**します。

7.  **インストール オプションの確認**、] をクリックして**必要な場合に、移行先サーバーを自動的に再起動**します。 これを選択したことを確認するメッセージが表示されたら、] をクリックして**はい**、] をクリックし、**インストール**します。 **インストールの進行状況**ページには、インストール プロセス中にステータスが表示されます。 ときに、プロセスが完了すると、メッセージ"構成が必要です。 インストールが成功した*ComputerName*"が表示されます*ComputerName*は DHCP サーバーをインストールしたコンピューターの名前です。 メッセージ ウィンドウで、クリックして**完全な DHCP 構成**します。 DHCP インストール後の構成ウィザードが開きます。 をクリックして**次**します。

8.  **承認**、Active Directory ドメイン サービスでは、DHCP サーバーを承認し、[使用する資格情報を指定**コミット**します。 認証が完了したら、クリックして**閉じる**します。

##### <a name="BKMK_newscopeDHCP"></a>作成し、新しい DHCP スコープをアクティブ化
この手順を使用すると、DHCP Microsoft 管理コンソール (MMC) を使用して、新しい DHCP スコープを作成します。 手順を完了すると、スコープがアクティブ化し、除外範囲を作成するを防止、DHCP サーバー、サーバーを静的に構成するために使用する IP アドレスと静的 IP アドレスを必要とするその他のデバイスをリースします。

メンバーシップ**DHCP Administrators**、相当するものでは、この手順を実行するために必要な最低限またはします。

###### <a name="to-create-and-activate-a-new-dhcp-scope"></a>作成し、新しい DHCP スコープをアクティブ化

1.  Dhcp1 のサーバー マネージャーで、をクリックして**ツール**、] をクリックし、 **DHCP**します。 DHCP MMC が開きます。

2.  **DHCP**、サーバー名を展開します。 たとえば、DHCP サーバー名が dhcp1.corp.contoso.com の場合の場合は、下向き矢印をクリックして] の横に**[dhcp1.corp.contoso.com]**します。

3.  右クリックし、サーバー名の下にある**IPv4**、] をクリックし、**新しいスコープ**します。 新しいスコープ ウィザードが開きます。

4.  **新しいスコープ ウィザードへようこそ**、] をクリックして**次**します。

5.  **スコープ名**で、**名**、スコープの名前を入力します。 たとえば、入力**サブネット 1**します。

6.  **説明**、新しいスコープの説明を入力し、[クリックして**次**します。

7.  **IP アドレスの範囲**、次の操作します。

    1.  **開始 IP アドレス**、IP アドレス範囲の最初の IP アドレスを入力します。 たとえば、入力**10.0.0.1**します。

    2.  **終了 IP アドレス**、最後の IP アドレスの範囲にある IP アドレスを入力します。 たとえば、入力**10.0.0.254**します。 値が**長さ**と**サブネット マスク**入力、自動的に入力した IP アドレスに基づく**開始 IP アドレス**します。

    3.  必要に応じて、内の値を変更**長さ**または**サブネット マスク**必要に応じて、アドレス指定スキームにします。

    4.  をクリックして**次**します。

8.  **除外の追加**、次の操作します。

    1.  **開始 IP アドレス**、除外範囲の最初の IP アドレスを IP アドレスを入力します。 たとえば、入力**10.0.0.1**します。

    2.  **終了 IP アドレス**、たとえばアドレス、除外範囲の末尾の IP にある IP アドレスの種類、種類**10.0.0.15**します。

9. をクリックして**追加**、] をクリックし、**次**します。

10. **リース期間**の既定値を変更**日**、**時間**、および**分**に合わせて、ネットワーク、および] をクリックし、 **[次へ]**します。

11. **DHCP オプションの構成**[**これらのオプションを構成する**、] をクリックし、 **[次へ]**します。

12. **ルーター (デフォルト ゲートウェイ)**、次のいずれかを実行します。

    -   場合は、ネットワーク上のルーターがない、] をクリックして**次**します。

    -   **IP アドレス**、またはデフォルト ゲートウェイ、ルーターの IP アドレスを入力します。 たとえば、入力**10.0.0.1**します。 をクリックして**追加**、] をクリックし、**次**します。

13. **ドメイン名と DNS サーバー**、次の操作します。

    1.  **親ドメイン**、クライアントが名前解決のために使用する DNS ドメインの名前を入力します。 たとえば、入力**corp.contoso.com**します。

    2.  **サーバー名**、クライアントが名前解決のために使用する DNS コンピューターの名前を入力します。 たとえば、入力**DC1**します。

    3.  をクリックして**解決**します。 DNS サーバーの IP アドレスが追加された**IP アドレス**します。 をクリックして**追加**DNS サーバーの IP アドレス検証を完了するまで待機し、[クリックして**次**します。

14. **WINS サーバー**、ネットワーク上の WINS サーバーがあるないため、] をクリックして**次**します。

15. **スコープのアクティブ化**[**できるようになりましたこのスコープをアクティブ化する**します。

16. をクリックして**次**、] をクリックし、**完了**します。

> [!IMPORTANT]
> 追加のサブネットに新しいスコープを作成するには、この手順を繰り返します。 展開、および他のサブネットにすべてのルーターで DHCP メッセージ転送が有効になっていることを確認しようとしているサブネットごとに異なる IP アドレスの範囲を使用します。

### <a name="BKMK_joinlogclients"></a>クライアント コンピューターをドメインに追加してログオン

> [!NOTE]
> Windows PowerShell を使用してこの手順を実行するには、PowerShell を開くし、次のコマンドレットを入力し、ENTER キーを押します。 使用する名前にドメイン名を置き換える必要があります。
>
> `Add-Computer -DomainName corp.contoso.com`
>
> そのためにはメッセージが表示されたら、ユーザー名とコンピューターをドメインに参加させるアクセス許可を持つアカウントのパスワードを入力します。 コンピューターを再起動するには、次のコマンドを入力し、ENTER キーを押します。
>
> `Restart-Computer`

##### <a name="to-join-computers-running-windows-10-to-the-domain"></a>Windows 10 を実行して、ドメインにコンピューターを参加させる

1.  ローカルの Administrator アカウントを使用してコンピューターにログオンします。

2.  **Web や Windows 検索**、種類**システム**します。 検索結果から] をクリックして**システム (コントロール パネル)**します。 **システム**] ダイアログ ボックスが開きます。

3.  **システム**、] をクリックして**システムの詳細設定**します。 **システム プロパティ**] ダイアログ ボックスが開きます。 をクリックして、**コンピューター名**] タブ。

4.  **コンピューター名**、] をクリックして**変更**します。 **コンピューター名/ドメイン名の変更**] ダイアログ ボックスが開きます。

5.  **コンピューター名/ドメイン名の変更**で、**のメンバー**、] をクリックして**ドメイン**、参加するドメインの名前を入力します。 たとえば、ドメイン名が corp.contoso.com の場合は、入力**corp.contoso.com**します。

6.  をクリックして**OK**します。 **Windows セキュリティ**] ダイアログ ボックスが開きます。

7.  **コンピューター名/ドメイン名の変更**で、**ユーザー名**、ユーザー名を入力し、[**パスワード**パスワードを入力し、[クリックして**OK**します。 **コンピューター名/ドメイン名の変更**をドメインにそれを通知するダイアログ ボックスが開きます。 をクリックして**OK**します。

8.  **コンピューター名/ドメイン名の変更**] ダイアログ ボックスで、変更を適用するコンピューターを再起動する必要があることを示すメッセージが表示されます。 をクリックして**OK**します。

9. **システムのプロパティ**] ダイアログ ボックスで、**コンピューター名**] タブ、[**閉じる**します。 **Microsoft Windows** ] ダイアログ ボックスが開き、もう一度変更を適用するコンピューターを再起動する必要があることを示すメッセージが表示されます。 をクリックして**今すぐ再起動**します。

##### <a name="to-join-computers-running-windows-81-to-the-domain"></a>ドメインに Windows 8.1 を実行しているコンピューターを参加させる

1.  ローカルの Administrator アカウントを使用してコンピューターにログオンします。

2.  右クリック**開始**、] をクリックし、**システム**します。 **システム**] ダイアログ ボックスが開きます。

3.  **システム**、] をクリックして**システムの詳細設定**します。 **システム プロパティ**] ダイアログ ボックスが開きます。 をクリックして、**コンピューター名**] タブ。

4.  **コンピューター名**、] をクリックして**変更**します。 **コンピューター名/ドメイン名の変更**] ダイアログ ボックスが開きます。

5.  **コンピューター名/ドメイン名の変更**で、**のメンバー**、] をクリックして**ドメイン**、参加するドメインの名前を入力します。 たとえば、ドメイン名が corp.contoso.com の場合は、入力**corp.contoso.com**します。

6.  をクリックして**OK**します。 **Windows セキュリティ**] ダイアログ ボックスが開きます。

7.  **コンピューター名/ドメイン名の変更**で、**ユーザー名**、ユーザー名を入力し、[**パスワード**パスワードを入力し、[クリックして**OK**します。 **コンピューター名/ドメイン名の変更**をドメインにそれを通知するダイアログ ボックスが開きます。 をクリックして**OK**します。

8.  **コンピューター名/ドメイン名の変更**] ダイアログ ボックスで、変更を適用するコンピューターを再起動する必要があることを示すメッセージが表示されます。 をクリックして**OK**します。

9. **システムのプロパティ**] ダイアログ ボックスで、**コンピューター名**] タブ、[**閉じる**します。 **Microsoft Windows** ] ダイアログ ボックスが開き、もう一度変更を適用するコンピューターを再起動する必要があることを示すメッセージが表示されます。 をクリックして**今すぐ再起動**します。

##### <a name="to-log-on-to-the-domain-using-computers-running-windows-10"></a>Windows 10 を実行しているコンピューターを使用してドメインにログオンするには

1.  ログオフ、またはコンピューターを再起動します。

2.  Ctrl キーを押しながら ALT + 削除します。 ログオン画面が表示されます。

3.  左下でクリックして**その他のユーザー**します。

4.  **ユーザー名**、形式で、ドメインとユーザー名を入力*domain \user*します。 たとえば、という名前のアカウントで corp.contoso.com ドメインにログオンする**- 01 ユーザー**、種類**\user-01**します。

5.  **パスワード**、ドメイン パスワードを入力し、矢印をクリックしてまたは ENTER キーを押します。

### <a name="BKMK_optionalfeatures"></a>ネットワーク アクセスの認証および Web サービスのオプションの機能を展開します。
コア ネットワークのインストール後に、ワイヤレス アクセス ポイントや VPN サーバーなどのネットワーク アクセス サーバーを展開する場合は、NPS サーバーと Web サーバーの両方を展開することをお勧めします。 ネットワーク アクセスの展開のセキュリティで保護された証明書ベースの認証方法の使用をお勧めします。 ネットワーク アクセス ポリシーを管理し、セキュリティで保護された認証方法を展開するには、NPS を使用することができます。 Web サーバーを使用して、安全な認証の証明書を提供する、証明機関 (CA) の証明書失効リスト (CRL) を公開することができます。

> [!NOTE]
> コア ネットワーク必携ガイドを使用して、サーバー証明書とその他の追加機能を展開することができます。 詳細については、次を参照してください。[さまざまな技術資料](#BKMK_resources)します。

次の図は、追加された NPS サーバーと Web サーバーで Windows Server コア ネットワーク トポロジを示します。

![追加された NPS サーバーと Web サーバーで Windows Server コア ネットワーク トポロジ](../media/Core-Network-Guide/cng16_overview_2.jpg)

次のセクションでは、NPS サーバーと Web サーバーをネットワークに追加するについて説明します。

-   [NPS1 を展開します。](#BKMK_deployNPS1)

-   [WEB1 を展開します。](#BKMK_IIS)

#### <a name="BKMK_deployNPS1"></a>NPS1 を展開します。
ネットワーク ポリシー サーバー (NPS) サーバーは、仮想プライベート ネットワーク (VPN) サーバー、ワイヤレス アクセス ポイント、802.1 X 認証スイッチなど、他のネットワーク アクセス テクノロジを展開するための準備手順としてインストールされます。

ネットワーク ポリシー サーバー (NPS) では、一元的に構成し、次の機能により、ネットワーク ポリシーを管理することができます。 リモート認証ダイヤルイン ユーザー サービス (RADIUS) サーバーと RADIUS プロキシします。

NPS はコア ネットワークのオプションのコンポーネントが、次のいずれかに該当する場合は、NPS をインストールする必要があります。

-   Windows Server 2016、Windows Server 2012 R2、Windows Server 2012、Windows Server 2008 R2 または Windows Server 2008 およびルーティングとリモート アクセス サービス、ターミナル サービス ゲートウェイ、またはリモート デスクトップ ゲートウェイを実行するコンピューターなど、RADIUS プロトコルと互換性のあるリモート アクセス サーバーを含む、ネットワークの展開を計画しています。


-   ワイヤード (有線) 802.1 X 認証を展開するか、ワイヤレス アクセスを計画します。

この役割サービスを展開するには、前に NPS サーバーとして構成しているコンピューターで次の手順を実行する必要があります。

-   セクションの手順を実行[すべてのサーバーを構成する](#BKMK_configuringAll)します。

-   セクションの手順を実行[への参加サーバー コンピューターをドメインとログオン](#BKMK_joinlogserver)

コンピューターである NPS1 を展開するネットワーク ポリシーとアクセス サービス サーバーの役割のネットワーク ポリシー サーバー (NPS) 役割サービスを実行するには、この手順を完了する必要があります。

-   [NPS1 の展開の計画](#bkmk_NetFndtn_Pln_NPS-01)

-   [ネットワーク ポリシー サーバー (NPS) のインストールします。](#BKMK_installNPS)

-   [既定のドメインに NPS サーバーを登録します。](#BKMK_registerNPS)

> [!NOTE]
> このガイドでは、スタンドアロン サーバーに NPS を展開するための説明または VM NPS1 をという名前です。  別の推奨される展開モデルは、ドメイン コント ローラー、NPS のインストールです。 スタンドアロン サーバー上の代わりにドメイン コント ローラー、NPS をインストールする場合は、DC1 に NPS をインストールします。

##### <a name="bkmk_NetFndtn_Pln_NPS-01"></a>NPS1 の展開の計画
コア ネットワークの展開後に、ワイヤレス アクセス ポイントや VPN サーバーなどのネットワーク アクセス サーバーを展開する場合は、NPS を展開することをお勧めします。

NPS をリモート認証ダイヤルイン ユーザー サービス (RADIUS) サーバーとして使用する場合、NPS は、ネットワーク アクセス サーバーを介した接続要求の認証と承認を実行します。 NPS では一元的に構成して、ネットワーク ポリシーを管理することもできますが、ネットワーク アクセスする方法、ネットワークにアクセスしたときに、ネットワークにアクセスできるユーザーを決定します。

NPS をインストールする前に重要な計画手順を次に示します。

- ユーザー アカウント データベースを計画します。 既定では場合は、Active Directory ドメインに NPS を実行しているサーバーを参加すると、NPS は認証と承認の AD DS ユーザー アカウント データベースを使用してを実行します。 場合によってなどの他の RADIUS サーバーへの接続要求を転送するように RADIUS プロキシとしての NPS を使用する大規模なネットワークとする場合があります非ドメイン メンバー コンピューターで NPS をインストールします。

- RADIUS アカウンティングを計画します。 NPS では、SQL Server データベースまたはローカル コンピューター上のテキスト ファイルは、アカウンティング データをログ記録することができます。 SQL Server ログを使用する場合は、インストールおよび SQL Server を実行しているサーバーの構成を計画します。

##### <a name="BKMK_installNPS"></a>ネットワーク ポリシー サーバー (NPS) のインストールします。
この手順を使用すると、追加の役割と機能のウィザードを使用してネットワーク ポリシー サーバー (NPS) をインストールします。 NPS は、ネットワーク ポリシーとアクセス サービス サーバーの役割の役割サービスです。

> [!NOTE]
> 既定では、NPS は、RADIUS トラフィックをすべてインストールされているネットワーク アダプターのポート 1812、1813、1645、および 1646 でリッスンします。 NPS をインストールするときに、セキュリティが強化された Windows ファイアウォールが有効である場合、これらのポートに対するファイアウォールの例外はインターネット プロトコル バージョン 6 \(IPv6\) と IPv4 トラフィックの両方のインストール プロセス中に自動的に作成されます。 場合は、ネットワーク アクセス サーバーは、これらの既定以外のポート上で RADIUS トラフィックを送信するように構成、セキュリティが強化された Windows ファイアウォールで、NPS のインストール中に作成される例外を削除し、RADIUS トラフィックに使用するポートの例外を作成します。

**管理者の資格情報**

この手順を完了するには、メンバーである、**Domain Admins**グループ。

> [!NOTE]
> Windows PowerShell を使用してこの手順を実行、PowerShell を開き、次のように入力し、ENTER キーを押します。
>
> `Install-WindowsFeature NPAS -IncludeManagementTools`

###### <a name="to-install-nps"></a>NPS をインストールするには

1.  NPS1 のサーバー マネージャーで、をクリックして**管理**、] をクリックし、**追加の役割と機能**します。 追加の役割と機能のウィザードが開きます。

2.  **開始する前に**、] をクリックして**次**します。

    > [!NOTE]
    > **開始する前に**以前選択した場合、追加の役割と機能のウィザードのページが表示されない**既定でこのページをスキップ**、追加の役割と機能のウィザードが実行されました。

3.  **[インストールの種類**、いることを確認**役割ベースまたは機能ベースのインストール**をクリックして選択して**次**します。

4.  **対象サーバーの選択**、いることを確認**サーバー プールからサーバーを選択**が選択されています。 **サーバー プール**、ローカル コンピューターが選択されていることを確認します。 をクリックして**次**します。

5.  **サーバーの役割の選択**で、**役割**[**ネットワーク ポリシーとアクセス サービス**します。 ネットワーク ポリシーとアクセス サービスに必要な機能を追加するかどうかを求めるダイアログ ボックスが開きます。 をクリックして**機能の追加**、] をクリックし、**次**します。

6.  **機能の選択**、] をクリックして**[次へ]**、し、[**ネットワーク ポリシーとアクセス サービス**が提供され、] をクリックし、情報を確認**次**します。

7.  **役割サービスの選択**、] をクリックして**ネットワーク ポリシー サーバー**します。  **ネットワーク ポリシー サーバーに必要な機能を追加**、] をクリックして**機能の追加**します。 をクリックして**次**します。

8.  **インストール オプションの確認**、] をクリックして**必要な場合に、移行先サーバーを自動的に再起動**します。 これを選択したことを確認するメッセージが表示されたら、] をクリックして**はい**、] をクリックし、**インストール**します。 インストールの進行状況] ページでは、インストール プロセス中にステータスが表示されます。 ときに、プロセスが完了すると、メッセージ"でインストールが成功した*ComputerName*"が表示されます*ComputerName*はネットワーク ポリシー サーバーをインストールしたコンピューターの名前です。 をクリックして**閉じる**します。

##### <a name="BKMK_registerNPS"></a>既定のドメインに NPS サーバーを登録します。
この手順を使用するには、サーバーがドメインのメンバーがドメインに NPS サーバーを登録します。

NPS サーバーは、承認プロセス中にユーザー アカウントのダイヤルイン プロパティを読み取るアクセス許可があるために、Active Directory に登録する必要があります。 サーバーを追加する NPS サーバーの登録、**RAS および IAS サーバー** Active Directory にグループ化します。

**管理者の資格情報**

この手順を完了するには、メンバーである、**Domain Admins**グループ。

> [!NOTE]
> Windows PowerShell 内でのネットワーク シェル (Netsh) コマンドを使用してこの手順を実行、PowerShell を開き、次のように入力し、ENTER キーを押します。
>
> `netsh nps add registeredserver domain=corp.contoso.com server=NPS1.corp.contoso.com`

###### <a name="to-register-an-nps-server-in-its-default-domain"></a>既定のドメインに NPS サーバーを登録するには

1.  Nps1 のサーバー マネージャーで、[ツール] をクリックして**ネットワーク ポリシー サーバー**します。 ネットワーク ポリシー サーバー MMC が開きます。

2.  右クリック**NPS (ローカル)**、] をクリックし、 **Active Directory でサーバーの登録**します。 **ネットワーク ポリシー サーバー** ] ダイアログ ボックスが開きます。

3.  **ネットワーク ポリシー サーバー**、] をクリックして**OK**、] をクリックし、**[OK]**もう一度します。

ネットワーク ポリシー サーバーの詳細については、次を参照してください。[ネットワーク ポリシー サーバー (NPS)](../technologies/nps/nps-top.md)します。

#### <a name="BKMK_IIS"></a>WEB1 を展開します。

Windows Server 2016 で Web サーバー (IIS) 役割は、web サイト、サービス、およびアプリケーションを確実にホストするため、セキュリティで保護された、管理が容易なモジュールで拡張性の高いプラットフォームを提供します。 インターネット インフォメーション サービス (IIS) で、インターネット、イントラネット、またはエクストラネット上のユーザーと情報を共有できます。 IIS は、IIS、ASP.NET、FTP サービス、PHP、および Windows Communication Foundation (WCF) を一体化した統合 web プラットフォームです。

ドメイン メンバー コンピューターがアクセスするための CRL を公開することを許可するだけでなく、Web サーバー (IIS) サーバーの役割にセットアップおよび複数の web サイト、web アプリケーション、および FTP サイトを管理することができます。 IIS では、次の利点も提供します。

-   削減サーバーのフット プリントとアプリケーションの自動分離により、web セキュリティを最大化します。

-   簡単に展開し、実行 ASP.NET、classic ASP、および PHP web アプリケーションを同じサーバーにします。

-   既定では、セキュリティ上のリスクをさらに削減ワーカー プロセスの一意の id とサンド ボックス化された構成を提供して、アプリケーションの分離を実現します。

-   簡単に追加、削除、およびものユーザーのニーズに適した、カスタムのモジュールで組み込みの IIS コンポーネントを置き換えます。

-   組み込みの動的キャッシュと拡張圧縮を通じて web サイトを短縮します。

Web サーバー (IIS) サーバーの役割を実行しているコンピューターである、WEB1 を展開するには、次の操作を行う必要があります。

-   セクションの手順を実行[すべてのサーバーを構成する](#BKMK_configuringAll)します。

-   セクションの手順を実行[への参加サーバー コンピューターをドメインとログオン](#BKMK_joinlogserver)

-   [Web サーバー (IIS) サーバーの役割をインストールします。](#BKMK_install_IIS)

##### <a name="BKMK_install_IIS"></a>Web サーバー (IIS) サーバーの役割をインストールします。
この手順を完了するには、メンバーである、**管理者**グループ。

> [!NOTE]
> Windows PowerShell を使用してこの手順を実行、PowerShell を開き、次のように入力し、ENTER キーを押します。
>
> `Install-WindowsFeature Web-Server -IncludeManagementTools`

1.  **サーバー マネージャー**、] をクリックして**管理**、] をクリックし、**追加の役割と機能**します。 追加の役割と機能のウィザードが開きます。

2.  **開始する前に**、] をクリックして**次**します。

    > [!NOTE]
    > **開始する前に**以前選択した場合、追加の役割と機能のウィザードのページが表示されない**既定でこのページをスキップ**、追加の役割と機能のウィザードが実行されました。

3.  **[インストールの種類**] ページで [**次**します。

4.  **対象サーバーの選択**] をクリックし、ローカル コンピューターが選択されていることを確認] ページで、**次**します。

5.  **サーバーの役割の選択**] ページでまでスクロールして選択**Web サーバー (IIS)**します。 **Web サーバー (IIS) に必要な機能を追加する**] ダイアログ ボックスが開きます。 をクリックして**機能の追加**、] をクリックし、**次**します。

6.  をクリックして**次**受け入れるまですべて、既定の web サーバーの設定] をクリック**インストール**します。

7.  すべてのインストールが成功すると、いることを確認し、をクリックして**閉じる**します。

## <a name="BKMK_resources"></a>その他のテクニカル リソース
このガイドのテクノロジの詳細については、次のリソースを参照してください。

 Windows Server 2016、Windows Server 2012 R2、および Windows Server 2012 テクニカル ライブラリ リソース

-   [Active Directory ドメイン サービス (AD DS) で Windows Server 2016 の新機能](https://technet.microsoft.com/en-us/library/mt163897.aspx)

-   [Active Directoryドメイン サービスの概要](https://technet.microsoft.com/library/hh831484.aspx)でhttps://technet.microsoft.com/library/hh831484.aspxします。

-   [ドメイン ネーム システム (DNS) の概要](https://technet.microsoft.com/library/hh831667.aspx)でhttps://technet.microsoft.com/library/hh831667.aspxします。

-   [DNS Admins 役割の実装](https://technet.microsoft.com/library/cc756152(WS.10).aspx)

-   [動的ホスト構成プロトコル (DHCP) 概要](https://technet.microsoft.com/library/hh831825.aspx)でhttps://technet.microsoft.com/library/hh831825.aspxします。

-   [ネットワーク ポリシーとアクセス サービスの概要](https://technet.microsoft.com/library/hh831683.aspx)でhttps://technet.microsoft.com/library/hh831683.aspxします。

-   [Web サーバー (IIS) の概要](https://technet.microsoft.com/library/hh831725.aspx)でhttps://technet.microsoft.com/library/hh831725.aspxします。

## <a name="BKMK_appendix"></a>付録 A E から
次のセクションでには、Windows Server 2016、Windows 10、Windows Server 2012、および Windows 8 以外のオペレーティング システムを実行しているコンピューターの追加の構成情報が含まれます。 さらに、展開に役立つネットワーク準備ワークシートが提供されます。

1.  [付録 A - コンピューターの名前を変更します。](#BKMK_A)

2.  [付録 B - 静的 IP の構成](#BKMK_B)

3.  [付録 C - コンピューターをドメインに参加させる](#BKMK_C)

4.  [付録 D - ドメインにログオン](#BKMK_D)

5.  [付録 E - コア ネットワークの計画準備シート](#BKMK_E)

## <a name="BKMK_A"></a>付録 A - コンピューターの名前を変更します。
このセクションの手順を使用するには別のコンピューター名を Windows Server 2008 R2、Windows 7、Windows Server 2008、および Windows Vista を実行しているコンピューターに送信します。

-   [Windows Server 2008 R2 および Windows 7](#bkmk_NetFndtn_Pln_rename_R2)

-   [Windows Server 2008 および Windows Vista](#bkmk_NetFndtn_Pln_Renam08)

### <a name="bkmk_NetFndtn_Pln_rename_R2"></a>Windows Server 2008 R2 および Windows 7
メンバーシップ**管理者**、またはそれと同等がこれらの手順を実行するために必要な最小値。

##### <a name="to-rename-computers-running-windows-server-2008-r2-and-windows-7"></a>Windows Server 2008 R2 および Windows 7 を実行しているコンピューターの名前を変更するには

1.  をクリックして**開始**、右クリック**コンピューター**、] をクリックし、**プロパティ**します。 **システム**] ダイアログ ボックスが開きます。

2.  **コンピューター名、ドメインおよびワークグループの設定**、] をクリックして**設定を変更する**します。 **システム プロパティ**] ダイアログ ボックスが開きます。

    > [!NOTE]
    > コンピューターで実行されている Windows 7、前に、**システム プロパティ**] ダイアログ ボックスが開き、**ユーザー アカウント制御**続行の許可を要求するダイアログ ボックスが開きます。 をクリックして**続行**を続行します。

3.  をクリックして**変更**します。 **コンピューター名/ドメイン名の変更**] ダイアログ ボックスが開きます。

4.  **コンピューター名**、お使いのコンピューターの名前を入力します。 たとえば、DC1 というコンピューター名にする場合は、入力**DC1**します。

5.  をクリックして**[ok]** 2 回クリックして**閉じる**、] をクリックし、**今すぐ再起動**コンピューターを再起動します。

### <a name="bkmk_NetFndtn_Pln_Renam08"></a>Windows Server 2008 および Windows Vista
メンバーシップ**管理者**、またはそれと同等がこれらの手順を実行するために必要な最小値。

##### <a name="to-rename-computers-running-windows-server-2008-and-windows-vista"></a>Windows Server 2008 および Windows Vista を実行しているコンピューターの名前を変更するには

1.  をクリックして**開始**、右クリック**コンピューター**、] をクリックし、**プロパティ**します。 **システム**] ダイアログ ボックスが開きます。

2.  **コンピューター名、ドメインおよびワークグループの設定**、] をクリックして**設定を変更する**します。 **システム プロパティ**] ダイアログ ボックスが開きます。

    > [!NOTE]
    > コンピューターの場合は、前に、Windows Vista を実行して、**システム プロパティ**] ダイアログ ボックスが開き、**ユーザー アカウント制御**続行の許可を要求するダイアログ ボックスが開きます。 をクリックして**続行**を続行します。

3.  をクリックして**変更**します。 **コンピューター名/ドメイン名の変更**] ダイアログ ボックスが開きます。

4.  **コンピューター名**、お使いのコンピューターの名前を入力します。 たとえば、DC1 というコンピューター名にする場合は、入力**DC1**します。

5.  をクリックして**[ok]** 2 回クリックして**閉じる**、] をクリックし、**今すぐ再起動**コンピューターを再起動します。

## <a name="BKMK_B"></a>付録 B - 静的 IP の構成
ここでは、次のオペレーティング システムを実行しているコンピューターで静的 IP アドレスを構成するための手順を説明します。

-   [Windows Server 2008 R2](#bkmk_R2Cng_WS08R2IP)

-   [Windows Server 2008](#bkmk_NetFndtn_Pln_CfgStatic08)

### <a name="bkmk_R2Cng_WS08R2IP"></a>Windows Server 2008 R2
メンバーシップ**管理者**、相当するものでは、この手順を実行するために必要な最低限またはします。

##### <a name="to-configure-a-static-ip-address-on-a-computer-running-windows-server-2008-r2"></a>Windows Server 2008 R2 を実行しているコンピューターで静的 IP アドレスを構成するには

1.  をクリックして**開始**、] をクリックし、**コントロール パネルの [**します。

2.  **コントロール パネルの [**、] をクリックして**ネットワークとインターネット**します。 **ネットワークとインターネット**が開きます。

    **ネットワークとインターネット**、] をクリックして**ネットワークと共有センター**します。 **ネットワークと共有センター**が開きます。

3.  **ネットワークと共有センター**、] をクリックして**アダプター設定を変更する**します。 **ネットワーク接続**が開きます。

4.  **ネットワーク接続**、構成、およびをクリックするネットワーク接続を右クリックして**プロパティ**します。

5.  **ローカル エリア接続のプロパティ**で、**この接続は、次の項目を使用して**[**インターネット プロトコル バージョン 4 (Tcp/ipv4)**、] をクリックし、**プロパティ**します。 **インターネット プロトコル バージョン 4 (Tcp/ipv4) プロパティ**] ダイアログ ボックスが開きます。

6.  **インターネット プロトコル バージョン 4 (Tcp/ipv4) プロパティ**] の [、**全般的な**] タブ、[**次の IP アドレスを使用して**します。 **IP アドレス**、使用する IP アドレスを入力します。

7.  Tab キーで、カーソルを押して**サブネット マスク**します。 サブネット マスクの既定値が自動的に入力します。 既定のサブネット マスクを受け入れるか、使用するサブネット マスクを入力します。

8.  **デフォルト ゲートウェイ**、デフォルト ゲートウェイの IP アドレスを入力します。

9. **優先 DNS サーバー**、DNS サーバーの IP アドレスを入力します。 優先 DNS サーバーにローカル コンピューターを使用する場合は、ローカル コンピューターの IP アドレスを入力します。

10. **代替 DNS サーバー**、存在する場合、代替の DNS サーバーの IP アドレスを入力します。 代替 DNS サーバーにローカル コンピューターを使用する場合は、ローカル コンピューターの IP アドレスを入力します。

11. をクリックして**OK**、] をクリックし、**閉じる**します。

### <a name="bkmk_NetFndtn_Pln_CfgStatic08"></a>Windows Server 2008
メンバーシップ**管理者**、またはそれと同等がこれらの手順を実行するために必要な最小値。

##### <a name="to-configure-a-static-ip-address-on-a-computer-running-windows-server-2008"></a>Windows Server 2008 を実行しているコンピューターで静的 IP アドレスを構成するには

1.  をクリックして**開始**、] をクリックし、**コントロール パネルの [**します。

2.  **コントロール パネルの [**、いることを確認**クラシック表示**が選択されているし、ダブルクリック**ネットワークと共有センター**します。

3.  **ネットワークと共有センター**で、**タスク**、] をクリックして**ネットワーク接続の管理**します。

4.  **ネットワーク接続**、構成、およびをクリックするネットワーク接続を右クリックして**プロパティ**します。

5.  **ローカル エリア接続のプロパティ**で、**この接続は、次の項目を使用して**[**インターネット プロトコル バージョン 4 (Tcp/ipv4)**、] をクリックし、**プロパティ**します。 **インターネット プロトコル バージョン 4 (Tcp/ipv4) プロパティ**] ダイアログ ボックスが開きます。

6.  **インターネット プロトコル バージョン 4 (Tcp/ipv4) プロパティ**] の [、**全般的な**] タブ、[**次の IP アドレスを使用して**します。 **IP アドレス**、使用する IP アドレスを入力します。

7.  Tab キーで、カーソルを押して**サブネット マスク**します。 サブネット マスクの既定値が自動的に入力します。 既定のサブネット マスクを受け入れるか、使用するサブネット マスクを入力します。

8.  **デフォルト ゲートウェイ**、デフォルト ゲートウェイの IP アドレスを入力します。

9. **優先 DNS サーバー**、DNS サーバーの IP アドレスを入力します。 優先 DNS サーバーにローカル コンピューターを使用する場合は、ローカル コンピューターの IP アドレスを入力します。

10. **代替 DNS サーバー**、存在する場合、代替の DNS サーバーの IP アドレスを入力します。 代替 DNS サーバーにローカル コンピューターを使用する場合は、ローカル コンピューターの IP アドレスを入力します。

11. をクリックして**OK**、] をクリックし、**閉じる**します。

## <a name="BKMK_C"></a>付録 C - コンピューターをドメインに参加させる
これらの手順を使用して、ドメインに Windows Server 2008 R2、Windows 7、Windows Server 2008、および Windows Vista を実行しているコンピューターを参加させることができます。

-   [Windows Server 2008 R2 および Windows 7](#BKMK_c1)

-   [Windows Server 2008 および Windows Vista](#BKMK_c2)

> [!IMPORTANT]
> コンピューターをドメインに参加させるにする必要があります。 またはログに記録ローカルの Administrator アカウントでコンピュータに場合は、ローカル コンピューター管理者の資格情報がないユーザー アカウントでコンピュータにログオンしている必要があります、資格情報を提供、ローカル Administrator アカウントのコンピューターをドメインに参加させるプロセス中にします。 さらに、コンピューターを参加するドメイン ユーザー アカウントが必要です。 コンピューターをドメインに参加させるプロセス中に、ドメイン アカウントの資格情報 (ユーザー名とパスワード) のように求められます。

### <a name="BKMK_c1"></a>Windows Server 2008 R2 および Windows 7
メンバーシップ**Domain Users**、相当するものでは、この手順を実行するために必要な最低限またはします。

##### <a name="to-join-computers-running-windows-server-2008-r2-and-windows-7-to-the-domain"></a>ドメインに Windows Server 2008 R2 および Windows 7 を実行しているコンピューターを参加させる

1.  ローカルの Administrator アカウントを使用してコンピューターにログオンします。

2.  をクリックして**開始**、右クリック**コンピューター**、] をクリックし、**プロパティ**します。 **システム**] ダイアログ ボックスが開きます。

3.  **コンピューター名、ドメインおよびワークグループの設定**、] をクリックして**設定を変更する**します。 **システム プロパティ**] ダイアログ ボックスが開きます。

    > [!NOTE]
    > コンピューターで実行されている Windows 7、前に、**システム プロパティ**] ダイアログ ボックスが開き、**ユーザー アカウント制御**続行の許可を要求するダイアログ ボックスが開きます。 をクリックして**続行**を続行します。

4.  をクリックして**変更**します。 **コンピューター名/ドメイン名の変更**] ダイアログ ボックスが開きます。

5.  **コンピューター名**で、**のメンバー**[**ドメイン**、参加するドメインの名前を入力します。 たとえば、ドメイン名が corp.contoso.com の場合は、入力**corp.contoso.com**します。

6.  をクリックして**OK**します。 **Windows セキュリティ**] ダイアログ ボックスが開きます。

7.  **コンピューター名/ドメイン名の変更**で、**ユーザー名**、ユーザー名を入力し、[**パスワード**パスワードを入力し、[クリックして**OK**します。 **コンピューター名/ドメイン名の変更**をドメインにそれを通知するダイアログ ボックスが開きます。 をクリックして**OK**します。

8.  **コンピューター名/ドメイン名の変更**] ダイアログ ボックスで、変更を適用するコンピューターを再起動する必要があることを示すメッセージが表示されます。 をクリックして**OK**します。

9. **システムのプロパティ**] ダイアログ ボックスで、**コンピューター名**] タブ、[**閉じる**します。 **Microsoft Windows** ] ダイアログ ボックスが開き、もう一度変更を適用するコンピューターを再起動する必要があることを示すメッセージが表示されます。 をクリックして**今すぐ再起動**します。

### <a name="BKMK_c2"></a>Windows Server 2008 および Windows Vista
メンバーシップ**Domain Users**、相当するものでは、この手順を実行するために必要な最低限またはします。

##### <a name="to-join-computers-running-windows-server-2008-and-windows-vista-to-the-domain"></a>ドメインに Windows Server 2008 および Windows Vista を実行しているコンピューターを参加させる

1.  ローカルの Administrator アカウントを使用してコンピューターにログオンします。

2.  をクリックして**開始**、右クリック**コンピューター**、] をクリックし、**プロパティ**します。 **システム**] ダイアログ ボックスが開きます。

3.  **コンピューター名、ドメインおよびワークグループの設定**、] をクリックして**設定を変更する**します。 **システム プロパティ**] ダイアログ ボックスが開きます。

4.  をクリックして**変更**します。 **コンピューター名/ドメイン名の変更**] ダイアログ ボックスが開きます。

5.  **コンピューター名**で、**のメンバー**[**ドメイン**、参加するドメインの名前を入力します。 たとえば、ドメイン名が corp.contoso.com の場合は、入力**corp.contoso.com**します。

6.  をクリックして**OK**します。 **Windows セキュリティ**] ダイアログ ボックスが開きます。

7.  **コンピューター名/ドメイン名の変更**で、**ユーザー名**、ユーザー名を入力し、[**パスワード**パスワードを入力し、[クリックして**OK**します。 **コンピューター名/ドメイン名の変更**をドメインにそれを通知するダイアログ ボックスが開きます。 をクリックして**OK**します。

8.  **コンピューター名/ドメイン名の変更**] ダイアログ ボックスで、変更を適用するコンピューターを再起動する必要があることを示すメッセージが表示されます。 をクリックして**OK**します。

9. **システムのプロパティ**] ダイアログ ボックスで、**コンピューター名**] タブ、[**閉じる**します。 **Microsoft Windows** ] ダイアログ ボックスが開き、もう一度変更を適用するコンピューターを再起動する必要があることを示すメッセージが表示されます。 をクリックして**今すぐ再起動**します。

## <a name="BKMK_D"></a>付録 D - ドメインにログオン
これらの手順を使用すると、Windows Server 2008 R2、Windows 7、Windows Server 2008、および Windows Vista を実行しているコンピューターを使用してドメインにログオンします。

-   [Windows Server 2008 R2 および Windows 7](#BKMK_d1)

-   [Windows Server 2008 および Windows Vista](#BKMK_d2)

### <a name="BKMK_d1"></a>Windows Server 2008 R2 および Windows 7
メンバーシップ**Domain Users**、相当するものでは、この手順を実行するために必要な最低限またはします。

##### <a name="log-on-to-the-domain-using-computers-running-windows-server-2008-r2-and-windows-7"></a>Windows Server 2008 R2 および Windows 7 を実行しているコンピューターを使用してドメインにログオンします。

1.  ログオフ、またはコンピューターを再起動します。

2.  Ctrl キーを押しながら ALT + 削除します。 ログオン画面が表示されます。

3.  をクリックして**ユーザーの切り替え**、] をクリックし、**その他のユーザー**します。

4.  **ユーザー名**、形式で、ドメインとユーザー名を入力*domain \user*します。 たとえば、という名前のアカウントで corp.contoso.com ドメインにログオンする**- 01 ユーザー**、種類**\user-01**します。

5.  **パスワード**、ドメイン パスワードを入力し、矢印をクリックしてまたは ENTER キーを押します。

### <a name="BKMK_d2"></a>Windows Server 2008 および Windows Vista
メンバーシップ**Domain Users**、相当するものでは、この手順を実行するために必要な最低限またはします。

##### <a name="log-on-to-the-domain-using-computers-running-windows-server-2008-and-windows-vista"></a>Windows Server 2008 および Windows Vista を実行しているコンピューターを使用してドメインにログオンします。

1.  ログオフ、またはコンピューターを再起動します。

2.  Ctrl キーを押しながら ALT + 削除します。 ログオン画面が表示されます。

3.  をクリックして**ユーザーの切り替え**、] をクリックし、**その他のユーザー**します。

4.  **ユーザー名**、形式で、ドメインとユーザー名を入力*domain \user*します。 たとえば、という名前のアカウントで corp.contoso.com ドメインにログオンする**- 01 ユーザー**、種類**\user-01**します。

5.  **パスワード**、ドメイン パスワードを入力し、矢印をクリックしてまたは ENTER キーを押します。

## <a name="BKMK_E"></a>付録 E - コア ネットワークの計画準備シート
コア ネットワークのインストールに必要な情報を収集するためにこのネットワークの計画準備シートを使用することができます。 このトピックでは、する必要がありますを提供する情報または特定の値のインストールまたは構成プロセス中に各サーバー コンピューターの個々 の構成項目を含むテーブルを示します。 各構成項目の値の例を示します。

計画および追跡の目的で、スペースを展開に使用される値を入力するための各表に示します。 これらのテーブル内のセキュリティ関連の値を記録する場合は、安全な場所に情報を格納する必要があります。

セクションでは、このトピックで構成項目と、このガイドで説明されている展開の手順に関連付けられている値の例を提供するに、次のリンクがあります。

1.  [Active Directory Domain Services および DNS をインストールします。](#BKMK_FndtnPrep_InstallAD)

    -   [DNS 逆引き参照ゾーンを構成します。](#BKMK_FndtnPrep_DNSRevrsLook)

2.  [DHCP をインストールします。](#BKMK_FndtnPrep_InstallDHCP)

    -   [DHCP で除外範囲を作成します。](#BKMK_FndtnPrep_DHCP_Exclusn)

    -   [新しい DHCP スコープを作成します。](#bkmk_NetFndtn_Pln_DHCP_NewScope)

3.  [ネットワーク ポリシー サーバー (省略可能) をインストールします。](#BKMK_FndtnPrep_InstallNPS)

### <a name="BKMK_FndtnPrep_InstallAD"></a>Active Directory Domain Services および DNS をインストールします。
このセクションの表は、事前インストールおよび Active Directory ドメイン サービス (AD DS) のインストールの構成項目の一覧と DNS します。

##### <a name="pre-installation-configuration-items-for-ad-ds-and-dns"></a>AD DS および DNS の事前インストール構成項目
以下の表で説明するように事前インストール構成項目のリスト[すべてのサーバーを構成する](#BKMK_configuringAll):

-   [静的 IP アドレスを構成します。](#BKMK_ip)

|構成項目|値の例|値|
|-----------------------|------------------|----------|
|IP アドレス|10.0.0.2||
|サブネット マスク|255.255.255.0||
|既定のゲートウェイ|10.0.0.1||
|優先 DNS サーバー|127.0.0.1||
|代替 DNS サーバー|10.0.0.15||

-   [コンピューターを名前します。](#BKMK_rename)

|構成項目|値の例|値|
|----------------------|-----------------|---------|
|コンピューター名|DC1||

##### <a name="ad-ds-and-dns-installation-configuration-items"></a>AD DS および DNS インストール構成項目
Windows Server コア ネットワークの展開手順の構成項目の[インストール AD DS および DNS を新しいフォレスト](#BKMK_installAD-DNS):

|構成項目|値の例|値|
|-----------------------|------------------|----------|
|完全 DNS 名|corp.contoso.com||
|フォレストの機能レベル|Windows Server 2003||
|Active Directory Domain Services データベース フォルダーの場所|E:\Configuration\\<br /><br />または、既定の場所をそのまま使用します。||
|Active Directory ドメイン サービスのログ ファイル フォルダーの場所|E:\Configuration\\<br /><br />または、既定の場所をそのまま使用します。||
|Active Directory Domain Services SYSVOL フォルダーの場所|E:\Configuration\\<br /><br />既定の場所を受け入れるか||
|ディレクトリ復元モード Administrator パスワード|J * p2leO4$ F||
|応答ファイルの名前 (省略可能)|AD DS_AnswerFile||

#### <a name="BKMK_FndtnPrep_DNSRevrsLook"></a>DNS 逆引き参照ゾーンを構成します。

|構成項目|値の例|値|
|-----------------------|------------------|----------|
|ゾーンの種類:|-プライマリ ゾーン<br />-セカンダリ ゾーン<br />スタブ ゾーン||
|ゾーンの種類<br /><br />**Active Directory にゾーンを格納します。**|選択されています。<br />-選択されていない||
|Active Directory ゾーン レプリケーション スコープ|このフォレスト内のすべての DNS サーバーに<br />このドメイン内のすべての DNS サーバーに<br />このドメイン内のすべてのドメイン コント ローラーになります。<br />このディレクトリ パーティションの範囲で指定されたすべてのドメイン コント ローラーになります。||
|逆引き参照ゾーン名<br /><br />(IP の種類)|-IPv4 逆引き参照ゾーン<br />IPv6 の逆引き参照ゾーン||
|逆引き参照ゾーン名<br /><br />(ネットワーク ID)|10.0.0||

### <a name="BKMK_FndtnPrep_InstallDHCP"></a>DHCP をインストールします。
このセクションの表は、事前インストールと DHCP のインストールの構成項目を一覧表示します。

##### <a name="pre-installation-configuration-items-for-dhcp"></a>DHCP の事前インストール構成項目
以下の表で説明するように事前インストール構成項目のリスト[すべてのサーバーを構成する](#BKMK_configuringAll):

-   [静的 IP アドレスを構成します。](#BKMK_ip)

|構成項目|値の例|値|
|-----------------------|------------------|----------|
|IP アドレス|10.0.0.3||
|サブネット マスク|255.255.255.0||
|既定のゲートウェイ|10.0.0.1||
|優先 DNS サーバー|10.0.0.2||
|代替 DNS サーバー|10.0.0.15||

-   [コンピューターを名前します。](#BKMK_rename)

|構成項目|値の例|値|
|----------------------|-----------------|---------|
|コンピューター名|DHCP1||

##### <a name="dhcp-installation-configuration-items"></a>DHCP インストール構成項目
Windows Server コア ネットワークの展開手順の構成項目の[インストール動的ホスト構成プロトコル (DHCP)](#BKMK_installDHCP):

|構成項目|値の例|値|
|-----------------------|------------------|----------|
|ネットワーク接続バインディング|イーサネット||
|DNS サーバーの設定|DC1||
|優先 DNS サーバーの IP アドレス|10.0.0.2||
|代替 DNS サーバーの IP アドレス|10.0.0.15||
|スコープ名|Corp1||
|開始 IP アドレス|10.0.0.1||
|終了 IP アドレス|10.0.0.254||
|サブネット マスク|255.255.255.0||
|デフォルト ゲートウェイ (省略可能)|10.0.0.1||
|リース期間|8 日||
|IPv6 DHCP サーバーの操作モード|有効になっていません||

#### <a name="BKMK_FndtnPrep_DHCP_Exclusn"></a>DHCP で除外範囲を作成します。
Dhcp スコープの作成時に除外範囲を作成する構成項目。

|構成項目|値の例|値|
|-----------------------|------------------|----------|
|スコープ名|Corp1||
|スコープの説明|メイン オフィス サブネット 1||
|除外範囲の開始 IP アドレス|10.0.0.1||
|除外範囲の終了 IP アドレス|10.0.0.15||

#### <a name="bkmk_NetFndtn_Pln_DHCP_NewScope"></a>新しい DHCP スコープを作成します。
Windows Server コア ネットワークの展開手順の構成項目の[を作成して、新しい DHCP スコープをアクティブ化](#BKMK_newscopeDHCP):

|構成項目|値の例|値|
|-----------------------|------------------|----------|
|新しいスコープ名|Corp2||
|スコープの説明|メイン オフィス サブネット 2||
|(IP アドレスの範囲)<br /><br />[開始 IP アドレス|10.0.1.1||
|(IP アドレスの範囲)<br /><br />終了 IP アドレス|10.0.1.254||
|長さ|8||
|サブネット マスク|255.255.255.0||
|(除外範囲)[開始 IP アドレス|10.0.1.1||
|除外範囲の終了 IP アドレス|10.0.1.15||
|リース期間<br /><br />日<br /><br />時間<br /><br />分|-   8<br />-   0<br />-   0||
|ルーター (デフォルト ゲートウェイ)<br /><br />IP アドレス|10.0.1.1||
|DNS 親ドメイン|corp.contoso.com||
|DNS サーバー<br /><br />IP アドレス|10.0.0.2||

### <a name="BKMK_FndtnPrep_InstallNPS"></a>ネットワーク ポリシー サーバー (省略可能) をインストールします。
このセクションの表は、事前インストールし、NPS のインストールの構成項目を一覧表示します。

##### <a name="pre-installation-configuration-items"></a>事前インストール構成項目
次の 3 つの表で説明するように事前インストール構成項目を一覧表示[すべてのサーバーを構成する](#BKMK_configuringAll):

-   [静的 IP アドレスを構成します。](#BKMK_ip)

|構成項目|値の例|値|
|-----------------------|------------------|----------|
|IP アドレス|10.0.0.4||
|サブネット マスク|255.255.255.0||
|既定のゲートウェイ|10.0.0.1||
|優先 DNS サーバー|10.0.0.2||
|代替 DNS サーバー|10.0.0.15||

-   [コンピューターを名前します。](#BKMK_rename)

|構成項目|値の例|値|
|----------------------|-----------------|---------|
|コンピューター名|NPS1||

##### <a name="network-policy-server-installation-configuration-items"></a>ネットワーク ポリシー サーバーのインストール構成項目
Windows Server コア ネットワークの NPS の展開手順の構成項目の[インストール ネットワーク ポリシー サーバー (NPS)](#BKMK_installNPS)と[既定のドメインに NPS サーバーを登録](#BKMK_registerNPS)します。

-   インストールし、NPS を登録するのには、追加の構成項目は必要ありません。

