---
title: ネットワーク ポリシーを構成する
description: このトピックでは、Windows Server 2016 でネットワーク ポリシー サーバーのネットワーク ポリシーの構成の概要を示します。
manager: brianlic
ms.prod: windows-server-threshold
ms.technology: networking
ms.topic: article
ms.assetid: fe77655a-e2be-4949-92e1-aaaa215d86ea
ms.author: pashort
author: shortpatti
ms.openlocfilehash: e8a03a6c67ddd59549cefc9742ca92e0702f92a7
ms.sourcegitcommit: 0d0b32c8986ba7db9536e0b8648d4ddf9b03e452
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 04/17/2019
ms.locfileid: "59842003"
---
# <a name="configure-network-policies"></a>ネットワーク ポリシーを構成する

>適用対象:Windows Server 2016 の Windows Server (半期チャネル)

このトピックを使用すると、NPS でネットワーク ポリシーを構成します。

## <a name="add-a-network-policy"></a>ネットワーク ポリシーを追加します。

ネットワーク ポリシー サーバー \(NPS\)ネットワーク ポリシーと、ネットワークに接続する接続要求が承認されているかどうか判断するユーザー アカウントのダイヤルイン プロパティを使用します。

NPS コンソールまたはリモート アクセス コンソールのいずれかで新しいネットワーク ポリシーを構成するのには、この手順を使用することができます。

### <a name="performing-authorization"></a>承認を実行します。

NPS が接続要求の承認を実行すると、以降では最初のポリシーと構成されているポリシーの一覧を下に移動し、ポリシーの順序付きリストでは、各ネットワーク ポリシーで要求を比較します。 NPS には、条件が接続要求を一致するポリシーが検出されると、NPS は承認を実行するのにポリシーと一致して、ユーザー アカウントのダイヤルイン プロパティを使用します。 ネットワーク ポリシーでアクセスまたは制御のアクセスを許可するユーザー アカウントのダイヤルイン プロパティが構成されている接続要求が承認されている場合は、NPS に接続するネットワーク ポリシーで構成されている設定が適用されます。

NPS に接続要求と一致するネットワーク ポリシーが見つからない場合は、アクセスを許可するユーザー アカウントのダイヤルイン プロパティが設定されている接続要求が拒否されます。

ユーザー アカウントのダイヤルイン プロパティがアクセスを拒否する設定されている場合は、接続要求が NPS によって拒否されます。

### <a name="key-settings"></a>キーの設定

新しいネットワーク ポリシー ウィザードを使用して、ネットワーク ポリシーで指定した値を作成するときに**ネットワーク接続方法**を自動的に構成するために使用、**ポリシーの種類**条件。 

- 未指定の既定値を保持する場合は、任意の種類のネットワーク アクセス サーバー (NAS) を使用しているすべてのネットワーク接続の種類の NPS で作成したネットワーク ポリシーが評価されます。
- ネットワークの接続方法を指定する場合、NPS は、指定したネットワーク アクセス サーバーの種類から接続要求が送信された場合にのみネットワーク ポリシーを評価します。

**アクセス許可**ページで、選択する必要があります**アクセス権が付与**する場合は、ネットワークに接続するユーザーを許可するポリシー。 場合は、ポリシーをユーザーが、ネットワークに接続することを防ぐために**アクセスが拒否されました**します。 

ユーザー アカウント ダイヤルイン プロパティでは、Active Directory で決定へのアクセス許可が必要な場合&reg;Domain Services \(AD DS\)、選択することができます、**アクセスはユーザー ダイヤルイン プロパティによって決まります**チェック ボックスをオンします。

この手順を完了するには、少なくとも **Domain Admins** グループ、またはそれと同等のメンバーシップが必要です。

### <a name="to-add-a-network-policy"></a>ネットワーク ポリシーを追加するには 

1. NPS コンソールを開きをダブルクリック**ポリシー**します。

2. コンソール ツリーで、右クリック**ネットワーク ポリシー**、 をクリック**新規**します。 新しいネットワーク ポリシー ウィザードが開きます。

3. ポリシーを作成するのにには、新しいネットワーク ポリシー ウィザードを使用します。

## <a name="create-network-policies-for-dial-up-or-vpn-with-a-wizard"></a>ダイヤルアップまたは VPN with a Wizard のネットワーク ポリシーを作成します。

この手順を使用するには、接続要求ポリシーを作成し、ネットワークのダイヤルアップ サーバーまたは仮想プライベート ネットワークのいずれかの展開に必要なポリシー \(VPN\)サーバーをリモート認証ダイヤルイン ユーザー サービスとして\(RADIUS\)を NPS RADIUS サーバーのクライアント。

>[!NOTE]
>ラップトップ コンピューターや、クライアント オペレーティング システムを実行している他のコンピューターなどのクライアント コンピュータが RADIUS クライアントではありません。 RADIUS クライアントはネットワーク アクセス サーバー、ワイヤレス アクセス ポイント、802.1 X 認証スイッチ、仮想プライベート ネットワークなど\(VPN\)サーバー、およびダイアル アップ サーバー: これらのデバイスの通信に RADIUS プロトコルを使用するため、NPSs などの RADIUS サーバー。

この手順では、NPS で新しいダイヤルアップまたは仮想プライベート ネットワーク接続ウィザードを開く方法について説明します。

ウィザードを実行した後、次のポリシーが作成されます。

- 1 つの接続要求ポリシー
- 1 つのネットワーク ポリシー

ダイヤルアップ サーバーと VPN サーバー用の新しいポリシーを作成する必要があるたびに、新しいダイヤルアップまたは仮想プライベート ネットワーク接続ウィザードを実行できます。

新しいダイヤルアップまたは仮想プライベート ネットワーク接続ウィザードを実行することは、NPS を RADIUS クライアントとして、ダイヤルアップまたは VPN サーバーをデプロイするために必要な唯一の手順ではありません。 両方のネットワーク アクセス メソッドは、追加のハードウェアおよびソフトウェア コンポーネントをデプロイすることが必要です。

この手順を完了するには、少なくとも **Domain Admins** グループ、またはそれと同等のメンバーシップが必要です。

### <a name="to-create-policies-for-dial-up-or-vpn-with-a-wizard"></a>ウィザードを使用してダイヤルアップまたは VPN 用のポリシーを作成するには

1. NPS コンソールを開きます。 選択されていない場合はクリックして**NPS\(ローカル\)** します。 NPS をリモートでポリシーを作成する場合は、サーバーを選択します。

2. **Getting Started**と**標準構成**を選択します**ダイヤルアップまたは VPN 接続用の RADIUS サーバー**します。 テキストと、選択内容を反映するようにテキストの変更の下のリンク。

3. クリックして**VPN の構成またはウィザードを使用してダイヤルアップ**します。 新しいダイヤルアップまたは仮想プライベート ネットワーク接続ウィザードが開きます。

4. 新しいポリシーの作成の完了には、ウィザードの指示に従います。

## <a name="create-network-policies-for-8021x-wired-or-wireless-with-a-wizard"></a>X ワイヤード (有線) またはウィザードを使用してワイヤレス 802.1 用のネットワーク ポリシーを作成します。

この手順を使用するには、接続要求ポリシーと、NPS をリモート認証ダイヤルイン ユーザー サービス (RADIUS) クライアントとして 802.1 X 認証スイッチまたは 802.1 X ワイヤレス アクセス ポイントのいずれかの展開に必要なネットワーク ポリシーを作成するにはRADIUS サーバー。

この手順では、NPS で、新しい IEEE 802.1 X ワイヤードおよびワイヤレス接続ウィザードを起動する方法について説明します。

ウィザードを実行した後、次のポリシーが作成されます。

- 1 つの接続要求ポリシー
- 1 つのネットワーク ポリシー

802.1 X アクセス用の新しいポリシーを作成する必要があるたびに、新しい IEEE 802.1 X ワイヤードおよびワイヤレス接続ウィザードを実行することができます。

新しい IEEE 802.1 X ワイヤードおよびワイヤレス接続ウィザードを実行することは、802.1 X スイッチやワイヤレス アクセス ポイントを NPS を RADIUS クライアントとして認証をデプロイするために必要な唯一の手順ではありません。 両方のネットワーク アクセス メソッドは、追加のハードウェアおよびソフトウェア コンポーネントをデプロイすることが必要です。

この手順を完了するには、少なくとも **Domain Admins** グループ、またはそれと同等のメンバーシップが必要です。

### <a name="to-create-policies-for-8021x-wired-or-wireless-with-a-wizard"></a>ウィザードを使用してワイヤード (有線) またはワイヤレス 802.1 X のポリシーを作成するには

1. NPS では、サーバー マネージャーで、をクリックして**ツール**、 をクリックし、**ネットワーク ポリシー サーバー**します。 NPS コンソールが開きます。 

2. 選択されていない場合はクリックして**NPS\(ローカル\)** します。 NPS をリモートでポリシーを作成する場合は、サーバーを選択します。

3. **Getting Started**と**標準構成**を選択します **「RADIUS server for 802.1 X Wireless or Wired Connections**します。 テキストと、選択内容を反映するようにテキストの変更の下のリンク。

4. クリックして**ウィザードを使用して X 構成 802.1**します。 新しい IEEE 802.1 X ワイヤードおよびワイヤレス接続ウィザードが開きます。

5. 新しいポリシーの作成の完了には、ウィザードの指示に従います。

## <a name="configure-nps-to-ignore-user-account-dial-in-properties"></a>ユーザー アカウントのダイヤルイン プロパティを無視する NPS を構成します。

承認プロセス中に Active Directory 内のユーザー アカウントのダイヤルイン プロパティを無視するのに NPS ネットワーク ポリシーを構成するのにには、この手順を使用します。 Active Directory ユーザーとコンピューターのユーザー アカウントがある場合を除き、NPS が承認プロセス中に評価されるダイヤルイン プロパティ、**ネットワーク アクセス許可**ユーザー アカウントのプロパティに設定されて**コントロールNPS ネットワーク ポリシーでアクセス**します。 

2 つの状況で Active Directory ユーザー アカウントのダイヤルイン プロパティを無視するように NPS を構成する場合があります。

- 場合、ネットワーク ポリシーを使用して NPS の認証を簡略化する必要も、すべてのユーザー アカウントが、**ネットワーク アクセス許可**プロパティに設定**NPS ネットワーク ポリシーでのアクセスを制御**します。 たとえば、一部のユーザー アカウントがある、**ネットワーク アクセス許可**ユーザー アカウントのプロパティに設定**アクセスを拒否する**または**アクセスを許可する**します。

- ときにユーザー アカウントの他のダイヤルイン プロパティでは、ネットワーク ポリシーで構成されている接続の種類に適用できません。 以外のプロパティなど、**ネットワーク アクセス許可**設定はダイヤルインまたは VPN 接続の場合にのみ適用できますが、作成するネットワーク ポリシーでは、ワイヤレスまたは認証スイッチ接続します。

ユーザー アカウントのダイヤルイン プロパティを無視するのにように NPS を構成するのには、この手順を使用することができます。 NPS がネットワークにアクセスするのにユーザーまたはコンピューターを承認するかどうかを判断するのにユーザー アカウントのダイヤルイン プロパティを使用しません接続要求には、このチェック ボックスが選択されているネットワーク ポリシーが一致すると、ネットワーク ポリシーの設定のみが、承認の判断に使用されます。

メンバーシップ **管理者**, 、または同等の権限は、この手順を実行するために必要な最小値。

1. NPS では、サーバー マネージャーで、をクリックして**ツール**、 をクリックし、**ネットワーク ポリシー サーバー**します。 NPS コンソールが開きます。

2. ダブルクリック**ポリシー**、 をクリックして**ネットワーク ポリシー**、詳細ウィンドウで構成するポリシーをダブルクリックします。

3. ポリシーで**プロパティ**] ダイアログ ボックスの [、**概要**] タブの [**アクセス許可**を選択、**ユーザー アカウント ダイヤルイン プロパティを無視する**チェック ボックスをオンにして**OK**します。

### <a name="to-configure-nps-to-ignore-user-account-dial-in-properties"></a>ユーザー アカウントのダイヤルイン プロパティを無視するように NPS を構成するには



## <a name="configure-nps-for-vlans"></a>Vlan の NPS を構成します。

Windows Server 2016 では、VLAN 対応のネットワーク アクセス サーバーと NPS を使用して、そのセキュリティ アクセス許可の適切なネットワーク リソースにのみアクセス権を持つユーザーのグループを行うことができます。 たとえば、行うことができます訪問者ワイヤレス アクセスをインターネットに組織のネットワークにアクセスを許可することがなく。 

また、Vlan、物理的に異なる場所または別の物理的なサブネットに存在するネットワーク リソースを論理的にグループにできます。 たとえば、営業部およびクライアント コンピューター、サーバー、およびプリンターなどのネットワーク リソースのメンバーは自分の組織に複数のさまざまなビル内に配置する場合がありますが、同じ ip アドレスを使用する 1 つの VLAN 上のすべてのリソースを配置することができます。アドレスの範囲。 VLAN、1 つのサブネットとして、エンドユーザーの観点からの関数。

さまざまなユーザー グループ間のネットワークを分離する場合に、Vlan を使用することもできます。 グループを定義する方法を確認した後は、Active Directory ユーザーとコンピューター スナップインでセキュリティ グループを作成し、グループにメンバーを追加します。

### <a name="configure-a-network-policy-for-vlans"></a>Vlan のネットワーク ポリシーを構成します。

この手順を使用すると、その VLAN にユーザーを割り当てるネットワーク ポリシーを構成します。 ルーター、スイッチ、およびアクセス コント ローラーなど、VLAN 対応のネットワーク ハードウェアを使用する場合は、特定の Active Directory グループのメンバーを特定の Vlan に配置するアクセス サーバーに指示するネットワーク ポリシーを構成できます。 Vlan で論理的にグループのネットワーク リソースには、この機能は、設計とネットワーク ソリューションを実装するときに柔軟性を提供します。

Vlan を使用するための NPS ネットワーク ポリシーの設定を構成すると、属性を構成する必要があります**Tunnel-type Medium**、**トンネル Pvt グループ ID**、**トンネルの種類の**、および**Tunnel-tag**します。 

この手順は; をガイドラインとして提供されます。ネットワーク構成では、次のような異なる設定を必要があります。

メンバーシップ **管理者**, 、または同等の権限は、この手順を実行するために必要な最小値。

### <a name="to-configure-a-network-policy-for-vlans"></a>Vlan のネットワーク ポリシーを構成するには

1. NPS では、サーバー マネージャーで、をクリックして**ツール**、 をクリックし、**ネットワーク ポリシー サーバー**します。 NPS コンソールが開きます。

2. ダブルクリック**ポリシー**、 をクリックして**ネットワーク ポリシー**、詳細ウィンドウで構成するポリシーをダブルクリックします。

3. ポリシーで**プロパティ**ダイアログ ボックスで、をクリックして、**設定**タブ。

4. ポリシーで**プロパティ**の**設定**の**RADIUS 属性**、いることを確認**標準**が選択されています。

5. 詳細ペインでの**属性**、**サービス型**の既定値を持つ属性が構成されている**フレーム**します。 既定の VPN とダイヤルアップ、アクセスの方法でのポリシーでは、**フレーム プロトコル**の値を持つ属性が構成されている**PPP**します。 Vlan に必要な追加の接続属性を指定するには、次のようにクリックします。**追加**します。 **RADIUS 標準属性の追加** ダイアログ ボックスが表示されます。

6. **RADIUS 標準属性の追加**属性でスクロールして、次の属性を追加します。

    - **トンネルの種類 Medium**します。 ポリシーを作成する前の選択項目に適切な値を選択します。 たとえば、構成しているネットワーク ポリシーがワイヤレス ポリシーの場合は、選択**値。802 (Includes all 802 media plus Ethernet canonical 形式)** します。

    - **トンネル Pvt グループ ID**します。 グループのメンバーの割り当てられた VLAN 番号を表す整数を入力します。 

    - **トンネルの種類**します。 選択**仮想 Lan (VLAN)** します。


7. **RADIUS 標準属性の追加**、 をクリックして**閉じる**します。 

8. ネットワーク アクセス サーバー (NAS) の使用を必要とする場合、 **Tunnel-tag**属性を追加する、次の手順を使用して、 **Tunnel-tag**属性、ネットワーク ポリシーをします。 NAS のドキュメントにこの属性がについて説明しない場合、ポリシーに追加しないでください。 必要な場合は、ように属性を追加します。

    - ポリシーで**プロパティ**の**設定**の**RADIUS 属性**、 をクリックして**ベンダー固有**します。 

    - 詳細ウィンドウで次のようにクリックします。**追加**します。 **ベンダー固有属性の追加** ダイアログ ボックスが表示されます。

    - **属性**下へスクロールして、選択**Tunnel-tag**、 をクリックし、**追加**します。 **属性情報** ダイアログ ボックスが表示されます。 

    - **属性値**ハードウェアのマニュアルから取得した値を入力します。

## <a name="configure-the-eap-payload-size"></a>EAP のペイロードのサイズを構成します。

場合、ルーターまたはファイアウォールの一部では、パケットを削除し、断片化を必要とするパケットを破棄するように構成されています。

拡張認証プロトコルを使用するネットワーク ポリシーで NPS を展開するときに\(EAP\) with Transport Layer Security \(TLS\)、または既定の最大値の認証方法として、EAP-TLS転送単位\(MTU\) 1,500 バイトは、NPS が EAP ペイロードを使用します。 

EAP ペイロードのこの最大サイズは、ルーターまたは、NPS および RADIUS クライアントの間にファイアウォールによって断片化を必要とする RADIUS メッセージを作成できます。 大文字と小文字の場合は、ルーターまたはファイアウォールが RADIUS クライアントと NPS 間に配置が警告なしに破棄いくつかのフラグメントは、その結果、認証エラーが発生し、アクセス クライアントのネットワークに接続できません。

1344 を超える値をネットワーク ポリシーでフレーム MTU 属性を調整することによって、NPS が EAP ペイロードを使用する最大サイズを小さくには、次の手順を使用します。

メンバーシップ **管理者**, 、または同等の権限は、この手順を実行するために必要な最小値。

### <a name="to-configure-the-framed-mtu-attribute"></a>フレーム MTU 属性を構成するには

1. NPS では、サーバー マネージャーで、をクリックして**ツール**、 をクリックし、**ネットワーク ポリシー サーバー**します。 NPS コンソールが開きます。
 
2. ダブルクリック**ポリシー**、 をクリックして**ネットワーク ポリシー**、詳細ウィンドウで構成するポリシーをダブルクリックします。

3. ポリシーで**プロパティ**ダイアログ ボックスで、をクリックして、**設定**タブ。

4. **設定**の**RADIUS 属性**、 をクリックして**標準**します。 詳細ウィンドウで次のようにクリックします。**追加**します。 **RADIUS 標準属性の追加** ダイアログ ボックスが表示されます。

5. **属性**下へスクロールして をクリックして**フレーム MTU**、 をクリックし、**追加**します。 **属性情報** ダイアログ ボックスが表示されます。

6. **属性値**、以下の値を入力よりも**1344**します。 をクリックして**OK**、 をクリックして**閉じる**、順にクリックします**OK**。



ネットワーク ポリシーの詳細については、次を参照してください。[ネットワーク ポリシー](nps-np-overview.md)します。

指定するパターン マッチング構文の例のネットワーク ポリシー属性を参照してください。 [NPS で正規表現を使用して](nps-crp-reg-expressions.md)します。

NPS の詳細については、次を参照してください。[ネットワーク ポリシー サーバー (NPS)](nps-top.md)します。
