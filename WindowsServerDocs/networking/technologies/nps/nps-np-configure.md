---
title: ネットワーク ポリシーを構成します。
description: このトピックでは、Windows Server 2016 でネットワーク ポリシー サーバーのネットワーク ポリシーの構成の概要を説明します。
manager: brianlic
ms.prod: windows-server-threshold
ms.technology: networking
ms.topic: article
ms.assetid: fe77655a-e2be-4949-92e1-aaaa215d86ea
ms.author: pashort
author: shortpatti
ms.openlocfilehash: 9abcd9343f10100884f96d17d82704382e2af760
ms.sourcegitcommit: 19d9da87d87c9eefbca7a3443d2b1df486b0b010
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 03/28/2018
---
# <a name="configure-network-policies"></a>ネットワーク ポリシーを構成します。

>適用対象: Windows Server (半期チャネル)、Windows Server 2016

このトピックを使用して、NPS でネットワーク ポリシーを構成することができます。

## <a name="add-a-network-policy"></a>ネットワーク ポリシーを追加します。

ネットワーク ポリシー サーバー \(NPS\) ネットワーク ポリシーを使用して、ネットワークに接続する接続要求するかどうかを決定するユーザー アカウントのダイヤルイン プロパティが承認されています。

NPS コンソールまたはリモート アクセス コンソールで、新しいネットワーク ポリシーを構成するのには、この手順を使用することができます。

### <a name="performing-authorization"></a>承認を実行します。

NPS では、接続要求の承認を実行するときに、最初のポリシーで始まると、構成されているポリシーの一覧を下に移動し、ポリシーの順序付きリストでは、各ネットワーク ポリシーで要求を比較します。 NPS では、条件に一致する接続要求ポリシーが検出されると、NPS ポリシーと一致して、ユーザー アカウントのダイヤルイン プロパティを使用して承認を実行します。 ネットワーク ポリシーを介してアクセスまたはコントロールのアクセスを許可するユーザー アカウントのダイヤルイン プロパティが構成されている接続要求が承認されている場合は、NPS に接続するネットワーク ポリシーで構成されている設定が適用されます。

接続要求と一致するネットワーク ポリシーが見つからなかった場合は、NPS のアクセスを許可するユーザー アカウントのダイヤルイン プロパティが設定されていない限り、接続要求が拒否されます。

ユーザー アカウントのダイヤルイン プロパティは、アクセスを拒否する設定は、NPS によって、接続要求が拒否されます。

### <a name="key-settings"></a>キーの設定

ネットワーク ポリシーで指定した値を作成する新しいネットワーク ポリシー ウィザードを使用すると**ネットワーク接続方法**を自動的に構成するために使用、**ポリシーの種類**条件。 

- 未指定の既定値を保持している場合、ネットワーク ポリシーを作成することは、任意の種類のネットワーク アクセス サーバー (NAS) を使用しているすべてのネットワーク接続の種類に対して、NPS で評価されます。
- ネットワークの接続方法を指定する場合、NPS は、指定したネットワーク アクセス サーバーの種類から、接続要求が送信された場合にのみ、ネットワーク ポリシーを評価します。

**アクセス許可**を選択する必要があります] ページで、**アクセスを許可**する場合は、ネットワークに接続するユーザーを許可するポリシーです。 場合は、ネットワークに接続できないようにするポリシー**アクセスが拒否された**します。 

Active Directory のダイヤルイン プロパティ アカウントのユーザーによって決定されるアクセス許可が必要な場合&reg;Domain Services \(AD DS\)、選択、**アクセスは、ユーザーのダイヤルイン プロパティによって決まります**チェック ボックスをオンします。

メンバーシップ**Domain Admins**、相当するものでは、この手順を完了するために必要な最低限またはします。

### <a name="to-add-a-network-policy"></a>ネットワーク ポリシーを追加するには 

1. NPS コンソールを開き、し、ダブルクリック**ポリシー**します。

2. コンソール ツリーで、右クリックして**ネットワーク ポリシー**、] をクリック**新規**します。 新しいネットワーク ポリシー ウィザードが開きます。

3. ポリシーを作成するのにには、新しいネットワーク ポリシー ウィザードを使用します。

## <a name="create-network-policies-for-dial-up-or-vpn-with-a-wizard"></a>ダイヤルアップまたは VPN ウィザードを使用してネットワーク ポリシーを作成します。

この手順を使用すると、接続要求ポリシーおよびダイアル アップ サーバーまたは仮想プライベート ネットワーク \(VPN\) サーバーのいずれかを NPS の RADIUS サーバーにリモート認証ダイヤルイン ユーザー サービス \(RADIUS\) クライアントとして展開するのにために必要なネットワーク ポリシーを作成します。

>[!NOTE]
>ラップトップ コンピューターや、クライアント オペレーティング システムを実行している他のコンピューターなどのクライアント コンピューターは、RADIUS クライアントではありません。 RADIUS クライアントのネットワーク アクセス サーバーとは、ワイヤレス アクセス ポイント、802.1 X 認証スイッチ、仮想プライベート ネットワーク \(VPN\) サーバー、およびダイアル アップ サーバーなど、これらのデバイスでは、RADIUS プロトコルを使用して、NPS サーバーなどの RADIUS サーバーと通信するため。

この手順では、NPS のダイヤルアップ接続または仮想プライベート ネットワーク接続の新規作成ウィザードを開く方法について説明します。

ウィザードを実行した後、次のポリシーが作成されます。

- 1 つの接続要求ポリシー
- 1 つのネットワーク ポリシー

ダイアル アップ サーバーと VPN サーバーの新しいポリシーを作成する必要があるたびに、ダイヤルアップ接続または仮想プライベート ネットワーク接続の新規作成ウィザードを実行できます。

ダイヤルアップまたは仮想プライベート ネットワーク接続の新規作成ウィザードを実行しているは、NPS サーバーの RADIUS クライアントとしてダイヤルアップまたは VPN サーバーを展開するために必要な唯一の手順ではありません。 両方のネットワークへのアクセス メソッドは、追加のハードウェアおよびソフトウェア コンポーネントを展開することが必要です。

メンバーシップ**Domain Admins**、相当するものでは、この手順を完了するために必要な最低限またはします。

### <a name="to-create-policies-for-dial-up-or-vpn-with-a-wizard"></a>ウィザードを使用してダイヤルアップまたは VPN のポリシーを作成するには

1. NPS コンソールを開きます。 選択されていない場合はクリックして**NPS \(Local\)**します。 リモート NPS サーバーでポリシーを作成する場合は、サーバーを選択します。

2. **Getting Started**と**標準構成**[**ダイヤルアップ接続または VPN 接続用の RADIUS サーバー**します。 テキストとリンクは [選択内容を反映するように、テキストを変更します。

3. をクリックして**VPN またはダイアル アップ ウィザードを使用して**します。 ダイヤルアップまたは仮想プライベート ネットワーク接続の新規作成ウィザードが開きます。

4. 新しいポリシーの作成を完了するウィザードの指示に従います。

## <a name="create-network-policies-for-8021x-wired-or-wireless-with-a-wizard"></a>X ワイヤード (有線) またはウィザードを使用してワイヤレス 802.1 用のネットワーク ポリシーを作成します。

この手順を使用すると、接続要求ポリシーと、NPS の RADIUS サーバーにリモート認証ダイヤルイン ユーザー サービス (RADIUS) クライアントとして 802.1 X 認証スイッチまたは 802.1 X ワイヤレス アクセス ポイントのいずれかを展開するのに必要があるネットワーク ポリシーを作成します。

この手順では、NPS でセキュリティで保護された新しい IEEE 802.1 X ワイヤードおよびワイヤレス接続ウィザードを起動する方法について説明します。

ウィザードを実行した後、次のポリシーが作成されます。

- 1 つの接続要求ポリシー
- 1 つのネットワーク ポリシー

802.1 X アクセス用の新しいポリシーを作成する必要があるたびに、セキュリティで保護された新しい IEEE 802.1 X ワイヤードおよびワイヤレス接続ウィザードを実行できます。

セキュリティで保護された新しい IEEE 802.1 X ワイヤードおよびワイヤレス接続ウィザードを実行することは、802.1 X NPS サーバーの RADIUS クライアントとしてスイッチやワイヤレス アクセス ポイントを認証を展開するために必要な手順のみではありません。 両方のネットワークへのアクセス メソッドは、追加のハードウェアおよびソフトウェア コンポーネントを展開することが必要です。

メンバーシップ**Domain Admins**、相当するものでは、この手順を完了するために必要な最低限またはします。

### <a name="to-create-policies-for-8021x-wired-or-wireless-with-a-wizard"></a>ウィザードを使用してワイヤード (有線) またはワイヤレス 802.1 X のポリシーを作成するには

1. NPS サーバーで、サーバー マネージャーで、をクリックして**ツール**、] をクリックし、**ネットワーク ポリシー サーバー**します。 NPS コンソールが開きます。 

2. 選択されていない場合はクリックして**NPS \(Local\)**します。 リモート NPS サーバーでポリシーを作成する場合は、サーバーを選択します。

3. **Getting Started**と**標準構成**[ **802.1 X ワイヤレスまたはワイヤード (有線) 接続用の RADIUS サーバー**します。 テキストとリンクは [選択内容を反映するように、テキストを変更します。

4. をクリックして**構成 802.1 X ウィザードを使用して**します。 セキュリティで保護された新しい IEEE 802.1 X ワイヤードおよびワイヤレス接続ウィザードが開きます。

5. 新しいポリシーの作成を完了するウィザードの指示に従います。

## <a name="configure-nps-to-ignore-user-account-dial-in-properties"></a>ユーザー アカウントのダイヤルイン プロパティを無視する NPS を構成します。

この手順を使用して、承認プロセス中に Active Directory 内のユーザー アカウントのダイヤルイン プロパティを無視する NPS ネットワーク ポリシーを構成します。 Active Directory ユーザーとコンピューターのユーザー アカウントがある場合を除き、NPS が認証プロセス中に評価ダイヤルイン プロパティ、**ネットワーク アクセス許可**ユーザー アカウントのプロパティに設定されて**NPS ネットワーク ポリシーでのアクセスを制御**します。 

2 つの状況で Active Directory のユーザー アカウントのダイヤルイン プロパティを無視するために NPS を構成することがあります。

- ときに、ネットワーク ポリシーを使用して NPS の承認を簡略化するが、すべてのユーザー アカウント、**ネットワーク アクセス許可**プロパティに設定**NPS ネットワーク ポリシーでのアクセスを制御**します。 たとえば、一部のユーザー アカウントがある、**ネットワーク アクセス許可**ユーザー アカウントのプロパティを設定**アクセス権の拒否**または**アクセスを許可する**します。

- ときにユーザー アカウントの他のダイヤルイン プロパティでは、ネットワーク ポリシーで構成されている接続の種類に適用されません。 以外のプロパティなど、**ネットワーク アクセス許可**設定がダイヤルイン接続または VPN 接続、のみに適用されるが、作成して、ネットワーク ポリシーでは、ワイヤレスや認証スイッチ接続します。

この手順を使用すると、ユーザー アカウントのダイヤルイン プロパティを無視するのにように構成します。 接続要求には、このチェック ボックスが選択されているネットワーク ポリシーが一致すると、NPS を使わないユーザー アカウントのダイヤルイン プロパティ ユーザーまたはコンピューターがネットワークにアクセスを許可されているかどうかを決定するにはネットワーク ポリシーの設定のみについては、承認の判断に使用されます。

メンバーシップ**管理者**、相当するものでは、この手順を完了するために必要な最低限またはします。

1. NPS サーバーで、サーバー マネージャーで、をクリックして**ツール**、] をクリックし、**ネットワーク ポリシー サーバー**します。 NPS コンソールが開きます。

2. ダブルクリック**ポリシー**、] をクリックして**ネットワーク ポリシー**、し、詳細ウィンドウでポリシーを構成する] をダブルクリックします。

3. ポリシーで**プロパティ**] ダイアログ ボックスで、**概要**] タブの [**アクセス許可**を選択、**ユーザー アカウントのダイヤルイン プロパティは無視**チェック ボックスをオンし、をクリックして**[OK]**します。

### <a name="to-configure-nps-to-ignore-user-account-dial-in-properties"></a>ユーザー アカウントのダイヤルイン プロパティを無視する NPS を構成するには



## <a name="configure-nps-for-vlans"></a>Vlan の NPS を構成します。

Windows Server 2016 では、VLAN 対応のネットワーク アクセス サーバーと NPS を使用して、そのセキュリティのアクセス許可に適切なネットワーク リソースにのみアクセス権を持つユーザーのグループを提供できます。 たとえば、することができます訪問者ワイヤレス アクセスをインターネットにせず、組織のネットワークへのアクセスを可能にします。 

さらに、Vlan、物理的に異なる場所または別の物理サブネット上に存在するネットワーク リソースを論理的にグループにできます。 たとえば、営業部とクライアント コンピューター、サーバー、およびプリンターなどのネットワーク リソースのメンバーは、組織で複数の異なる建物内である可能性がありますが、同じ IP アドレスの範囲を使用する 1 つの VLAN 上のすべてのリソースを配置することができます。 VLAN、関数は、1 つのサブネットとして、エンド ユーザーの観点からです。

Vlan は、さまざまなユーザー グループ間のネットワークを分離する場合にも使用できます。 グループを定義する方法を確認した後は、Active Directory ユーザーとコンピューター スナップインにセキュリティ グループを作成し、グループにメンバーを追加できます。

### <a name="configure-a-network-policy-for-vlans"></a>Vlan のネットワーク ポリシーを構成します。

この手順を使用すると、ネットワーク ポリシー、VLAN にユーザーを割り当てますを構成します。 ルーター、スイッチ、およびアクセスのコントローラーなど、VLAN 対応のネットワーク ハードウェアを使用する場合は、特定の Active Directory グループのメンバーを特定の Vlan に配置するのにはアクセス サーバーを指示するためのネットワーク ポリシーを構成できます。 な Vlan を論理的にグループのネットワーク リソースには、この機能は、ネットワークのソリューションの実装の設計と柔軟性を提供します。

属性を構成する必要がありますな Vlan を使用するための NPS ネットワーク ポリシーの設定を構成するときに**トンネルの種類 [中]**、**トンネル Pvt グループ ID**、**トンネルの種類**、および**トンネル タグ**します。 

この手順は; ガイドラインとして提供されます。ネットワーク構成には、次のような異なる設定を必要があります。

メンバーシップ**管理者**、相当するものでは、この手順を完了するために必要な最低限またはします。

### <a name="to-configure-a-network-policy-for-vlans"></a>Vlan のネットワーク ポリシーを構成するには

1. NPS サーバーで、サーバー マネージャーで、をクリックして**ツール**、] をクリックし、**ネットワーク ポリシー サーバー**します。 NPS コンソールが開きます。

2. ダブルクリック**ポリシー**、] をクリックして**ネットワーク ポリシー**、し、詳細ウィンドウでポリシーを構成する] をダブルクリックします。

3. ポリシーで**プロパティ**ダイアログ ボックスで、をクリックして、**設定**] タブ。

4. ポリシーで**プロパティ**で、**設定**で、**RADIUS 属性**、いることを確認**標準**が選択されています。

5. 詳細ウィンドウで**属性**、**サービスの種類**の既定値を持つ属性が構成されている**フレーム**します。 VPN およびダイヤルアップ用のアクセス メソッドを使ったポリシーに対して、既定で、**フレーム プロトコル**の値を持つ属性が構成されている**PPP**します。 Vlan の必要な追加の接続の属性を指定する] をクリックして**追加**します。 **標準 RADIUS 属性を追加**] ダイアログ ボックスが開きます。

6. **標準 RADIUS 属性を追加**属性で下にスクロールして、次の属性を追加します。

    - **トンネルの種類 [中]**します。 以前選択した内容がポリシーに適切な値を選択します。 たとえば、ワイヤレス ポリシー、ネットワーク ポリシーを構成している場合は、[**値: 802 (が含まれてすべて 802 にはメディアとイーサネットの標準形式)**します。

    - **トンネル Pvt グループ ID**します。 グループのメンバーの割り当てられた VLAN 番号を表す整数を入力します。 

    - **トンネルの種類**します。 選択**仮想 Lan (VLAN)**します。


7. **標準 RADIUS 属性を追加**、] をクリックして**閉じる**します。 

8. ネットワーク アクセス サーバー (NAS) の使用が必要な場合、**トンネル タグ**属性を追加する、次の手順を使用して、**トンネル タグ**属性、ネットワーク ポリシーをします。 NAS のマニュアルにこの属性がについて説明しない場合、ポリシーに追加しないでください。 必要な場合、次のように属性を追加します。

    - ポリシーで**プロパティ**で、**設定**で、**RADIUS 属性**、] をクリックして**ベンダー固有**します。 

    - 詳細ウィンドウで、をクリックして**追加**します。 **ベンダー固有属性を追加**] ダイアログ ボックスが開きます。

    - **属性**まで下にスクロールし、選択**トンネル タグ**、] をクリックし、**追加**します。 **属性情報**] ダイアログ ボックスが開きます。 

    - **属性値**、ハードウェアのマニュアルから取得した値を入力します。

## <a name="configure-the-eap-payload-size"></a>EAP ペイロードのサイズを構成します。

場合によっては、ルーターまたはファイアウォール パケットをドロップ断片化を必要とするパケットを破棄するように構成されているためです。

既定の最大転送単位 \(MTU\) EAP ペイロードの NPS を使用するが、認証方法として、トランスポート層セキュリティ \(TLS\) または EAP-TLS では、Extensible Authentication Protocol \(EAP\) を使用しているネットワーク ポリシーと NPS を展開するときに 1500 バイトです。 

EAP ペイロードのこの最大サイズをルーターまたは NPS サーバーと RADIUS クライアントの間にファイアウォールが断片化を必要とする RADIUS メッセージを作成できます。 このような場合、ルーターまたはファイアウォールの RADIUS クライアントと NPS サーバーの間に配置可能性がありますサイレント破棄認証が失敗し、アクセス クライアントのネットワークに接続できない場合に、一部のフラグメントします。

EAP ペイロードに 1344 を超える値をネットワーク ポリシーでフレーム MTU 属性を調整して、NPS を使用する最大サイズを小さくには、次の手順を使用します。

メンバーシップ**管理者**、相当するものでは、この手順を完了するために必要な最低限またはします。

### <a name="to-configure-the-framed-mtu-attribute"></a>フレーム MTU 属性を構成するには

1. NPS サーバーで、サーバー マネージャーで、をクリックして**ツール**、] をクリックし、**ネットワーク ポリシー サーバー**します。 NPS コンソールが開きます。
 
2. ダブルクリック**ポリシー**、] をクリックして**ネットワーク ポリシー**、し、詳細ウィンドウでポリシーを構成する] をダブルクリックします。

3. ポリシーで**プロパティ**ダイアログ ボックスで、をクリックして、**設定**] タブ。

4. **設定**で、**RADIUS 属性**、] をクリックして**標準**します。 詳細ウィンドウで、をクリックして**追加**します。 **標準 RADIUS 属性を追加**] ダイアログ ボックスが開きます。

5. **属性**下にスクロールして] をクリックして**フレーム MTU**、] をクリックし、**追加**します。 **属性情報**] ダイアログ ボックスが開きます。

6. **属性値**、以下の値を入力より**1344**します。 をクリックして**OK**、] をクリックして**閉じる**、] をクリックし、**[OK]**します。



ネットワーク ポリシーの詳細については、次を参照してください。[ネットワーク ポリシー](nps-np-overview.md)します。

ポリシーの属性のネットワークを指定するパターン マッチング構文の例についてを参照してください。[NPS で正規表現を使用](nps-crp-reg-expressions.md)します。

NPS の詳細については、次を参照してください。[ネットワーク ポリシー サーバー (NPS)](nps-top.md)します。
