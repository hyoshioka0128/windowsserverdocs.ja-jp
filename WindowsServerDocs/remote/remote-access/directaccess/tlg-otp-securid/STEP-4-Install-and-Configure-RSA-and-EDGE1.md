---
title: 手順 4 のインストールと RSA と EDGE1 構成
description: このトピックでは、OTP 認証と Windows Server 2016 で RSA SecurID を使用した DirectAccess のデモンストレーションのテスト ラボ ガイドの一部
manager: brianlic
ms.custom: na
ms.prod: windows-server-threshold
ms.reviewer: na
ms.suite: na
ms.technology: networking-da
ms.tgt_pltfrm: na
ms.topic: article
ms.assetid: d46ede6f-1a21-414d-b8c3-6b5c87344b9d
ms.author: pashort
author: shortpatti
ms.openlocfilehash: 5280a02568305512f868f559fe35d11dc618f2b4
ms.sourcegitcommit: afb0602767de64a76aaf9ce6a60d2f0e78efb78b
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 06/20/2019
ms.locfileid: "67283069"
---
# <a name="step-4-install-and-configure-rsa-and-edge1"></a>手順 4 のインストールと RSA と EDGE1 構成

>適用先:Windows Server 2016 の Windows Server (半期チャネル)

RSA は RADIUS および OTP サーバーであり、半径と OTP を構成する前にインストールされます。  
  
RSA 展開を構成する次の手順が実行されます。  
  
1. RSA サーバー上のオペレーティング システムをインストールします。 RSA サーバーで Windows Server 2016、Windows Server 2012 R2 または Windows Server 2012 をインストールします。  
  
2. RSA に TCP/IP を構成します。 RSA サーバー上の TCP/IP 設定を構成します。  
  
3. 認証マネージャーのインストール ファイルを RSA サーバーにコピーします。 インストールした後は、RSA、上のオペレーティング システムは、認証マネージャーのファイルを RSA コンピューターにコピーします。  
  
4. RSA サーバーを CORP ドメインに参加します。 RSA を CORP ドメインに参加させます。  
  
5. RSA 上の Windows ファイアウォールを無効にします。 RSA サーバー上の Windows ファイアウォールを無効にします。  
  
6. RSA サーバーには、RSA Authentication Manager をインストールします。 RSA Authentication Manager をインストールします。  
  
7. RSA Authentication Manager を構成します。 認証マネージャーを構成します。  
  
8. DAProbeUser を作成します。 調査のためのユーザー アカウントを作成します。  
  
9. CLIENT1 には、RSA SecurID ソフトウェア トークンをインストールします。 CLIENT1 には、RSA SecurID ソフトウェア トークンをインストールします。  
  
10. RSA 認証エージェントとして EDGE1 を構成します。 EDGE1 で RSA 認証エージェントを構成します。  
  
11. EDGE1 は、OTP 認証をサポートするように構成します。 DirectAccess では、OTP を構成し、構成を確認します。  
  
## <a name="InstallOS"></a>RSA サーバー上のオペレーティング システムをインストールします。  
  
1.  RSA には、Windows Server 2016、Windows Server 2012 R2 または Windows Server 2012 のインストールを開始します。  
  
2.  指示に従って、Windows Server 2016、Windows Server 2012 R2 または Windows Server 2012 (完全インストール) およびローカル管理者アカウントの強力なパスワードを指定する、インストールを完了します。 ローカルの Administrator アカウントを使用してログオンします。  
  
3.  RSA をインターネットにアクセスできるネットワークに接続し、Windows Update の Windows Server 2016、Windows Server 2012 R2 または Windows Server 2012 では、最新の更新プログラムをインストールし、インターネットから切断を実行します。  
  
4.  RSA を Corpnet サブネットに接続します。  
  
## <a name="TCP"></a>RSA に TCP/IP を構成します。  
  
1.  初期構成タスク をクリックして**ネットワークの構成**します。  
  
2.  **ネットワーク接続**を右クリックして**ローカル エリア接続**、 をクリックし、**プロパティ**します。  
  
3.  **[インターネット プロトコル バージョン 4 (TCP/IPv4)]** をクリックし、 **[プロパティ]** をクリックします。  
  
4.  **[次の IP アドレスを使う]** をクリックします。 **[IP アドレス]** に、「 **10.0.0.5**」と入力します。 **[サブネット マスク]** に、「 **255.255.255.0**」と入力します。 **デフォルト ゲートウェイ**、型**10.0.0.2**します。 をクリックして**次の DNS サーバーのアドレスを使用して、** の**優先 DNS サーバー**、型**10.0.0.1**します。  
  
5.  **[詳細設定]** 、 **[DNS]** タブの順にクリックします。  
  
6.  **この接続の DNS サフィックス**、型**corp.contoso.com**、順にクリックします**OK** 2 回です。  
  
7.  **ローカル エリア接続のプロパティ**ダイアログ ボックスで、をクリックして**閉じる**します。  
  
8.  **[ネットワーク接続]** ウィンドウを閉じます。  
  
## <a name="copyinstfiles"></a>認証マネージャーのインストール ファイルを RSA サーバーにコピーします。  
  
1.  RSA では、サーバーは、C:\RSA インストール フォルダーを作成します。  
  
2.  C:\RSA インストール フォルダーには、RSA Authentication Manager 7.1 SP4 メディアの内容をコピーします。  
  
3.  サブフォルダーを作成します C:\RSA Installation\License とトークンです。  
  
4.  C:\RSA Installation\License して、トークンは、RSA ライセンス ファイルをコピーします。  
  
## <a name="JoinDomain"></a>RSA サーバーを CORP ドメインに参加させる  
  
1.  右クリックして**マイ コンピューター**、 をクリック**プロパティ**します。  
  
2.  **[システムのプロパティ]** ダイアログ ボックスの **[コンピューター名]** タブで **[変更]** をクリックします。  
  
3.  **コンピューター名**、型**RSA**します。 **のメンバー**、 をクリックして**ドメイン**、型**corp.contoso.com**、 をクリック**OK**します。  
  
4.  ユーザー名とパスワードを求められたら、入力**User1**とそのパスワード、およびクリック**OK**します。  
  
5.  ドメイン参加 ダイアログ ボックスで次のようにクリックします。 **OK**します。  
  
6.  コンピューターを再起動するよう求めるメッセージが表示されたら、 **[OK]** をクリックします。  
  
7.  **[システムのプロパティ]** ダイアログ ボックスで、 **[閉じる]** をクリックします。  
  
8.  コンピューターを再起動するよう求めるメッセージが表示されたら、 **[今すぐ再起動する]** をクリックします。  
  
9. コンピューターの再起動後に入力**User1**と CORP でを選択して、パスワード、**へのログオン:** ドロップダウン リストで、をクリックして**OK**。  
  
## <a name="BKMK_Firewall"></a>RSA 上の Windows ファイアウォールを無効にします。  
  
1.  をクリックして**開始**、 をクリックして**コントロール パネルの** 、 をクリックして**システムとセキュリティ**、 をクリック**Windows ファイアウォール**します。  
  
2.  クリックして**オンまたはオフ、Windows ファイアウォール有効**します。  
  
3.  **Windows ファイアウォールを無効に**すべての設定。  
  
4.  クリックして**OK**と Windows ファイアウォールを閉じます。  
  
## <a name="install"></a>RSA サーバーに RSA Authentication Manager をインストールします。  
  
1.  このプロセス中にいつでも、セキュリティ警告メッセージが表示されたら、クリックして**実行**を続行します。  
  
2.  C:\RSA インストール フォルダーを開き、ダブルクリックして**autorun.exe**します。  
  
3.  をクリックして**今すぐインストール**、 をクリックして**次**、南北アメリカの一番上のオプションを選択して、クリックして**次へ**します。  
  
4.  選択**ライセンス契約の条項に同意**、 をクリック**次**します。  
  
5.  選択**プライマリ インスタンス**、 をクリック**次**します。  
  
6.  **ディレクトリ名:** フィールドに「 **C:\RSA**、 をクリック**次**します。  
  
7.  サーバー名 (RSA.corp.contoso.com) と IP アドレスが正しいこと、し、をクリックを確認します。**次**します。  
  
8.  C:\RSA Installation\License とトークンを参照してクリックして**次**します。  
  
9. **ライセンス ファイルを確認してください**] ページで [**次**します。  
  
10. **ユーザー ID**フィールドに「**管理者**、し、**パスワード**と**パスワードの確認**フィールドは、強力なパスワードを入力します。 **[次へ]** をクリックします。  
  
11. [ログの選択] 画面で、既定値のままし、クリックして**次**します。  
  
12. 概要の画面では、次のようにクリックします。**インストール**します。  
  
13. インストールが完了したら、クリックして**完了**します。  
  
## <a name="confiauthmgr"></a>RSA Authentication Manager を構成します。  
  
1.  RSA セキュリティ コンソールが自動的に開かない場合は、RSA コンピューターのデスクトップで「RSA Security コンソール」をダブルクリックします。  
  
2.  場合は、セキュリティ証明書の警告、セキュリティの警告が表示されたら、をクリックして**このサイトの閲覧を続行する** をクリックしてまたは**はい**を続行し、要求された場合、このサイトを信頼済みサイトに追加します。  
  
3.  **ユーザー ID**フィールドに「**管理者** をクリック**OK**します。  
  
4.  **パスワード**フィールド管理者アカウントのパスワードを入力し、クリックして**ログオン**します。  
  
5.  トークン情報を挿入します。  
  
    1.  **RSA Security コンソール**クリックして**認証** をクリック**SecurID トークン**します。  
  
    2.  をクリックして**インポート トークン ジョブ**、 をクリックし、**新規追加**します。  
  
    3.  **インポート オプション**セクション**参照**します。 参照して、C:\ でトークンの XML ファイルを選択します。RSA Installation\License トークン フォルダーとクリック**オープン**します。  
  
    4.  クリックして**ジョブの送信**ページの下部にあります。  
  
6.  OTP の新しいユーザーを作成します。  
  
    1.  **RSA Security コンソール** をクリックして、 **Identity**  タブで **ユーザー**、 をクリック**新規追加**します。  
  
    2.  **Last Name:** セクション型**ユーザー**、し、**ユーザー ID:** セクション型**User1** (ユーザー Id がありますを使用する AD ユーザー名と同じこのラボ)。  **パスワード:** と**パスワードの確認:** のセクションでは、強力なパスワードを入力します。 クリア、 **'ユーザーが次回ログオン時にパスワードを変更する必要があります'**  チェック ボックスをクリックします**保存**します。  
  
7.  インポートされたトークンのいずれかに User1 を割り当てます。  
  
    1.  **ユーザー**ページ クリック**User1**  をクリック**SecurID トークン**します。  
  
    2.  をクリックして**SecurID トークン**クリック**トークンを割り当てる**。  
  
    3.  下、**シリアル番号**見出しが表示されている場合、最初の数値をクリックし、クリックして**割り当てる**します。  
  
    4.  割り当て済みのトークンをクリックし、をクリックして**編集**します。 **SecurID PIN 管理**セクション**ユーザーの認証要件を**を選択します**PIN (トークン コードのみ) を必要としない**します。  
  
    5.  クリックして**を保存および配布トークン**します。  
  
    6.  **配布ソフトウェア トークン**ページで、**基本**セクションで、**問題トークン ファイル (SDTID)** します。  
  
    7.  **配布ソフトウェア トークン**ページで、**トークン ファイル オプション** セクションで、クリア、**コピー防止を有効にする**チェック ボックスをオンします。 をクリックして**パスワードの**と**次**しません。  
  
    8.  **配布ソフトウェア トークン**ページで、**ファイルのダウンロード**セクションで、**今すぐダウンロード**します。 **[保存]** をクリックします。 C:\RSA インストールを参照してクリックして**保存**と**閉じる**します。  
  
    9. 最小化、 **RSA Security コンソール**後で使用します。  
  
8.  認証マネージャーは、RADIUS サーバーとして構成します。  
  
    1.  RSA コンピューター デスクトップ ダブルクリックで **「セキュリティのオペレーション コンソールの RSA」** します。  
  
    2.  場合は、セキュリティ証明書の警告、セキュリティの警告が表示されたら、をクリックして**このサイトの閲覧を続行する** をクリックしてまたは**はい**を続行し、要求された場合、このサイトを信頼済みサイトに追加します。  
  
    3.  ユーザー ID とパスワードを入力し、クリックして**ログオン**します。  
  
    4.  クリックして**サーバーを構成する展開の構成 - RADIUS -** します。  
  
    5.  **必要な追加の資格情報** ページが管理者のユーザー ID とパスワードを入力し、をクリックして**OK**します。  
  
    6.  **RADIUS サーバーの構成**ページは、管理者ユーザーのために使用する同じパスワードを入力します、**シークレット**と**マスター パスワード**します。 管理者のユーザー ID とパスワードを入力し、クリックして**構成**します。  
  
    7.  いることを確認、メッセージ **'が正常に構成されている RADIUS サーバー'** が表示されます。 **[完了]** をクリックします。 閉じる、 **RSA オペレーション コンソール**します。  
  
    8.  戻り、 **「RSA Security コンソール」** します。  
  
    9. **RADIUS**タブをクリックして**RADIUS サーバー**します。 その rsa.corp.contoso.com が表示されていることを確認します。  
  
9. RSA 認証クライアントとしては、RSA サーバーを構成します。  
  
    1.  **RADIUS** ] タブで [ **RADIUS クライアント**と**新規追加**します。  
  
    2.  をクリックして、**任意の RADIUS クライアント**チェック ボックスをオンします。  
  
    3.  強力なパスワードに任意の入力、**共有シークレット**フィールド。 OTP 用 EDGE1 を構成するときに、これと同じパスワードを後で使用されます。  
  
    4.  ままに、 **IP アドレス**フィールドを空白、および**ように/モデル**エントリとして**RADIUS 標準**。  
  
    5.  クリックして**RSA エージェントがない保存**します。  
  
10. RSA 認証エージェントと EDGE1 を構成するために必要なファイルを作成します。  
  
    1.  **アクセス** タブで、強調表示**認証エージェント**、 をクリック**新規追加**します。  
  
    2.  型**EDGE1**で、 **Hostname**フィールドをクリックして**を解決する IP**します。  
  
    3.  EDGE1 の IP アドレスが表示されるようになりましたが、 **IP アドレス**フィールド。 **[保存]** をクリックします。  
  
11. EDGE1 サーバー (AM_Config.zip) の構成ファイルを生成します。  
  
    1.  **アクセス** タブで、強調表示**認証エージェント**、 をクリック**構成ファイルの生成**します。  
  
    2.  **構成ファイルの生成**ページ クリック**構成ファイルの生成**、順にクリックします**今すぐダウンロード**。  
  
    3.  クリックして**保存**C:\ を参照RSA のインストール、およびクリック**保存**します。  
  
    4.  クリックして**閉じる**上、**ダウンロードの完了**ダイアログ。  
  
12. EDGE1 サーバー (EDGE1_NodeSecret.zip) のノードのシークレット ファイルを生成します。  
  
    1.  **アクセス** タブで、強調表示**認証エージェント**、 をクリック**既存管理**します。  
  
    2.  現在構成されているノード EDGE1 をクリックし、をクリックして**ノード シークレットの管理**します。  
  
    3.  チェック、 **、新しいノードのランダム シークレットを作成し、ノードのシークレットをファイルにエクスポート**チェック ボックスをオンします。  
  
    4.  管理者ユーザーの使用と同じパスワードを入力、**暗号化パスワード**と**暗号化パスワードの確認**フィールド、およびクリック**保存**します。  
  
    5.  **ノード シークレット ファイルの生成**ページ クリック**今すぐダウンロード**。  
  
    6.  **ファイルのダウンロード**ダイアログ**保存**C:\RSA インストールを参照し、クリックして、**保存**。 クリックして**閉じる**上、**ダウンロードの完了**ダイアログ。  
  
    7.  RSA Authentication Manager メディアのコピー \auth_mgr\windows-x86_64\am\rsa-ace_nsload\win32-5.0-x86\agent_nsload.exe C:\RSA インストールするからです。  
  
## <a name="BKMK_DAProbeUser"></a>DAProbeUser を作成します。  
  
1.  **RSA Security コンソール** をクリックして、 **Identity**  タブで **ユーザー**、 をクリック**新規追加**します。  
  
2.  **姓:** セクション型**プローブ**、し、**ユーザー ID:** セクション型**DAProbeUser**。 **パスワード:** と**パスワードの確認:** のセクションでは、強力なパスワードを入力します。 クリア、 **'ユーザーが次回ログオン時にパスワードを変更する必要があります'**  チェック ボックスをクリックします**保存**します。  
  
## <a name="InstToken"></a>CLIENT1 で RSA SecurID ソフトウェア トークンをインストールします。  
CLIENT1 に SecurID ソフトウェア トークンをインストールするのにには、この手順を使用します。  
  
#### <a name="install-securid-software-token"></a>SecurID ソフトウェア トークンをインストールします。  
  
1.  CLIENT1 コンピューターには、C:\RSA ファイル フォルダーを作成します。 C:\RSA ファイルに、RSA コンピューター上の C:\RSA インストールから Software_Tokens.zip ファイルをコピーします。 CLIENT1 で C:\RSA ファイル User1_000031701832.SDTID ファイルを抽出します。  
  
2.  RSA SecurID ソフトウェアのトークンのメディア ソースへのアクセスし、で RSASECURIDTOKEN410 をダブルクリックして、 **SecurID SoftwareToken クライアント アプリ**RSA SecurID インストールを開始するフォルダー。 場合、**ファイルを開く - セキュリティ警告**メッセージが表示されます、 をクリックし、**実行**します。  
  
3.  **RSA SecurID ソフトウェア トークン - InstallShield ウィザード**ダイアログ**次**2 回です。  
  
4.  ライセンス条項に同意し、をクリックして**次**します。  
  
5.  **セットアップの種類**ダイアログの **標準**、 をクリックして**次**、 をクリック**インストール**します。  
  
6.  **[ユーザー アカウント制御]** ダイアログ ボックスが表示されたら、表示された操作が正しいことを確認し、 **[はい]** をクリックします。  
  
7.  選択、**起動 RSA SecurID ソフトウェア トークン**チェック ボックスをオンにして**完了**します。  
  
8.  クリックして**ファイルからインポート**します。  
  
9. クリックして**参照**C:\RSA Files\User1_000031701832.SDTID を選択して、クリックして**オープン**します。  
  
10. **[OK]** を 2 回クリックします。  
  
## <a name="configAuthAgt"></a>RSA 認証エージェントとして EDGE1 を構成します。  
RSA 認証を実行するのに EDGE1 を構成するのにには、この手順を使用します。  
  
#### <a name="configure-the-rsa-authentication-agent"></a>RSA 認証エージェントを構成します。  
  
1. EDGE1 上、Windows エクスプ ローラーを開き C:\RSA ファイル フォルダーを作成します。 RSA ACE のインストール メディアに移動します。  
  
2. コピー ファイル agent_nsload.exe、AM_Config.zip と C:\RSA ファイルに RSA メディアから EDGE1_NodeSecret.zip です。  
  
3. 次の場所に両方の zip ファイルの内容を抽出します。  
  
   1.  C:\Windows\system32\  
  
   2.  C:\Windows\SysWOW64\  
  
4. C:\Windows\SysWOW64 に agent_nsload.exe をコピー\\します。  
  
5. 管理者特権のコマンド プロンプトを開き、C:\Windows\SysWOW64 に移動します。  
  
6. 型**agent_nsload.exe-f nodesecret.rec-p <password>** 場所<password>RSA の初期構成中に作成した強力なパスワードです。 Enter キーを押します。  
  
7. Copy C:\Windows\SysWOW64\securid to C:\Windows\System32.  
  
## <a name="configOTP"></a>EDGE1 OTP 認証をサポートするように構成します。  
この手順を使用して、DirectAccess の OTP を構成して、構成を確認します。  
  
#### <a name="configure-otp-for-directaccess"></a>DirectAccess の OTP を構成します。  
  
1.  EDGE1 でサーバー マネージャーを開き、をクリックして**リモート アクセス**左側のウィンドウでします。  
  
2.  右クリックして**EDGE1**サーバー ウィンドウを選び**リモート アクセス管理**します。  
  
3.  **[構成]** をクリックします。  
  
4.  **DirectAccess のセットアップ**ウィンドウで、**手順 2 - リモート アクセス サーバー**、 をクリックして**編集**します。  
  
5.  をクリックして **[次へ]** 3 回と、**認証**セクション選択**2 要素認証**と**OTP を使用**、ことを確認します**コンピューター証明書を使用して、** がチェックされます。 ルート CA に設定されていることを確認**CN = corp-CA APP1**します。 **[次へ]** をクリックします。  
  
6.  **OTP RADIUS サーバー**セクションで、空白をダブルクリックして**サーバー名**フィールド。  
  
7.  **RADIUS サーバーの追加**ダイアログ ボックスで、型**RSA**で、**サーバー名**フィールド。 クリックして**変更**横に、**共有シークレット**フィールドを RSA サーバーの RADIUS クライアントを構成するときに使用したのと同じパスワードを入力して、**新しいシークレット**と**新しいシークレットの確認入力**フィールド。 をクリックして**OK** 2 回、 をクリック**次へ**します。  
  
    > [!NOTE]  
    > RADIUS サーバーが、リモート アクセス サーバーとは異なるドメインの場合、**サーバー名**フィールドは、RADIUS サーバーの FQDN を指定する必要があります。  
  
8.  **OTP CA サーバー**選択の APP1.corp.contoso.com セクションし、をクリックして**追加**します。 **[次へ]** をクリックします。  
  
9. **OTP 証明書テンプレート**ページ クリック**参照**OTP 認証の場合とで発行される証明書の登録に使用される証明書テンプレートを選択する、 **証明書のテンプレート**ダイアログ ボックスの  **DAOTPLogon**します。 **[OK]** をクリックします。 クリックして**参照**サインオン OTP 証明書の登録要求とに、リモート アクセス サーバーによって使用される証明書を登録するために使用する証明書テンプレートを選択する、**証明書テンプレート** ダイアログ ボックス選択**DAOTPRA**します。 **[OK]** をクリックします。 **[次へ]** をクリックします。  
  
10. **リモート アクセス サーバーのセットアップ**ページ をクリックします**完了**、 をクリック**完了**上、 **DirectAccess エキスパート ウィザード**。  
  
11. **リモート アクセスの確認** ダイアログ ボックスをクリックして**適用**DirectAccess ポリシーを更新するには、待機し、クリックして**閉じる**します。  
  
12. **開始**画面で「**powershell.exe**、を右クリックして**powershell** をクリック**詳細**、 をクリック**として実行管理者**します。 **[ユーザー アカウント制御]** ダイアログ ボックスが表示されたら、表示された操作が正しいことを確認し、 **[はい]** をクリックします。  
  
13. Windows PowerShell ウィンドウで、入力**gpupdate/force** ENTER キーを押します。  
  
14. 終了し、リモート アクセス管理コンソールを再度開くすべての OTP 設定が正しいことを確認します。  
  


