---
title: サーバーに NPS サーバーをインストールして構成する
description: VPN サーバーによって送信される接続要求の NPS サーバーの処理は、ユーザーは、ユーザーの id を接続する権限を持つし、NPS で RADIUS アカウンティングを構成したときに選択した接続要求の側面のログを確認します。
ms.prod: windows-server-threshold
ms.technology: networking-ras
ms.topic: article
ms.assetid: ''
ms.localizationpriority: medium
ms.author: pashort
author: shortpatti
ms.date: 08/30/2018
ms.openlocfilehash: ca53ef28497a78f264c60ac1132f721fb6e01c15
ms.sourcegitcommit: 0d0b32c8986ba7db9536e0b8648d4ddf9b03e452
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 04/17/2019
ms.locfileid: "59890313"
---
# <a name="step-4-install-and-configure-the-network-policy-server-nps"></a>手順 4. インストールし、ネットワーク ポリシー サーバー (NPS) の構成

>   適用先:Windows Server 2019、Windows Server (半期チャネル)、Windows Server 2016、Windows Server 2012 R2、Windows 10


&#171;  [**次に：** 手順 3.リモート アクセス サーバーを常に VPN の構成します。](vpn-deploy-ras.md)<br>
&#187;  [**次に：** 手順 5.DNS とファイアウォールの設定を構成します。](vpn-deploy-dns-firewall.md)


この手順では、VPN サーバーによって送信される接続要求の処理のネットワーク ポリシー サーバー (NPS) をインストールします。

- ユーザーが接続するアクセス許可を持っていることを確認するための承認を実行します。
- ユーザーの id を確認する認証を実行します。
- NPS で RADIUS アカウンティングを構成したときに選択した接続要求の側面をログにアカウンティングを実行しています。

このセクションの手順では、次の項目を完了できます。

1.  コンピューターまたは NPS サーバーの計画および、組織や企業ネットワークにインストールされている VM の場合、NPS をインストールできます。

   >[!TIP] 
   >ネットワークで既に 1 つまたは複数の NPS サーバーが場合、は、NPS サーバーのインストールを実行する必要はありません - 代わりに、このトピックを使用して既存の NPS サーバーの構成を更新することができます。

>[!NOTE]  
Windows Server Core でネットワーク ポリシー サーバー サービスをインストールできます。

2.  組織/コーポレートの NPS サーバーでは、VPN サーバーから受信した接続要求を処理する RADIUS サーバーとして実行する NPS を構成できます。

## <a name="install-network-policy-server"></a>ネットワーク ポリシー サーバーをインストールする

この手順では、Windows PowerShell またはサーバー マネージャーの役割の追加と機能のウィザードのいずれかを使用して NPS をインストールします。 NPS とは、サーバーの役割である "ネットワーク ポリシーとアクセス サービス" の役割サービスの 1 つです。

>[!TIP] 
>既定では、NPS は、インストールされているすべてのネットワーク アダプターの RADIUS トラフィックをポート 1812、1813、1645、および 1646 でリッスンします。 NPS をインストールすると、セキュリティが強化された Windows ファイアウォールを有効にすると、これらのポートに対するファイアウォールの例外が IPv4 と IPv6 の両方のトラフィックに対して自動的に作成を取得します。 これらの既定値以外のポート経由で RADIUS トラフィックを送信するようにネットワーク アクセス サーバーを場合、NPS のインストール中にセキュリティが強化された Windows ファイアウォールで作成された例外を削除しを使用したポートに対する例外を作成RADIUS トラフィック。

**Windows PowerShell の手順:**

Windows PowerShell、Windows PowerShell を管理者として実行を使用してこの手順を実行するには、次のコマンドを入力し、ENTER キーを押します。

`Install-WindowsFeature NPAS -IncludeManagementTools`

**サーバー マネージャー用の手順:**

1.  サーバー マネージャーで、**管理**、 をクリック**追加の役割と機能の**します。 <p>役割と機能の追加ウィザードが起動されます。

2.  開始する前に次のようにクリックします。**次**します。

    >[!NOTE] 
    >**開始する前に**以前に選択した場合、追加の役割と機能のウィザードのページは表示されません**既定でこのページをスキップ**追加の役割と機能ウィザードを実行するときにします。

1.  インストールの種類で**役割ベースまたは機能ベースのインストール**が選択されているし、をクリックして**次**します。

2.  移行先サーバーで**サーバー プールからサーバーを選択**が選択されています。

3.  サーバーのプール、ローカル コンピューターが選択されていることを確認しますし**次**します。

4.  サーバーの役割を選択**ロール**、**ネットワーク ポリシーとアクセス サービス**します。 ネットワーク ポリシーとアクセス サービス用に必要な機能を追加する場合を求めるダイアログ ボックスが開きます。

5.  クリックして**機能の追加**、 をクリック**次へ**

6.  機能の選択 をクリックして**次**とネットワーク ポリシーとアクセス サービスで、提供された情報を確認し をクリックして**次**します。

7.  サービスの役割を選択し、クリックして**ネットワーク ポリシー サーバー**します。

8.  ネットワーク ポリシー サーバーに必要な機能をクリックして**機能の追加**クリックして、**次**します。

9.  インストール オプションの確認 をクリックして**必要な場合に、移行先サーバーを自動的に再起動**します。

10. をクリックして**はい**をクリックして、選択したことを確認**インストール**します。 <p>インストールの進行状況 ページでは、インストール プロセス中に状態を表示します。 ときに、プロセスが完了すると、メッセージ"が正常にインストール*ComputerName*"が表示されます*ComputerName*ネットワーク ポリシー サーバーをインストールしたコンピューターの名前を指定します。

11. **[閉じる]** をクリックします。

## <a name="configure-nps"></a>NPS の構成

すべての認証、承認、処理するために NPS を構成するために NPS をインストールして、接続のアカウンティング職務要求後は、VPN サーバーから受信します。

### <a name="register-the-nps-server-in-active-directory"></a>Active Directory での NPS サーバーを登録します。

この手順で接続要求の処理中にユーザー アカウントの情報にアクセスするためのアクセス許可があるように Active Directory でサーバーを登録します。

**手順:**

1.  サーバー マネージャーで、クリックして **ツール**, 、クリックして **ネットワーク ポリシー サーバー**します。 NPS コンソールが開きます。

2.  NPS コンソールで、右クリックして**NPS (ローカル)**、 をクリック**Active Directory にサーバーを登録**をオンにします。<p>ネットワーク ポリシー サーバーのダイアログ ボックスが開きます。

3.  ネットワーク ポリシー サーバー] ダイアログ ボックスで、[ **OK** 2 回クリックします。

NPS を登録するための代替メソッドは、次を参照してください。 [Active Directory ドメインに NPS サーバーを登録](../../../../../networking/technologies/nps/nps-manage-register.md)します。

### <a name="configure-network-policy-server-accounting"></a>ネットワーク ポリシー サーバー アカウンティングを構成する

この手順では、ネットワーク ポリシー サーバー アカウンティングを使用して、次のログ記録の種類のいずれかを構成します。

-   **イベントのログ記録**します。 監査とトラブルシューティングの接続の試行に主に使用されます。 NPS のイベント、NPS コンソールで、NPS サーバーのプロパティを取得することでログを構成できます。

-   **ユーザー認証およびアカウンティング要求をローカル ファイルにログ記録**します。 接続の分析と課金のために主に使用します。 攻撃を受けた後悪意のあるユーザーのアクティビティを追跡する方法が提供するため、セキュリティ調査ツールとしても使用します。 アカウンティング構成ウィザードを使用してローカル ファイル ログを構成することができます。

-   **準拠している Microsoft SQL Server の XML データベースにユーザー認証およびアカウンティング要求を記録**します。 1 つのデータ ソースとする NPS を実行している複数のサーバーを許可するために使用します。 また、リレーショナル データベースを使用する利点を提供します。 SQL サーバーのログ記録を構成するには、アカウンティング構成ウィザードを使用します。

ネットワーク ポリシー サーバーのアカウンティングを構成するには、次を参照してください。[を構成するネットワーク ポリシー サーバー アカウンティング](../../../../../networking/technologies/nps/nps-accounting-configure.md)します。

### <a name="add-the-vpn-server-as-a-radius-client"></a>VPN サーバーを RADIUS クライアントとして追加します。

[の Always On VPN リモート アクセス サーバーを構成する](vpn-deploy-ras.md) セクションで、インストール、VPN サーバーを構成するとします。 VPN サーバーの構成時に、VPN サーバー、RADIUS 共有シークレットを追加しました。 

この手順では、同じ共有シークレットのテキスト文字列を使用する VPN サーバーを NPS を RADIUS クライアントとして構成します。 VPN サーバーで使用されるまたは NPS サーバーと VPN サーバーの間の通信が失敗したのと同じテキスト文字列を使用します。

>[!IMPORTANT] 
>ネットワークに新しいネットワーク アクセス サーバー (VPN サーバー、ワイヤレス アクセス ポイント、認証スイッチ、またはダイヤルアップ サーバー) を追加するときに、NPS を認識しており、ネットワーク アクセス サーバーと通信できるように、NPS を RADIUS クライアントとして、サーバーを追加する必要があります。

**手順:**

1.  NPS サーバーで NPS コンソールで、ダブルクリック**RADIUS クライアントとサーバー**します。

2.  右クリック**RADIUS クライアント**クリック**新規**します。 新しい RADIUS クライアントのダイアログ ボックスが開きます。

3.  いることを確認、**この RADIUS クライアントを有効にする** チェック ボックスをオンします。

4.  **フレンドリ名**、VPN サーバーの表示名を入力します。

5.  **アドレス (IP または DNS)** NAS の IP アドレスまたは FQDN を入力します。<p>FQDN を入力すると、クリックして**確認**名前が正しいことと、有効な IP アドレスにマップすることを確認するかどうか。

6.  **共有シークレット**、操作を行います。

    1.  いることを確認**手動**が選択されています。

    2.  また、VPN サーバーに入力した強力なテキスト文字列を入力します。

    3.  共有シークレットの確認に共有シークレットを再入力します。

7.  **[OK]** をクリックします。 VPN サーバーは、NPS サーバーで構成されている RADIUS クライアントの一覧に表示されます。

## <a name="configure-nps-as-a-radius-for-vpn-connections"></a>NPS を RADIUS として VPN 接続を構成します。

この手順で組織のネットワーク上の RADIUS サーバーとして NPS を構成します。 NPS では、- VPN サーバーを組織または企業ネットワークにアクセスする特定のグループのユーザーのみを許可するポリシーを定義する必要があり、PEAP の認証要求で有効なユーザー証明書を使用する場合にのみです。

**手順:**

1.  NPS コンソールで、標準構成で**ダイヤルアップまたは VPN 接続用の RADIUS サーバー**が選択されています。

2.  クリックして**VPN またはダイヤルアップを構成する**します。<p>VPN またはダイヤルアップ ウィザードが開きます。

3.  クリックして**仮想プライベート ネットワーク (VPN) 接続**、 をクリック**次**します。

4.  指定のダイヤルアップまたは VPN サーバー、RADIUS クライアントでは、前の手順で追加した、VPN サーバーの名前を選択します。<p>たとえば、VPN サーバーの NetBIOS 名が RAS1 の場合は、クリックして**RAS1**します。

5.  **[次へ]** をクリックします。

6.  認証方法の構成で、次の手順を行います。

    1.  クリア、 **Microsoft 暗号化認証バージョン 2 (Ms-chapv2)** チェック ボックスをオンします。

    2.  をクリックして、 **Extensible Authentication Protocol**チェック ボックスをオンにします。

    3.  (アクセスおよびネットワーク構成のメソッドに基づく) の種類、 **Microsoft:保護された EAP (PEAP)**、 をクリック**構成**します。<p>保護された EAP プロパティの編集 ダイアログ ボックスが開きます。

    4.  クリックして**削除**をセキュリティで保護されたパスワード (EAP-MSCHAP v2) EAP の種類を削除します。

    5.  **[追加]** をクリックします。 EAP の追加 ダイアログ ボックスが開きます。

    6.  をクリックして**スマート カードまたはその他の証明書**、 をクリック**OK**します。

    7.  をクリックして**OK**を保護された EAP のプロパティの編集を閉じます。

7.  **[次へ]** をクリックします。

8.  ユーザー グループの指定で、次の手順を行います。

    1.  **[追加]** をクリックします。 [ユーザー、コンピューター、サービス アカウント、またはグループ] ダイアログ ボックスが開きます。

    2.  型**VPN ユーザー**  をクリック**OK**します。

    3.  **[次へ]** をクリックします。

9.  IP フィルターの指定 をクリックして**次**します。

10. 暗号化設定の指定 をクリックして**次**します。 すべての変更を加えないでください。<p>これらの設定は、Microsoft Point-to-Point 暗号化 (MPPE) 接続では、このシナリオをサポートしていないにのみ適用されます。

11. 領域名を指定するには、クリックして**次**します。

12. クリックして**完了**ウィザードを閉じます。

## <a name="autoenroll-the-nps-server-certificate"></a>NPS サーバー証明書の自動登録

この手順でグループ ポリシー、ローカルの NPS サーバーで手動で更新します。 グループ ポリシーの更新、証明書の自動登録が構成されていて正しく機能している、ローカル コンピューターでは自動-登録されている場合、証明書、証明機関 (CA) によって。

>[!NOTE]  
>または、ドメイン メンバー コンピューターを再起動すると、ユーザーがドメイン メンバー コンピューターにログオンしたときに、グループ ポリシーは自動的に更新します。 また、グループ ポリシーを定期的に更新します。 既定では、この定期的な更新は 90 分に 1 に最大 30 分のランダム オフセットでは発生します。

メンバーシップ **管理者**, 、または同等の権限は、この手順を実行するために必要な最小値。

**手順:**

1.  NPS では、Windows PowerShell を開きます。

2.  Windows PowerShell プロンプトで「 **gpupdate**, 、ENTER キーを押します。

## <a name="next-step"></a>次の手順
[手順 5 です。Always On VPN の DNS とファイアウォール設定を構成する](vpn-deploy-dns-firewall.md):この手順では、Windows PowerShell またはサーバー マネージャーの役割の追加と機能のウィザードのいずれかを使用してネットワーク ポリシー サーバー (NPS) をインストールします。 また、すべての認証、承認、および接続のアカウンティングの職務を処理するために NPS を構成する VPN サーバーから受信した要求。



---

