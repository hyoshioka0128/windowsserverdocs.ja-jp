---
title: "Windows Server Essentials でのサーバー記憶域を管理します。"
description: "Windows Server Essentials を使用する方法について説明します。"
ms.custom: na
ms.date: 10/03/2016
ms.prod: windows-server-2016-essentials
ms.reviewer: na
ms.suite: na
ms.tgt_pltfrm: na
ms.topic: article
ms.assetid: 1836682e-c7bb-4dd5-a2b5-6ff032693574
author: nnamuhcs
ms.author: coreyp
manager: dongill
ms.openlocfilehash: e0f65dfd25afbd584764d33904ba82e4da4c5443
ms.sourcegitcommit: db290fa07e9d50686667bfba3969e20377548504
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 12/12/2017
---
# <a name="manage-server-storage-in-windows-server-essentials"></a>Windows Server Essentials でのサーバー記憶域を管理します。

>Windows Server 2016 Essentials、Windows Server 2012 R2 Essentials での Windows Server 2012 Essentials を適用対象:
   
 Windows Server Essentials を管理できます (ハード ドライブと記憶域スペースを含む) すべてのサーバー記憶域から、**ハード ドライブ**ページ、**記憶域**ダッシュ ボードのタブ。  
  
 次のセクションでは、サーバー記憶域の増加、理解、記憶域スペースを使用し、およびハード ドライブを管理するうえで役立つ情報を提供します。  
  
-   [ダッシュ ボードを使用して、ハード ドライブを管理します。](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_1)  
  
-   [サーバー上の記憶域を増加します。](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_2)  
  
-   [ハード ドライブのチェックと修復を実行します。](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_Check)  
  
-   [形式のハード ドライブ](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_3)  
  
-   [新しいハード ドライブを追加します。](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_4)  
  
-   [記憶域スペースの概要](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_5)  
  
-   [記憶域スペースを作成します。](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_6)  
  
##  <a name="BKMK_1"></a>ダッシュ ボードを使用して、ハード ドライブを管理します。  
 Windows Server Essentials では、ダッシュ ボードを使用してサーバーに接続されているすべてのハード ドライブを管理することができます。 ダッシュ ボードで**記憶域**] タブの [**ハード ドライブ**データやサーバーのバックアップを格納するため、サーバーで使用可能なすべてのハード ドライブが表示されます。 サーバーは各ハード ドライブ上の未使用領域を監視し、ハード ドライブの空き領域が少なくなった場合にアラートを表示します。 **ハード ドライブ**] タブには、次の情報が表示されます。  
  
-   各ハード ドライブの名前  
  
-   各ハード ドライブの容量  
  
-   各ハード ドライブ上の使用済み領域の量  
  
-   各ハード ドライブ上の空き領域の量  
  
-   各ハード ドライブの状態空白の状態は、ハード ドライブが正しく動作していることを意味します。  
  
-   詳細ウィンドウで、選択されたハード ドライブが (物理ディスク) ではなく、記憶域スペース上にある場合 (記憶域プール、記憶域スペース、およびハード ドライブ) のすべての記憶域スタック情報が表示されます。  
  
 次の表は、ダッシュ ボードとその説明で利用可能なハード ドライブ管理タスクを示します。 一部のタスクには、ハード ドライブが選択されている場合にのみが表示されます。  
  
### <a name="available-hard-drive-management-tasks"></a>利用可能なハード ドライブ管理タスク  
  
|タスク名|説明|  
|---------------|-----------------|  
|**ハード ドライブのプロパティを表示します。**|開き、*HardDriveName***プロパティ**ページ。 このタスクは、ハード ドライブが選択されているときに表示されます。 **全般的な**のタブ、*HardDriveName*プロパティ ページには、次のような追加タスクが含まれています。<br /><br /> -   **ドライブ クリーンアップ**: (このタスクは Windows Server Essentials で使用できるのみです)、ハード ドライブ上の未使用のファイルをクリーンアップすることができます。<br />-   **チェックし、修復**: ハード ドライブにファイル システム エラーを確認し、検出されたエラーを自動的に修復を試みます。<br /><br /> **シャドウ コピー**のタブ、*HardDriveName***プロパティ**] ページでは、シャドウ コピーを有効にすることができます。 このタブには、シャドウ コピーの実行がスケジュールされて、次回も表示されます。|  
|**記憶域スペースを管理します。**|**注:**、Windows Server Essentials の既存の記憶域スペースがあるときにこのタスクは表示のみです。<br /><br /> 開き、**記憶域スペース**コントロール パネルの [作成および記憶域プールおよび記憶域スペースを管理できます。|  
|**記憶域スペースを作成します。**|作成を記憶域プールの容量を増やすには 1 つまたは複数のハード ドライブを使用することができますが記憶域スペースのウィザードが開きます。|  
|**記憶域プールの容量を増加します。**|**注:**このタスクにのみ選択されたハード ドライブが記憶域スペース上にある場合に表示します。<br /><br /> 記憶域プールの容量の増加を 1 つまたは複数のハード ドライブを使用することができますが、記憶域プール ウィザードの容量の増加を開きます。|  
  
##  <a name="BKMK_2"></a>サーバー上の記憶域を増加します。  
 サーバー上の記憶域を増やすと、サーバーに追加の内部ハード_ディスクを追加することができます。 追加の内部ハード ディスクを追加、する必要があります、サーバーをシャット ダウン、内蔵ハード_ディスクを追加し、サーバーを再起動します。 ハード_ディスクが SCSI コントローラーに接続されている場合、サーバーをシャット ダウンする必要はありません。 その場合は、ハード ディスクを接続できるサーバーの実行中にします。  
  
 追加するハード ディスクがフォーマット済みかどうかに応じて、次のいずれかの操作を行います。  
  
-   **フォーマット**内蔵ハード_ディスクが NTFS または ReFS でフォーマットされた場合は、サーバーが、ドライブ文字を割り当てることで、ハード _ ディスクが表示されます、**ハード ドライブ**] タブ。今すぐ作成したり、サーバー フォルダーを新しいハード ドライブに移動できます。  
  
-   **フォーマットされていない**内蔵ハード_ディスクが未フォーマットの場合、以下の警告が表示されます。1 つまたは複数の書式設定されていないハード ドライブがサーバーに接続します。 ハード ディスクをフォーマットするのにには、次の手順を使用します。  
  
#### <a name="to-format-the-hard-disk"></a>ハード ディスクをフォーマットするには  
  
1.  ダッシュ ボードを開きます。  
  
2.  ナビゲーション ウィンドウで、アラート アイコンをクリックして起動、**アラート ビューアー**、Windows Server Essentials で、または**正常性の監視**] タブの [Windows Server Essentials でします。  
  
3.  **アラート ビューアー**または**正常性の監視**] タブで、[アラート] をクリックし、[タスク] ウィンドウで、] をクリックして**この問題のトラブルシューティングを行う**します。  
  
4.  追加、新しいハード ドライブのウィザードを完了するための手順に従います。  
  
###  <a name="BKMK_Clean"></a>ハード ドライブをクリーンアップするには  
  
1.  ダッシュ ボードを開きます。  
  
2.  ナビゲーション ウィンドウで、をクリックして**記憶域**、] をクリックし、**ハード ドライブ**します。  
  
3.  **ハード ドライブ**セクション、および [タスク] ウィンドウで、新しく追加されたハード_ディスクに割り当てられたドライブ文字を選択し、をクリックして**ハード ドライブのプロパティを表示**します。  
  
4.  **< Driveletter\ > プロパティ**] の [、**全般**] タブ、[**ドライブ クリーンアップ**します。  
  
##  <a name="BKMK_Check"></a>ハード ドライブのチェックと修復を実行します。  
 ハード ドライブのチェックし、修復プロセスをハード ドライブに保存されているファイル システムの正常性を確認します。 実行する、**chkdsk**にバックアップ ファイルが格納されているボリューム上で処理します。 以下のアラート問題はチェックを実行することで解決でき、修復をハード ドライブ。  
  
-   サーバー バックアップの 1 つまたは複数のハード ドライブをチェックする必要があります。  
  
#### <a name="to-check-and-repair-hard-drives"></a>チェックと修復のハード ドライブ  
  
1.  ダッシュ ボードを開きます。  
  
2.  をクリックして**サーバー フォルダーとハード ドライブ**、] をクリックし、**ハード ドライブ**します。  
  
3.  エラーが表示されているハード ドライブを選択し、[**ハード ドライブのプロパティを表示**します。  
  
4.  **チェックし、修復**] タブ、[**チェックし、修復**します。  
  
##  <a name="BKMK_3"></a>形式のハード ドライブ  
 書式設定されていない内蔵ハード ドライブがサーバーで検出されると、そのフォーマット プロセスを案内正常性アラートが表示されます。 [追加ハード ドライブの新規作成ウィザードの指示に従って、ハード ドライブをフォーマットおよび、次の方法のいずれかで、ハード ドライブを構成することができます。  
  
1.  ハード ドライブをフォーマットし、それにドライブを自動的に作成します。 ウィザードが完了したときに、このオプションを選択した場合は、論理ハード ドライブが NTFS ファイル システムでフォーマットされた 1 つが作成されます。  
  
2.  ハード ドライブをフォーマットし、サーバー バックアップ用に設定します。 このオプションを選択する場合、バックアップのセットアップ サーバー ウィザードが起動され、サーバー バックアップの構成、そこでいます。  
  
3.  記憶域スペースではありません t が存在する場合は、記憶域スペースを作成する、新しいハード ドライブを使用します。 記憶域スペースを作成するには、少なくとも 2 台のハード ドライブが必要です。  
  
4.  記憶域スペースが既に存在する場合は、記憶域プールの容量を増やす、新しいハード ドライブを使用します。 このオプションは、サーバーで作成した既存の記憶域スペースがある場合にのみ表示されます。 このオプションを選択する場合、ウィザードは、記憶域プールにこのハード ドライブを追加します。  
  
##  <a name="BKMK_4"></a>新しいハード ドライブを追加します。  
 Windows Server Essentials を実行しているサーバーに新しいハード ディスク ドライブを接続することができます。  
  
-   [新しいハード ドライブを使用して、サーバー フォルダーを格納するには](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_4a)  
  
-   [新しいハード ドライブを使用してサーバー バックアップを保存するには](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_4b)  
  
-   [新しいハード ドライブを使用して、記憶域プールの容量を増やす](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_4c)  
  
###  <a name="BKMK_4a"></a>新しいハード ドライブを使用して、サーバー フォルダーを格納するには  
 サーバー フォルダーを格納する、新しいハード ドライブを使用するには、ハード ドライブに新しいサーバー フォルダーを追加したり、既存のサーバー フォルダーをハード ドライブに移動できます。  
  
##### <a name="to-store-server-folders"></a>サーバー フォルダーを格納するには  
  
1.  ダッシュ ボードを開きます。  
  
2.  をクリックして、**記憶域**タブをクリックし、をクリックして**サーバー フォルダー**します。  
  
3.  **サーバー フォルダーのタスク**] ウィンドウで、次のいずれかの操作。  
  
    1.  サーバー フォルダーを追加する] をクリックして**フォルダーを追加**します。  
  
    2.  サーバー フォルダーを移動するフォルダーを選択、新しいハード ドライブに移動し、をクリックする**フォルダーを移動**します。  
  
    > [!NOTE]
    >  ハード ドライブを参照して、フォルダーを作成することがなく、サーバー フォルダーの場所として選択する場合、次のエラー メッセージが表示されます: **(C:\\、D:\\) などのルート ディレクトリ サーバー フォルダーとして追加することはできません。新しいフォルダーを作成またはルート ディレクトリの下の既存の 1 つを選択し、もう一度やり直してください**します。 このエラーを解決するには、新しく追加されたハード ドライブに新しいフォルダーを作成し、サーバー フォルダーを格納する場所として、新しいフォルダーを選択します。  
  
4.  指示に従ってウィザードを完了します。  
  
 サーバー フォルダーの移動の詳細については、次を参照してください。[の追加とサーバー フォルダーの移動](Manage-Server-Folders-in-Windows-Server-Essentials.md#BKMK_5)します。  
  
###  <a name="BKMK_4b"></a>新しいハード ドライブを使用してサーバー バックアップを保存するには  
 新しく追加されたハード ドライブを使用すると、サーバー バックアップを保存します。  
  
##### <a name="to-store-server-backups"></a>サーバー バックアップを保存するには  
  
1.  ダッシュ ボードを開きます。  
  
2.  をクリックして、**デバイス**] タブで、一覧ウィンドウからサーバーを選択し、タスク ウィンドウから、次のいずれかを実行します。  
  
    1.  サーバーのバックアップ構成しなかった場合は、サーバーで、クリックして**サーバー バックアップのセットアップ**します。  
  
    2.  サーバーのバックアップが、サーバー上で構成されている場合、以下の] をクリックして**サーバー バックアップのカスタマイズ**します。  
  
     バックアップのセットアップ サーバー ウィザードが表示されます。  
  
3.  **バックアップ先の選択**] ページで、バックアップ先として、新しいハード ドライブを選択します。  
  
4.  指示に従ってウィザードを完了します。  
  
###  <a name="BKMK_4c"></a>新しいハード ドライブを使用して、記憶域プールの容量を増やす  
 記憶域プールの容量が不足している記憶域プールの作成ウィザードの容量の増加を使用して、記憶域プールに新しいハード ドライブを追加することで、記憶域プール容量を増やすことができることを示すアラートが表示されます。  
  
> [!NOTE]
>  サーバーで記憶域プールを作成した場合にのみ、この手順を実行することができます。  
  
##### <a name="to-increase-storage-pool-capacity"></a>記憶域プールの容量を増やす  
  
1.  ダッシュ ボードを開きます。  
  
2.  をクリックして、**記憶域**タブをクリックし、をクリックして**ハード ドライブ**します。  
  
3.  低容量が表示されているドライブを選択します。  
  
4.  作業ウィンドウで、次のように選択します。**記憶域プールの容量の増加**します。 記憶域プールの作成ウィザードの容量の増加が表示されます。  
  
5.  指示に従ってウィザードを完了します。  
  
##  <a name="BKMK_5"></a>記憶域スペースの概要  
 グループ化する記憶域スペースによりディスクが記憶域プールに一緒にします。 プールの容量は記憶域スペースを作成し、使用できます。 記憶域スペースは仮想ドライブに表示される、**ハード ドライブ**ダッシュ ボードのタブ。 そのため、他のドライブなどの記憶域スペースを使用することができます、s ファイルとそれらの作業を容易にします。 プールの容量の不足を実行するときに、大きな記憶域スペースを作成して、記憶域プールにさらにドライブを追加します。 ドライブの障害の影響を受けず双方向ミラーと記憶域スペースを作成するには、記憶域プール内で 2 つまたは複数のディスクがあれば、ですか? も 2 つのドライブの障害か。3 方向ミラー記憶域スペースを作成する場合。  
  
 記憶域スペースを作成するに必要なは 1 つまたは複数の余分なドライブさらに Windows がインストールされているドライブにします。 これらのドライブには、内部または外部のハード ドライブまたはソリッド ステート ドライブを指定できます。 記憶域スペースを含む USB、SATA、SAS ドライブでは、さまざまな種類のドライブを使用できます。  
  
> [!NOTE]
>  Windows Server Essentials を実行しているサーバーで記憶域スペースを構成する場合は、工場出荷時リセットを実行することはできません、**データのクリーンアップ**オプションです。 この問題を回避最初に記憶域スペースを削除しで工場出荷時リセットを実行するには、**データのクリーンアップ**オプションです。  
  
 記憶域スペースの詳細については、次を参照してください。[記憶域スペースに関してよく寄せられる質問 (FAQ)](https://social.technet.microsoft.com/wiki/contents/articles/11382.storage-spaces-frequently-asked-questions-faq.aspx)します。  
  
##  <a name="BKMK_6"></a>記憶域スペースを作成します。  
 サーバー上の記憶域スペースの使用を開始するには、次の最小要件を満たす必要があります。  
  
-   Windows Server Essentials を実行しているサーバーは、その他の物理ドライブ (ブート ドライブだけでなく) に接続する必要があります、ドライブでのすべてのボリュームをホストしていない必要があります、10 GB の最小容量が必要です。 1 つの物理ドライブが必要です。記憶域プールを作成するには回復力のあるミラー記憶域スペースを作成するには、次の 2 つの物理ドライブの最小値が必要です。  
  
-   次の 2 つの物理ドライブの最小値は、パリティや双方向のミラーリングによる回復性と記憶域スペースを作成する必要があります。  
  
> [!NOTE]
>  Windows Server Essentials を実行しているサーバーで記憶域スペースを構成する場合は、工場出荷時リセットを実行することはできません、**データのクリーンアップ**オプションです。 この問題を回避最初に記憶域スペースを削除しで工場出荷時リセットを実行するには、**データのクリーンアップ**オプションです。  
  
#### <a name="to-create-a-storage-space-in-windows-server-essentials"></a>Windows Server Essentials で記憶域スペースを作成するには  
  
1.  追加または記憶域スペースと共に Windows Server Essentials を実行しているサーバーにグループ化するすべてのドライブを接続します。  
  
2.  ダッシュ ボードで、をクリックして**詳細設定: 記憶域スペースの管理**します。  
  
3.  をクリックして**新しいプールと記憶域スペースを作成**します。  
  
4.  クリックして、新しい記憶域スペースに追加するドライブを選択して**プールを作成する**します。  
  
5.  ドライブの名前と文字とし、画面のレイアウトを選択します。 **双方向ミラー**、**3 方向ミラー**、および**パリティ**ドライブの障害から記憶域スペース内のファイルを保護できます。  
  
6.  最大サイズを記憶域スペースがリーチの拡大、クリックして入力**記憶域スペースを作成する**します。  
  
 、Windows Server Essentials でを作成、双方向ミラー化された記憶域スペースの作成] ページを使用して記憶域スペースの作成ウィザード、ダッシュ ボードからです。  
  
#### <a name="to-create-a-storage-space-in-windows-server-essentials"></a>Windows Server Essentials で記憶域スペースを作成するには  
  
1.  追加または記憶域スペースと共に Windows Server Essentials を実行しているサーバーにグループ化するすべてのドライブを接続します。  
  
2.  ダッシュ ボードで、をクリックして**記憶域スペースの管理**します。 作成する記憶域スペースの作成ウィザードが表示されます。  
  
3.  ウィザードを完了するための手順に従います。  
  
 記憶域プールの容量を増やす方法の詳細については、次を参照してください。[を記憶域プール容量を増やすには、新しいハード ドライブを使用して](Manage-Server-Storage-in-Windows-Server-Essentials.md#BKMK_4c)します。  
  
## <a name="see-also"></a>参照してください。  
  
-   [サーバー フォルダーを管理します。](Manage-Server-Folders-in-Windows-Server-Essentials.md)  
  
-   [Windows Server Essentials を使用します。](../use/Use-Windows-Server-Essentials.md)  
  
-   [Windows Server Essentials を管理します。](Manage-Windows-Server-Essentials.md)
